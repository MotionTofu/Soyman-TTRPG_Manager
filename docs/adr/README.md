# Решения об архитектуре (ADR)

`AGENTS.md` объявляет доменными документами репозитория `CONTEXT.md` и этот
каталог. Весь остальной `docs/` — рабочий материал и в репозиторий не идёт;
для `docs/adr/` в `.gitignore` сделано исключение (`/docs/*` + `!/docs/adr/`),
иначе ADR никогда бы сюда не попали.

Одна запись — одно принятое решение: что решили, из чего выбирали и почему
отвергнутый вариант хуже. Имя файла — `NNNN-краткое-имя.md`.
