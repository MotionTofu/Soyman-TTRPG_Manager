// Через двадцать секунд ожидания на заставке появляется пояснение: столько
// длится обычно только перестройка базы после обновления, и Мастеру надо
// сказать, что приложение занято делом, а не зависло. Ставим таймер здесь, а
// не шлём ipc из main.js: странице для этого не нужно ничего, кроме часов.
setTimeout(() => {
  const el = document.getElementById("slow");
  if (el) el.hidden = false;
}, 20000);
