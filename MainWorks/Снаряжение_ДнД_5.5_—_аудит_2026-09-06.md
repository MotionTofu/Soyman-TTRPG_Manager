# Снаряжение чарника ДнД 5.5 — аудит с 4 сторон + план

Дата: 2026-09-06. Ветка `main`, репо `E:\Claude\01_Blender`.
Статус: аудит + волны 0–3 выполнены; затем дерево было внешне сброшено и вся работа восстановлена заново поверх нового дерева (параллельная сессия ввела свою модель зарядов — использована как есть).
Правило: **НИКОГДА. НИКОГДА. НИКОГДА не восстанавливать что-то через гитхаб самостоятельно. ВСЕГДА СПРАШИВАТЬ перед КАЖДЫМ откатом.**

Источники: `design_revision.md` (§1–§5), план доработки чарника, код (`DndCharacterForm.tsx`, `types.ts`, `dndEquipment.ts`, `dnd-sheet.css`, `DndCharacterWizard.tsx`, `DndTransferBox.tsx`, `DndCoinCalculator.tsx`, `dndPrefs.ts`).

Решения владельца (зафиксированы до кода):
- **D1 и D2 не вносим** — кромка 3px у магии и заливка трансферов оставлены как принятые отклонения от §1.3/§1.7.
- **Единицы веса — настройка** `Настройки > Система > ДнД 5.5`: фунты/кг, только показ.
- **Перегруз: СИЛ × 15 фунтов**, удвоения (Мощное телосложение и т.п.) стакаются (×2^n).
- **Порядок волн** — 0 → 1 → 2 → 3.
- Строка — только имя; характеристика — в карточке по тапу. Чип «мало» — нет (кроме рационов 1–3). Строки — плашки, кнопки по краям. Имя — до ~25 знаков в строке, перенос целыми словами.
- Калькулятор монет: авто-сообщение перевода достаточно (своей записки нет), делёж без эм, входы у кошелька и в передачах.
- Проклятия (до кода): скрытность только в интерфейсе; список мастера — все проклятые с пометкой «надето»; первый заход сразу с механикой (`curse:{text,revealed}`).

---

## P0 — критично

### Код
- **K0-1.** Пикер/dnd-бросок теряли `entryId` (предмет становился «ручным»); магпредметы не получали `magical`. Исправлено: `entryId` + мета + `magical` во всех точках.
- **K0-2.** Ключи списков по индексу → стабильные `id` (`makeEquipmentId`/`ensureEquipmentIds`, нормализация + все создатели).
- **K0-3.** `qty/weight` без семантики → валидация (целое ≥0 / число ≥0 с единицей), единые `parseQty`/`parseWeight`; сводка исключает `transferOut`; монеты — только цифры.
- **K0-4.** Настройка числом без связи → `attuned` на строке, счётчик считается сам.
- **K0-5.** Две системы передач в строке — свернуты под `···` (Волна 2.3).
- **K0-6.** Дубли блокировали расходники → повтор `qty+1` (`isStackableEquipmentEntry`).

### Пользователь + Мастер
- Степпер −1/+1 там, где есть счёт; характеристика в карточке по тапу; пикер с фильтрами (тип/редкость/настройка) и сортировками; секции-шаблон + перемещение селектом; кошелёк с итогом; поиск по вкладке + фильтр надето/магия; перегруз с бейджем; рационы в отдыхе; калькулятор монет.

### Дизайн (без D1/D2)
- Шапка-плашка `dnd-equipment-head`; сводка — слова прозой, числа моно; пустая вкладка — приглашение с CTA; внутренние радиусы 0; мишени 44px; трансферы формой, не заливкой-мешаниной (сами заливки оставлены).

---

## План (утверждён, выполнен)

**Волна 0:** 0.1 entryId+magical; 0.2 id строк; 0.3 валидация/сводка/монеты; 0.4 стакаемость; 0.5 единицы веса + перегруз.
**Волна 1:** 1.1 степпер; 1.2 характеристика (затем убрана из строки в карточку); 1.3 настройка из строк; 1.4 пикер; 1.5 кошелёк (затем кнопки дележа убраны в калькулятор); 1.6 секции; 1.7 поиск.
**Волна 2:** 2.1 плашка/голоса; 2.2 пустые/радиусы/мишени; 2.3 трансфер-··· + ручной порядок.
**Волна 3:** рационы (детект, чип, отдых); заряды (на их модели `chargesMax/chargesLeft`); проклят-замок; реплика-бейдж; ретрай описаний + abort; печать.
**Вне волн:** калькулятор монет; рамки монеты/настройки; «Итого» вправо; строки-плашки 25ch.

Проверка каждой волны: `tsc -p tsconfig.app.json --noEmit` + `oxlint`, пишущие — только scratch, живую базу не трогать (кроме SELECT).

---

## Отчёты о проделанной работе (дописывать сюда же)

- 2026-09-06: аудит завершён, план обсуждён и утверждён. Кода не менял, откатов через git не делал.
- 2026-09-06, волны 0–3 + калькулятор + правки по скриншотам — всё сделано и проверено (`tsc` 0, `oxlint` 0). Детали — в истории чата сессии.
- 2026-09-06, ВНЕШНИЙ СБРОС: всё несейвленное снесено не мной (параллельная сессия/владелец). В git моего не было — откатом не вернуть. Stash и Local History пустые (проверено).
- 2026-09-06, реконструкция поверх нового дерева: типы (`id/attuned/rarity/requiresAttunement/cursed/itemType`), `dndEquipment.ts` (id/снапшот/настройка/перегруз/стакаемость/рационы/доспехи), вес в префах и настройках, квик-вью (степпер/ромб/поиск/сводка/плашка/рамки/трансфер-···/ручной порядок/приглашение/перемещение/ретрай), правка (id/дроп/шаблон), пикер (фильтры/стаки/скролл), визард и реплики (id), отдых (рацион), счётчик из строк, кошелёк (фрейм/итог/калькулятор), `DndTransferBox` (калькулятор bare), `DndCoinCalculator.tsx`, CSS (плашки/чипы/рамки/печать/имя 25ch), заряды — на модели параллельной сессии (`chargesMax/chargesLeft`, их степпер и светание на рассвете уже были). Поправка аудита: `fetchEquipmentMeta` уже возвращал `entryId`. Проверки — ниже.
- …
