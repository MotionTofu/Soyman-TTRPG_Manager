# Грилинг: режим «Древо / Корень [beta]» в Географии сеттинга

Дата: 2026-09-04. Формат: skill `grilling`, 2 раунда, фронт вопросов → решения.

## Контекст запроса
Сделать в профиле сеттинга, раздел География, режим просмотра локаций в виде древа/корня, пометить `[beta]`.
Ветвление: несколько локаций первого уровня — основание древа/корня, от них линии в локации второго уровня и т.д.
Хочется двигать ноды, дочерние отзывчивы к движению родительской.

## Факты (найдены агентом, не спрашивали у пользователя)
- `client/src/pages/SettingDetailPage.tsx:129-140,1086,1433-1439` — таб «География» → `GeographyTab` → `<LocationTree/>`.
- `client/src/components/LocationTree.tsx:23-589` + `591-1035` — рекурсивные `<details data-location-id>`, переключателя Список/Сетка/Карта нет, только дерево vs плоский список при поиске (`457-545`), сорт `name|kind`, фильтры kind/map/desc, Развернуть/Свернуть все, миникарта при >10.
- Модель: `server/src/db/schema.sql:365-386 setting_locations(id,setting_id,parent_id,name,kind,...)` — нет level/sort/position, только `ORDER BY name`. API `server/src/routes/settingLocations.ts:115-143 GET, 981-1033 PUT /:id/parent` с проверкой цикла.
- Библиотеки: `client/package.json:13-19` — `@xyflow/react ^12.11.3` (только Холст), нет d3/dnd-kit/leaflet. Граф связей самописный canvas (`RelationGraph.tsx`).
- Drag сейчас: только смена родителя через HTML5 DnD (`LocationTree.tsx:768-808`), корень через `handleDropRoot:224-235`. Координат у узлов дерева нет.

## Раунд 1
- Q1 Переключатель режимов: А) сегмент `Дерево | Корень [beta]` + localStorage — ОТВЕТ: а.
- Q2 Ориентация древа: А) сверху-вниз Б) слева-направо В) радиальное — ОТВЕТ: переключатель из трёх: снизу-вверх + слева-направо + сверху-вниз.
- Q3 Двигание: А) free-drag + сабтри на дельту, localStorage — ОТВЕТ: а.
- Q4 Технология: А) `@xyflow/react` — ОТВЕТ: ок.
- Q5 Нода: name+kind+счётчик+клик+коллапс, тонкие линии — ОТВЕТ: да, да.

## Раунд 2
- Q6 Drag «двигать» vs «сменить родителя»: А) тело двигает, drop на чужую ноду меняет parent — ОТВЕТ: хорошо.
- Q7 Автолейаут: А) `dagre` rankdir TB/BT/LR — ОТВЕТ: хорошо.
- Q8 Сброс: А) новые в автопозицию, ручные не трогаем, кнопка сброса, ключ `geography-rootpos-${settingId}` — ОТВЕТ: окей.
- Q9 Лес + фильтры + 100+ нод: лес в одном полотне, поиск+kind, коллапс, Fit+Minimap, без виртуализации — ОТВЕТ: ок.
- Q10 Бейдж [beta] в сегменте + хинт «локально», пустое — плейсхолдер — ОТВЕТ: да.

## Зафиксировано
Сегмент `Дерево | Корень [beta]`, три ориентации, free-drag с дельта-сдвигом сабтри в localStorage, `@xyflow/react` + `dagre`, карточка name/kind/счётчик/клик/коллапс, drop-на-родителя = смена parent с защитой от цикла, кнопка сброса, Fit+Minimap, бейдж в сегменте + хинт.
