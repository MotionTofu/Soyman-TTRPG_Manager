# Реализация: «Корень [beta]» — готово

## Файлы
- NEW `client/src/geographyRootLayout.ts` — послойная раскладка леса без новых зависимостей (в плане был dagre — заменён своим layout, чтобы не тащить dep в beta), `load/save/resetRootPositions(settingId)`, `collectSubtreeIds`, `isDescendantOf`.
- NEW `client/src/components/LocationRootNode.tsx` — нода `name + kind + счётчик детей + 🗺 + коллапс + Link /locations/:id`.
- NEW `client/src/components/LocationRootGraph.tsx` — `@xyflow/react`: лес, 3 ориентации (`TB/BT/LR`), поиск + kind-фильтр, коллапс веток, `FitView` + `MiniMap` + `Controls`, `Сбросить раскладку`.
- EDIT `client/src/pages/SettingDetailPage.tsx` — `GeographyTab`: сегмент `Дерево | Корень [beta]` (`localStorage:geography-view-${settingId}`), импорт графа.

## Поведение
- Drag тела = двигать; весь сабтри сдвигается на дельту (`dragStartRef` снапшот → `handleNodeDragStop`), persist `geography-rootpos-${settingId}`.
- Drop на чужую карточку (центр в боксе 220×~76 + запас) = `PUT /:id/parent`, запрет цикла через `isDescendantOf` + алерт.
- Новые локации/смена parent извне — автопозиция, ручные не затираются. Архивные скрыты. Пусто — `EmptyState`.

## Проверки
- `oxlint` по трём новым файлам: 0 warnings/errors.
- `tsc --noEmit -p tsconfig.app.json`: чисто.
- Ручной прогон (нужен браузер): лес 2–3 корня × 3 глубины; drag родителя тянет детей; reload держит; 3 ориентации; drop меняет parent; фильтры; пусто.
