# Карты — на будущее (отложено из аудита 01)

Дата: 2026-09-03. Источник: `MapsAudition_01.md` (аудит 01). Вынесено по решению владельца — не делать сейчас.

## 1. Показ на стол (бывший P2-5) — ЧАСТИЧНО ЗАКРЫТ 2026-09-03
- Сделано (Этап 5): кнопка «Показать игрокам» в `/maps/:id` — выставляет `player_visible=1`,
  копирует ссылку в буфер, показывает тост со ссылкой (`shareWithPlayers`).
- Остаток (условие возврата не снято): автопуш/автооткрытие у игроков — канала трансляции нет;
  второй экран / туман войны — отдельные заходы.
- Исходная постановка: кнопка «Показать игрокам» в `/maps/:id` (выставить `player_visible`, тост/ссылка, автооткрытие у игроков, если есть канал трансляции); позже — второй экран / туман войны.
- Контекст: `SendMapToSessionModal` сейчас wired только к `LocationMap` (`LocationMap.tsx:1099`), к плиточным картам — нет. Гейт видимости уже есть (`playerAccess.ts:54`, `maps.ts:19-21,31,61`).
- Условие возврата: после привязок карт к кампаниям/сеттингам (P2-4) и легенды в PNG (P0-7) — показывать нечего без легенды.

## 2. Мобильный тулбар (бывший P2-6) — ЧАСТИЧНО ЗАКРЫТ 2026-09-03
- Сделано (Этап 5): ряды инструментов на ≤640px — горизонтальный скролл в одну строку
  (`index.css`, `@media (max-width: 640px)` на `.map-editor .res-toolbar[role="toolbar"]`).
- Остаток: сегмент-контрол/шит, липкие тулбары; проверка на 390 px в «Соевом панке» и «Соевом нуаре» —
  по-прежнему за владельцем (ручной прогон).

## 3. Бэклог второго прохода (аудит 03, 2026-09-03 — не сейчас)

- **M5. Слой NPC/токенов.** Маркеры существ для тактического боя (гоблин стоит здесь) — отдельный спек-тикет, слой поверх комнат/дверей, видимость игрокам по флагам.
- **U2. Панель истории undo.** Список последних шагов («Кисть: лес ×12», «Подпись: Вотердип»), клик — jump-to. Сейчас undo вслепую.
- **M2. Пакетная замена типа.** «Все равнины → холмы» одной командой (заливка G — только связная область).
- **M7. История сидов.** Избранные сиды в localStorage («тот красивый остров»).
- **U3. Переключение двух последних карт.** Хоткей НЕ Ctrl+Tab (зарезервирован браузером) — напр. Alt+←; `lastMapId` в localStorage.
- **U8. Тач-жест undo.** Свайп двумя пальцами влево на планшете за столом (вместе с мобильным тулбаром, п.2).
