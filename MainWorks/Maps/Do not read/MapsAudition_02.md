# Карты — аудит 02 (генератор + редактор, повторный)

Дата: 2026-09-03. Область: `client/src/pages/MapsListPage.tsx`, `MapEditorPage.tsx` (3124 строки),
`client/src/maps/{mapTypes,grid,generate,render,dungeon}.ts`, `server/src/routes/maps{,Validation}.ts`,
правила `design_revision.md` §1–§7.
Метод: чтение кода построчно + сверка с `MainWorks/Maps/00_Отчёт_фундамент.md`, `План.md`, `Находки.md`,
`На_будущее.md`, `MapsAudition_01.md`. Живой прогон в браузере не делался. Ничего не правилось.
Проверка типов: `npx tsc -p tsconfig.app.json --noEmit` в `client/` (см. P0-A про ловушку корневого конфига).

## Сквозные запреты (действуют всегда)

1. Никаких откатов/восстановлений через git/GitHub самостоятельно. Перед КАЖДЫМ откатом — вопрос с вариантами.
2. Никаких фиксов из плана ниже без отдельного «добро» после прочтения аудита.

Этот файл — журнал: наблюдения ниже; отчёты о фиксах дописываются разделами `## Отчёт <дата>` внизу.
`MapsAudition_01.md` не трогаем (там все этапы P0–P2 и пакеты A–D числятся закрытыми с чистым `tsc`;
настоящий аудит расходится с этой записью — см. P0-A…P0-C).

---

## P0 — критично (потеря данных, поломка сборки/рантайма)

### P0-A. Редактор не компилируется, PNG-экспорт и линейка упадут в рантайме — [Код]
- `MapEditorPage.tsx:629,2556-2557,1306,1383`: `pngName`/`setPngName` используются (`openPng`, `exportPng`,
  JSX панели PNG), но `useState` для них в файле нет (есть только `pngOpen/pngGrid/pngCoords/pngLegend/pngDensity/pngPlayerView`).
- `MapEditorPage.tsx:1525-1526`: `cellCenter` вызывается в отрисовке линейки, но из `../maps/grid` не импортирован (строка 9).
- `MapEditorPage.tsx:708,710,814,829,840,849,875,884,916,928,953,1024`: `setSelected(null)` против
  `useState<ObjSel>(null)` без `| null` в типе (`702-708`).
- `MapEditorPage.tsx:1189,1193`: `cropped possibly null` (`TS18047`).
- Итого `tsc -p tsconfig.app.json --noEmit` → 23 ошибки, `EXIT:2`. Ловушка: голый `npx tsc --noEmit`
  в корне `client/` молчит всегда — корневой `tsconfig.json` это `files: []` + references, реальная
  проверка только через `-p tsconfig.app.json`. Именно так журнал 01 и получил ложный «tsc exit 0».
- План: Этап 0 — вернуть `pngName/setPngName`, импорт `cellCenter`, тип `ObjSel | null`, gard на `cropped`;
  зафиксировать команду проверки (`tsc -p tsconfig.app.json`) как каноническую для раздела.

### P0-B. Сохранение/удаление подписи стирает весь слой объектов — [Код]
- `MapEditorPage.tsx:678` (`saveLabelDraft`) и `:689-693` (`deleteLabel`): `next: MapCells` собирается только
  из `{terrain, roads, labels}` — `rooms/doors/traps/start/finish` отбрасываются, дальше `serializeCells`
  пишет это поверх (v3 → v2 даунгрейд). Подписал город на данже — молча потерял все двери/ловушки/комнаты.
- TS это ловит (две ошибки `TS2739` из списка P0-A), но код лежит в дереве.
- План: собирать через `{...before, labels: ...}` (Этап 1).

### P0-C. Ужимка поля в «Настройках» стирает подписи и объекты — [Код]
- `MapEditorPage.tsx:1186-1194`: `cropped: MapCells = {terrain, roads}` — без `labels/rooms/doors/traps/start/finish`,
  плюс обрезка не фильтрует даже их остатки за новым размером (ошибка `TS2740` из списка P0-A).
- Остатки за границей потом упрутся в серверную валидацию (`out of bounds`, `mapsValidation.ts:138,156,171,191`).
- План: кроп всех слоёв с фильтром по новому размеру (Этап 1).

### P0-D. `beforeunload` не покрывает состояние ошибки (рецидив P0-1) — [Код]
- `MapEditorPage.tsx:366-372`: сторож срабатывает только при `dirty/saving`. При `saveState.kind === "error"`
  уход со вкладки молчаливый, данные только в памяти (история эфемерна, `207-211`). Кнопка «Повторить» есть — хорошо.
- План: covering и `error` (Этап 1).

---

## P1 — сильно болит (данные, перф, UX-тупики)

### P1-1. Список делает N+1 запросов миниатюр без лимита — [Код]
- `MapsListPage.tsx:141-156`: после списка — веер `GET /maps/:id/thumbnail` по всем картам параллельно,
  без пагинации/лимита/кэша. 50–100 карт = залп из 50–100 запросов при каждом открытии. Прошлый аудит верно
  убрал thumbnail из списка, но замена «все превью сразу» — тот же трафик, только попиленный.
- План: лимит параллельных превью (первые N видимых + IntersectionObserver), кэш по `updated_at`, позже пагинация (Этап 2).

### P1-2. Линейка врёт по диагонали на квадратах — [Код, Мастер]
- `grid.ts:147-148`: Чебышев (`max(|dx|,|dy|)`), он же идёт в подпись замера и в `formatMeters`
  (`MapEditorPage.tsx:1549-1553`). Диагональ 5 клеток — не 5, а ~7 по прямой.
- `mapTypes.ts:46` (`parseCellLore`): только одиночное число+единица; «1 клетка = 5 миль» — null, линейка молча
  показывает только клетки.
- План: честная метрика (евклид для квадратов + подпись «по прямой»), расширить парсинг или честно показать
  «не распознал единицу» (Этап 3).

### P1-3. Слой объектов — только квадраты, гексы без данжей — [UX, Мастер]
- `MapEditorPage.tsx:1741-1743,1979-1980`, `render.ts:207-209`, `hitObject:719-750` (`if square return null`).
  Гексовые «Страна/Регион» не получают комнат/дверей/ловушек/старта-финиша; выбор на гексах отвечает ошибкой.
- План: отдельный заход слоя на гексах (край/вершина гекса вместо n/s/e/w); до него — честная плашка
  ограничений уже есть, оставить (Этап 5/отдельный).

### P1-4. В PNG-легенде нет ловушек/комнат/старта-финиша — [Мастер]
- `MapEditorPage.tsx:1318-1335`: легенда = 13 террейнов + виды дверей с карты + дорога. На поле при этом
  рисуются ловушки (глифы `render.ts:276-281`), тинты комнат (`225-232`), старт/финиш — на бумаге они есть,
  в легенде их нет. Распечатка несамодостаточна.
- План: легенда = всё, что нарисовано (Этап 3).

### P1-5. Имя JSON-выгрузки не санитизировано — [Код]
- `MapEditorPage.tsx:1089`: `map-${map.name}.json` без чистки (у PNG она есть, `:1264`). Слэши, точки
  в начале, длина 200 — всё едет в `a.download`.
- План: та же чистка, что у PNG + лимит длины (Этап 4).

### P1-6. Сервер не триммит клетки при смене размера без `cells` — [Код]
- `maps.ts:129-139`: `width/height` меняются, `cells` остаётся старым. Прямой PUT (или гонка) оставляет клетки
  `out of bounds` — следующий сейв тех же клеток падает на `validateCellsBlob`.
- План: серверный трим или явный 400 с понятным текстом; инвариант не должен держаться только на клиенте (Этап 1).

---

## P2 — дизайн-перфекционист (по `design_revision.md`)

Закрытое ранее держится: превью без дуотона (`.map-tile-art`, `index.css:1933`), выбор чернильной обводкой
(`1942-1945`), координаты Oswald+трекинг, аккордеон панелей (один CTA). Остатки:

### P2-1. `.map-tool[aria-pressed]` — `outline: 2px`, инвариант §1.3 говорит 1px везде — [Дизайн]
- `index.css:1942-1945`. Аргумент «outline не влияет на layout» верен, но визуально выбранная кисть вдвое
  жирнее любой рамки системы.
- План: либо 1px + offset, либо дописать исключение в ревизию — сейчас это устная договорённость
  в комментарии, а не правило (Этап 4).

### P2-2. Моношир (§1.5) протекает в прозу — [Дизайн]
- `MapsListPage.tsx:84-86`, `MapEditorPage.tsx:2179,2293,3005`: мета/статусы целиком в `var(--font-mono)`,
  включая слова («Сохранено 12:03», «Есть несохранённое»). По инварианту моно — только значения.
- План: разделить значение/слова по голосам (Этап 4).

### P2-3. Палитра: лава–горы и яд–холмы на грани — [Дизайн, Мастер]
- Журнал 01 честно зафиксировал слабины (лава–горы дейтеранопия 0.012). Мотивы кроют их на экране ≥16px,
  но миниатюра 320px и ЧБ-печать — под вопросом (ЧБ-проба делалась только для болота).
- План: ЧБ-проба лавы/яда + при нужде разводка светлоты (Этап 3).

---

## P3 — придирчивый пользователь / мастер за столом

- **Тулбар — 3 ряда и растёт** (`MapEditorPage.tsx:2216-2357`): 8 инструментов + размер + undo/redo + очистить +
  3 аккордеона + ряд террейна из 13 кнопок + легенда. На 390px — штора над канвасом. Отложено в `На_будущее.md`
  п.2, но с тех пор добавились линейка/подпись/выбор/обмен — стало хуже. Условие возврата («когда состав
  зафиксируется») фактически наступило.
- **Нет справки по хоткеям в UI.** Хоткеи есть (`V/B/G/E/I/R/M/T/+/−/0/Esc/Del/Ctrl+Z`), вся документация —
  title-подсказки и строка мелким шрифтом (`3095-3099`). Новый пользователь их не найдёт.
- **Нет поиска внутри редактора** (найти подпись «Вотердип» на 60×44 — только глазами), нет списка подписей.
- **Координаты на экране по умолчанию выкл**, порог 18 — только в title (`3006`). Мастер «идёте в B12»
  сначала должен догадаться включить чекбокс.
- **Показ игрокам — только флаг.** Кнопки «Показать игрокам» в `/maps/:id` нет (отложено в `На_будущее.md` п.1).
- **Клавиатурное рисование невозможно**, канвас `role="img"` — для скринридера карта «картинка»
  (журнал 01 признал и отложил — фиксирую как отложенное, не забытое).
- **Undo 50 полных снапшотов** (`53,264-270`, `cloneCells:56-67`): на 100×100 каждый шаг — Map на тысячи
  записей, лимита памяти нет.

---

## План исправления (порядок — на обсуждение, без старта)

- **Этап 0 — починить сборку.** P0-A: вернуть `pngName/setPngName`, импорт `cellCenter`, тип `ObjSel | null`,
  gard на `cropped`. Зафиксировать каноническую проверку `tsc -p tsconfig.app.json --noEmit`.
- **Этап 1 — потери данных.** P0-B (подписи через `{...before}`), P0-C (кроп всех слоёв с фильтром),
  P0-D (`beforeunload` на `error`), P1-6 (серверный трим или явный 400).
- **Этап 2 — перф списка.** P1-1: лимит превью + IntersectionObserver + кэш по `updated_at`, позже пагинация.
- **Этап 3 — мастер/печать.** P1-2 (честная метрика линейки), P1-4 (полная легенда PNG), P2-3 (ЧБ-проба лавы/яда).
- **Этап 4 — UX-долги.** Тулбар-мобильность, справка хоткеев, координаты, P1-5 (имя JSON), P2-1/P2-2
  (outline/моношира, часть — решением в `design_revision.md`).
- **Этап 5 — отложенное.** `На_будущее.md` п.1 (показ игрокам) и п.2 (мобильный тулбар) — условия возврата
  созрели; P1-3 (объекты на гексах) — отдельный заход слоя.

---

## Дополнение по MapsAudition_05 (2026-09-03)

Источник: `MainWorks/Maps/MapsAudition_05.md` (дата в шапке — 2026-09-18). Разобран построчно со сверкой
каждого утверждения с текущим кодом. Вердикт: **отчёт написан по старому состоянию дерева** — его план
Этапов 1–5 дословно копирует план аудита 01, а «Этап 6» называет пакеты C (данж-генератор) и D (миникарта,
JSON-обмен, лава/кислота/яд) «будущими», хотя всё это уже реализовано. Почти все его «нарушения» в текущем
коде уже исправлены (confirm генерации, легенда-панель, ПКМ-ластик, пипетка с дорогами, `saveSeq`,
`pickThumbnail`-кэш, плашка битого blob, culling видимого диапазона, `worldBounds()` в миниатюрах, кнопка
«Сбросить», русские ошибки). Три утверждения прямо ложны: `hexLum` «при каждом рендере» (на деле посчитан
раз на уровне модуля, `render.ts:78-80`), «гексы без отсечения» (culling есть, `render.ts:503-506`), кисть
«`border: 2px solid var(--accent)`» (этих строк в дереве нет; выбор — `outline` чернилами, остаток зафиксирован
как P2-1 выше). Предложение чинить выбор через `box-shadow` с акцентом — отвергнуто сразу (нарушает §1.2).
В тексте 05-го мусорные токены и неверные атрибуции файлов — целиком в журнал его не кладём, забираем только
подтверждённое новое (ниже). Приоритеты и Этапы 0–5 выше не меняются; находки ложатся довеском.

### Принято (проверено по коду, уровни P2/P3)

- **Д-1. Двойной проход fbm в генераторе (P3).** Подтверждено: `generate.ts:85-95` считает `e = fbm(x,y,seed)`
  и `mountainNoise`, затем `:100-105` пересчитывает то же `e` заново — достаточно переиспользовать массив
  `isLand`. Эффект ничтожный (генерация — разовый клик), правка на 5 строк. → Бэклог/Этап 4.
- **Д-2. Плотность PNG 64 + линейка масштаба на распечатке (P2, мастер).** Сейчас `PNG_DENSITIES = [16,24,32,48]`
  (`MapEditorPage.tsx:484`). «64 для крупных распечаток» разумно; новое зерно 05-го — линейка масштаба на бумаге
  (строка «1 клетка = …» не спасает, если печать растянули, — нужна графическая шкала, независимая от DPI).
  Ни в 01, ни в 02 этого нет. → Этап 3.
- **Д-3. Маркеры старт/финиш мелкие на печати (P2-проверка).** Контрольный пункт для ручного прогона, не задача. → Этап 3.
- **Д-4. «Быстрый данж в один клик» (P3/бэклог).** Пресет «5 комнат» поверх 7 параметров генератора — новая UX-идея
  для развития пакета C, в 01/02 её нет. → Бэклог.
- **Д-5. Сетка «вкл для подготовки / выкл для печати» запоминается (P3).** `showGrid` в localStorage —
  можно уронить распечатку с забытой сеткой (дефолт PNG независим — `pngGrid` свой, риск только визуальный). → Этап 4.
- **Д-6. «9 цветов» в §7 ревизии vs 13 в коде (док-задача).** `design_revision.md:211` говорит «9 цветов
  (`MAP_TERRAIN_FILL`)», в коде 13: lava/acid/poison (пакет D) + wall (пакет C), решения задокументированы
  в отчётах 01, дизайн-документ не обновлён. Правильное действие — обновить строку ревизии до 13, код не резать. → Этап 4.

### Отвергнуто (с причинами)

- «Неоднозначность параметров генератора между масштабами» — пороги шума по дизайну масштаб-независимы
  (`00_Отчёт_фундамент.md` п.17), так задумано.
- «Сырые английские ошибки» — фолбэк сырым текстом осознанный (не врать префиксом), зафиксирован в 01.
- «Монолит render.ts 935 строк / два прохода» — проходы намеренные (группировка по `fillStyle` + отдельное
  состояние мотивов); чистка — рефакторинг P3, не дефект.

---

## Дополнение по MapsAudition_04 (2026-09-03)

Источник: `MainWorks/Maps/MapsAudition_04.md`. Разобран построчно со сверкой с текущим кодом.
Вердикт: **документ исторический, полностью поглощён реализацией.** Его таблица статусов сама делит пункты на
«✅ Закрыт (MapsAudition_01)» и «Открыт» — и все его «Открыт» (P0-3 дуотон, P0-7 PNG-легенда, P1-5 culling,
P2-1 линейка, P2-2 подписи, P2-8 мелочи) в текущем дереве уже реализованы и проверены в аудите 02 выше.
Его план Этапов 1–3 — это фактически тот work-list, который был потом выполнен. Отдельные формулировки 04-го
устарели сильнее, чем в 01: «Сбросить фильтры только в пустом состоянии» — нет, кнопка уже при любых активных
фильтрах (`MapsListPage.tsx:293-307`); в таблице задвоена строка P1-10. Как план действий 04-й не нужен.
Забираем только три новых микро-замечания (все P3), которых нет ни в 01, ни в 02:

### Принято (проверено по коду, уровень P3)

- **Д-7. `road` в `MAP_TOOL_ORDER`, но не в `MAP_TERRAIN_FILL` — типовая ловушка.** Проверено grep по
  `MAP_TERRAIN_FILL[`: 4 места — `render.ts:538` (есть `?? plain`-фолбэк), `MapEditorPage.tsx:1327,2369`
  (идут по `MAP_TERRAIN_ORDER`, `road` там нет), `:2337` (внутри тернарной ветки `code === "road"`,guard есть).
  То есть сегодня всё обложено, но держится на внимательности: любой новый код с `MAP_TERRAIN_FILL[code]`
  по элементу `MAP_TOOL_ORDER` получит `undefined`. Лечение — тип `(Terrain | "road")[]` для ORDER или явный
  guard-контракт. → Бэклог/Этап 4.
- **Д-8. `translateMapError` — плоская таблица регэксов (`mapTypes.ts:69-99`).** Справедливо как замечание
  по обслуживаемости: ~15 паттернов в одном цикле без группировки по домену. Уточнение к 04-му: перекрытие
  `/out of bounds/i` не стреляет — специфичные паттерны (labels/rooms/doors) стоят раньше в таблице и съедают
  своё, generic ловит только остатки. Группировка по доменам (валидация/сеть/CRUD) — да, P3. → Бэклог/Этап 4.
- **Д-9. Реф-шаблон хоткеев (`undoRef/redoRef/selectedRef/deleteSelectedRef/fitCameraRef/zoomByRef`,
  `MapEditorPage.tsx:1704-1715`).** Хрупкая синхронизация «присвой каждый рендер»; протухший реф = тихий
  вызов устаревшего замыкания. Работает, но контракт не enforced. Альтернатива — `useEvent`-подобный хелпер
  или колбэки через `useCallback` с честными зависимостями. P3, трогать только вместе с Этапом 0 (там рядом
  правки типов). → Бэклог/Этап 4.

### Отвергнуто / поглощено

- Всё остальное из 04-го дублирует 01/02 (включая `COALESCE`-несброс `parent_map_id/thumbnail` — уже
  зафиксировано как принятое-до-иерархии, и P2-5/P2-6 — уже в `На_будущее.md`).
- Приоритеты и Этапы 0–5 выше не меняются.

---

## Дополнение по MapsAudition_03 (2026-09-03)

Источник: `MainWorks/Maps/MapsAudition_03.md` («второй проход», исходит из закрытого аудита 01).
Вердикт: **единственный из трёх дополнительных отчётов, написанный по текущему дереву** — номера строк
бьются один в один (3124 строки `MapEditorPage.tsx`, легенда PNG `:1326-1379`, миникарта `:3051-3093`,
экспорт `:1276-1383`, хоткеи `:1638-1702`). Качество выше, чем у 04/05: автор честно фиксирует
самоопровержения (C6 — «дублирования нет, я ошибся», U9 — «вопрос снят», C2/C4/C5/C8/C9/C10 — «не трогать»,
все проверены и верны). Мусорных токенов меньше, но есть (冻, 巨大, 很低, 应当, 遍历, 移动, «papeshки»).
**Дополняет сильно, план не меняет.** Важно: 03-й НЕ заметил P0-A/B/C выше (отсутствующие `pngName`,
стирание объектов подписями/кропом) — автор читал то же сломанное дерево, что и я. Мораль: чтение глазами
не ловит отсутствие деклараций, `tsc -p tsconfig.app.json` — ловит; каноническая проверка из Этапа 0
обязательна после каждого чтения, не только после правок.

### Принято (проверено по коду)

- **Д-11. Легенда PNG без мотивов (средне, мастер).** Подтверждено: свотчи 14×14 сплошные (`:1360-1379`),
  мотивы на поле есть. Для стен (кирпич) и воды (волны) мотив — основной различитель. Сюда же M3 (легенда
  редактора без мотивов, `:2358-2403`) — чинить парой: глиф/мини-мотив в обеих легендах. → Этап 3.
- **Д-12. Легенда режима игрока спойлерит (средне).** Подтверждено и ново: `legRows` всегда из всех 13
  террейнов (`:1326-1335`), по `pv` фильтруются только двери. PNG «глазами игрока» перечисляет лаву/кислоту/яд,
  которых на карте нет. Лечение — фильтр по реально присутствующим (`cells.terrain`). → Этап 3.
- **Д-13. Синхронный PNG-блок на 100×100@48 (средне).** Подсчёт 03-го верен (~23.5 млн px, секунды заморозки,
  риск двойного клика). Минимум — disable кнопки + «Генерируется…» + `setTimeout`. → Этап 3/4.
- **Д-14. `document.title` с dirty-индикатором (средне, дёшево).** `saveState` виден только в тулбаре
  (`:193-196`, `:2291-2302`); при уходе на другую вкладку контекста нет. Один `useEffect` на
  `saveState.kind + map.name`. → Этап 4.
- **Д-15. `@media print` для печати через браузер (средне).** Подтверждено: print-стилей для редактора нет,
  браузер режет canvas на листы сам. Скрыть тулбары, canvas — `width:100%, avoid-break`. Сюда же D5 (readout
  реального размера «~51×37 cm при 96 dpi» в панели PNG) и Д-2 (линейка масштаба) — одна тема «печать». → Этап 3.
- **Д-16. Камера не сохраняется (средне).** Подтверждено: `fitCamera` при открытии (`:1449-1469`), `cam`
  никуда не пишется. Сохранять в localStorage по `map.id`, «Вписать» — сброс. → Этап 4.
- **Д-17. «Быстрый черновик» (средне).** Создание — 4 поля + клик (`MapsListPage.tsx:382-443`); дефолтное имя
  + дефолты (hex/continent/40×30) в один клик снижают барьер. → Этап 4.
- **Д-18. Комната без имени — голый номер (средне-низко).** Подтверждено: `render.ts:628` — `String(idx+1)`,
  имя только `if (r.name)`. Дефолт «Казарма 3» / «Комната 3». → Этап 3.
- **Д-19. Микро-UX пакетом (низко, дёшево):** U4 «Очистить» рядом с Undo/Redo (`:2246-2259`, отнести вправо +
  danger-стиль); U6 запоминание вкладки `genTab` (`:377`, localStorage как `showGrid`); U10 `title` у селекта
  видимости (`MapsListPage.tsx:274-285`); D6 readout видимых клеток в титуле миникарты; D2 Safari-проверка
  `letterSpacing` (гард есть, `:806-808`, нужен только ручной прогон). → Этап 4.
- **Д-20. Бэклог мастера (в `На_будущее.md`, не сейчас):** M5 слой NPC/токенов (отдельный спек), U2 панель истории
  undo, M2 пакетная замена типа, M7 история сидов. U8 свайп-undo — туда же к P2-6.

### Отвергнуто / скорректировано

- **U3 (переключение двух карт по Ctrl+Tab)** — Ctrl+Tab зарезервирован браузером, угнать ненадёжно; идея
  «последняя карта» жива только с другим хоткеем (напр. `Alt+←`) — в бэклог с пометкой.
- **U7 (Ctrl+G / Ctrl+Enter для генератора)** — Ctrl+G в Chrome/FF это «найти далее», конфликт; брать `Alt+G`
  или одиночную клавишу вне полей ввода.
- **C3 (мутировать draft вместо cloneCells)** — неверное предписание: мутация убьёт ре-рендер
  (`setCells` тем же объектом). Наблюдение (клон на каждый move) верно, лечение — коалесцинг pointermove
  через rAF, не мутация. P3, в бэклог с пометкой.
- **D3 принят выводом самого автора** (`monospace` оставить как есть) — не задача.
- **C2/C4/C5/C8/C9/C10** — автор сам закрыл как «не трогать», перепроверено, согласен.

### Собственная находка поверх 03-го

- **Д-21. `var()` внутри `ctx.font` — невалидный шрифт, canvas молча держит предыдущий (низко-средне,
  дизайн).** `render.ts:625` (`10px var(--font-mono, monospace)`), `:633` (`Oswald, var(--font-ui, sans-serif)`),
  `:805`, `:822` — CSS-переменные в canvas-шрифтах не резолвятся, вся декларация игнорируется, номера комнат,
  их имена, координаты и подписи рисуются унаследованным шрифтом (утёкшим из предыдущего вызова). Лечение —
  резолвить через `getComputedStyle` как в `readChrome()` или захардкодить стеки. Пропустили все три аудита
  включая мой. → Этап 4 (рядом с P2-2/моноширой).

---

## Отчёты о проделанной работе

### 2026-09-03. Все этапы 0–5 выполнены (план утверждён владельцем «сразу все»)

- **Этап 0 (P0-A).** `MapEditorPage.tsx`: возвращены `pngName/setPngName`, импорт `cellCenter`,
  тип `ObjSel | null` (+ `selectedKeyOf(s: ObjSel | null)`). Каноническая проверка зафиксирована:
  `npx tsc -p tsconfig.app.json --noEmit` (голый `tsc --noEmit` молчит из-за `files: []` в корне).
- **Этап 1 (P0-B/C/D, P1-6).** Подписи (`saveLabelDraft`/`deleteLabel`) — через `{...before}`;
  кроп настроек — все слои с фильтром (комната за краем — целиком, старты/финиш копируются);
  текст модалки честно предупреждает; `beforeunload` покрывает и `error`. Сервер (`maps.ts`):
  `trimCellsTo()` — ресайз без `cells` триммит хранимое под новый размер (та же семантика, что у клиента);
  явные `cells` — по-прежнему строгая валидация. Тест +1 (`maps.api.test.ts`, трим всех слоёв) —
  сервер `32 passed` (22 + 10).
- **Этап 2 (P1-1).** `MapsListPage.tsx`: кэш превью `id:updated_at` (cap 200) + пул 4 вместо веера N запросов.
- **Этап 3 (печать/мастер).** Линейка — евклид на квадратах + «по прямой» (`rulerMeasure`); легенда PNG —
  мотивы в свотчах (Д-11), ловушки/комнаты/старт/финиш (P1-4), фильтр террейнов в виде игрока (Д-12),
  графическая линейка 5 кл (Д-2); плотность 64; экспорт асинхронный (busy-кнопка + таймер, Д-13);
  readout размера файла/бумаги (D5); `@media print` (M6); имена комнат по умолчанию (M4);
  легенда редактора дополнена теми же рядами. P2-3 закрыт числами (скрипт): худшие пары
  plain–desert dLum 0.056/deut 0.078 — с разными мотивами приемлемо, палитру не трогаем.
  Д-3 закрыт анализом: маркеры масштабно-зависимы, пропорции одинаковы на любой плотности.
- **Этап 4 (UX).** `document.title` с dirty (Д-14, с рестором исходного); память камеры по `map.id`
  + «Вписать»/ресайз-настройки её сбрасывают (Д-16); «+ Черновик» в списке (Д-17);
  «Очистить» отнесена от Undo (U4); `genTab` в localStorage (U6); `title` у фильтра видимости (U10);
  видимые клетки в титуле миникарты (D6); `readCanvasFonts` вместо невалидного `var()` в `ctx.font`
  + сброс `letterSpacing` после координат (Д-21); fbm в один проход, выход бит-в-бит (Д-1);
  санитизация имени JSON (P1-5); тип `MAP_TOOL_ORDER` без `road`-ловушки (Д-7);
  `translateMapError` сгруппирована по доменам + точное `cell/road out of bounds` (Д-8);
  Alt+G / Alt+P панели, Ctrl+Enter — сгенерировать (U7, без конфликтов: Ctrl+G не трогаем);
  шпаргалка хоткеев `<details>`; outline выбора 1px + offset 2px (P2-1, решение в `design_revision.md` §7);
  моно снята со строки статуса (P2-2); rAF-коалесцинг мазков с докрасской на pointerup/cancel (C3);
  C7-частично: `maps/mapExport.ts` (PNG-сборка) + `maps/mapExchange.ts` (обмен) — компонент держит
  только состояние; touch-хук не выделялся (риск > пользы, зафиксировано).
  Хинт про независимость сетки PNG от экранной (Д-5).
- **Этап 5.** «Показать игрокам» (`shareWithPlayers`: флаг + ссылка в буфер + тост; автопуша нет —
  честно сказано) — `На_будущее.md` п.1 частично закрыт; моб. тулбар — скролл-ряд ≤640px (п.2 частично);
  объекты на гексах (P1-3): комнаты/ловушки/старт/финиш/выбор/rect/drag везде, двери — только квадраты
  (создание заблокировано в модалке + guard в `saveCreateDraft`, рендер дверей на гексах пропущен);
  маркеры ловушек/старта центрированы через `cellCenter`; «Быстрый данж» (Д-4, пресет 5 комнат);
  бэклог Д-20 (M5/U2/M2/M7/U3/U8) записан в `На_будущее.md` п.3.
- **Доки.** `design_revision.md` §7: 13 цветов (было 9), решение про outline 1px+offset, запрет `var()`
  в `ctx.font`. По ходу: две self-caught ошибки (затёртый `Camera` — возвращён; удалённый вместе
  с блоком `exportPng` — восстановлен; дважды затираемый CSS-блок — возвращён) — все пойманы `tsc`/ревью
  до коммита.
- **Проверки:** клиент `tsc -p tsconfig.app.json` exit 0; `check-type-scale.mjs` — «сырых значений нет»
  (одно своё `fontSize: 12` → `var(--fs-meta)`); сервер `32 passed`. Живая база не трогалась
  (тесты — `DB_DIR` во tmp). Git-операций не было.
- **Остаётся за владельцем:** ручной прогон по `Находки.md` п.10 в «Соевом панке» и «Соевом нуаре»
  (390px, печать PNG, гексовые объекты, вид игрока).
