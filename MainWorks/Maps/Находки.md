# Карты — находки

## 2026-09-03, тикет 02: сломан `tsc` в чужом месте
`client/src/components/dnd/dndFeatures.ts` импортирует `DndCheck/DndCost/DndEffect` из `types.ts`, где они объявлены локально без экспорта. Блокирует `npm run build` целиком, к картам отношения не имеет. Решение владельца: не чинить в этом заходе, проверка карт — прогоном через `run`. Вернуться отдельным заходом.

## 2026-09-03, тикеты 06–07: итог фундамента
- 06 (PNG): 32 px/клетка тем же рендером, сетка дефолт вкл, координаты дефолт выкл, имя правит пользователь. Проверен кодом (линты/типы), визуально — за владельцем.
- 07: тумблер видимости в шапке редактора (с откатом при ошибке), фильтр видимости в списке (только мастер), интеграционный тест `maps.api.test.ts` (временная база через `DB_DIR`, настоящий гейт: CRUD, валидация, clearCells, 403 игрокам, фильтр списка) — 6/6. Полный серверный набор: 26/26.
- В `design_revision.md` записано исключение §7 (палитра террейна) + строка обхода.
- Остаток ручной проверки (всё — прогоном в «Соевом покое»): список/создание (02), поле/зум/пан (03), кисти/дорога/undo/перезагрузка (04), генератор+сид (05), PNG (06), игрок видит только открытое (07).
