# Карточка места, «Партия здесь», выходы — решения гриллинга

Дата: 2026-09-11. Репозиторий `E:\Claude\01_Blender` (SoyMan). Статус: гриллинг пройден, дальше `/design`, затем план этапов.

Основание: разбор раздела «География» профиля сеттинга (переписка 2026-09-11) — внешние рекомендации «Атлас сеттинга» сверены с кодом, половина уже была сделана; из остального выросли три темы ниже.

## 0. Коротко

У раздела две работы: подготовка («что в мире есть») и стол («где мы, что здесь, куда отсюда»). Второе решается одной **карточкой места**, которая живёт и в Географии, и в пульте. К ней — отметка **«Партия здесь»** и новое понятие **выходы** между местами.

## 1. Проводник (LocationMiller)

1. **Чипы — корни мира** (`parent_id IS NULL`), а не дети корня. Выбранный чип задаёт ветку, колонки начинаются с его детей. Первая колонка «Мир» исчезает. Данные на 2026-09-11: корней на сеттинг 1–4, чаще 2–3 — колонка шириной 240px держала два-три пункта.
2. Под чипами — компактная шапка выбранного корня: имя, строка описания, счётчики, миниатюра.
3. Вход в раздел — последний выбранный корень, иначе первый по алфавиту. Корень без детей — колонок нет, сразу карточка.
4. Drag-drop: на полосу чипов — «в корень», на чип — «внутрь корня».
5. Много корней — одна строка, остальные под «ещё N ▾».
6. «Создать» — внутрь выбранного места (родитель подставлен в визард, сменить можно).
7. Глифы `◉ ◆ ◇ ▣ •` и легенда уходят: иконки из `NavIcon` + роль словом в бейдже.
8. Карточка — постоянная панель справа, в аккордеон колонок не входит. Выбран только чип — карточка корня.
9. Узкий экран (<560px) — карточка последним экраном, как сейчас.

## 2. Карточка места

Один компонент, два места: правая панель Географии и пульт сессии. Полная страница локации не меняется.

Порядок фиксированный, пустой блок не рисуется:

1. **Шапка** — миниатюра, имя, роль/`kind`, кликабельный путь, «⚑ Партия здесь».
2. **Описание** — 3–4 строки, «ещё» раскрывает на месте.
3. **Кто здесь** — жители поимённо (существа + сообщества), жители точек с подписью «(Точка)».
4. **Что здесь** — `location_content` (секреты, лут, ловушки, особенности).
5. **Выходы.**
6. **Точки.**
7. **Артефакты здесь** (`artifacts.location_id` — сейчас нигде на локации не показываются).
8. **Что здесь было** — группы «Впереди» (непройденные сцены с этим местом, `campaign_scene_state` ≠ done) и «Было» (сессии: одна строка на сессию с пометкой причины — сцена / пульт / упоминание; последние 5 + «все»). В пульте — текущая кампания, в профиле — все с названием кампании.
9. **Действия** — «Открыть локацию»; «+ Вложенная» (только География); «Мы здесь» (только пульт).

Правка: только чтение + быстрое добавление строкой «+» в «Что здесь» и «Выходы». Остальное — на полной странице.

Пульт: клик по месту (панели пульта, строка «Партия здесь») кладёт карточку в **докстанцию** (`PreviewDock`) развёрнутой, наверх; уже лежащая там — разворачивается и поднимается. Шапка-плашка сворачивает карточку, как у существ; «Свернуть всё» дока работает и для мест. Док скрыт — клик его показывает. Ctrl+клик — полная страница. Уточнено владельцем 2026-09-11 после `/design`: не поверх пульта и не модалкой — модалка закрывает пульт и требует закрытия перед следующей сценой. В доке карточка без крупного имени: заголовок — плашка дока (тип + имя), путь без корня. Реализация — ветка `location` в `EntityPreviewContent` (`components/EntityPreviewModal.tsx`), сейчас там только `kind` и описание.

## 3. «Партия здесь»

1. Точка **кампании**, переносится между сессиями до смены. Разделение партии не моделируем.
2. По умолчанию выводится из последней запущенной сцены (`session_scenes` → `scene_location` в `generic_links`). Несколько локаций у сцены — первая, остальные «также в сцене».
3. Ручная отметка «Мы здесь» — только в пульте; действует до запуска следующей сцены.
4. Ручная отметка заодно пишет место в панель локаций текущей сессии (`origin = 'live'`) — чтобы «Что здесь было» работало и в песочнице без сцен.
5. В Географии профиля — флажки всех активных кампаний сеттинга на строках пути, с названием кампании.

## 4. Выходы

1. **Отдельная таблица**, не `entity_relations`: выход — устройство мира, а не мнение (тон ± бессмыслен, полей под одностороннее/тайное нет).
2. Поля: откуда, куда, как (свободный текст с подсказками, как `kind`), время в пути (необязательная строка — «полдня», не число), только туда (флаг), тайный (флаг), заметка.
3. Двусторонний по умолчанию, одна запись видна с обоих концов; «только туда» — исключение.
4. Между любыми ролями (комната ↔ комната, район ↔ район), **только внутри одного сеттинга**.
5. Архивация конца — выход скрывается и возвращается при восстановлении; окончательное удаление — удаляется.
6. Игрокам не отдаётся в этом заходе. Флаг «тайный» ставится сразу — на будущее показа игрокам.
7. Экспорт/импорт сеттинга — всегда, по uid.
8. В этом заходе — только карточка. Полотно «Дерево» и граф связей — следующим шагом.
9. **Удаление выхода — в строке карточки** (крестик на наведении, с подтверждением). Отступление от «остальное — на полной странице»: редактора выходов на полной странице локации нет, а `LocationDetailPage.tsx` 2026-09-11 параллельно перестраивает другая сессия. Правка полей выхода (`PUT /setting-locations/exits/:id`) в интерфейсе пока не выведена — удалить и завести заново.

## 4а. Этапы реализации

Макеты: `MainWorks/design/geography-card/` (холст «Карточка места»). Проверка — пара `api-scratch-3003` / `client-scratch-5175`; живая база только на чтение. Перед готовностью каждого этапа: `npx tsc -p tsconfig.app.json --noEmit` и `npx oxlint src` в `client`, `npx tsc --noEmit` в `server`. База на 2026-09-11: server tsc чисто; client tsc — 18 ошибок в чужих незакоммиченных `DndCharacterForm.tsx`/`DndCharacterWizard.tsx` (не трогать); oxlint — снимок в журнале сессии.

1. **Проводник** (клиент): чипы-корни, компактная шапка, колонки от детей корня, карточка справа вне аккордеона, «Создать» внутрь выбранного, иконки ролей, меню пустого места колонки и полосы чипов. — **Сделано**, проверено в приложении (узко и 1440px): путь Вотердип → Город Мёртвых → Склеп Гостов, аккордеон 52/169/240, карточка 320px справа.
2. **Карточка места** (`PlaceCard`): шапка, описание, «Кто здесь», «Что здесь» с быстрым добавлением, «Точки», «Артефакты здесь» (сервер: артефакты в ответе локации). В Географии заменяет превью. — **Сделано**; форма «Что здесь» проверена без отправки (живая база).
3. **Выходы**: таблица + миграция в конец, маршруты, архивная видимость, экспорт/импорт по uid, блок и форма в карточке. — **Сделано**: `location_exits`, `services/locationExits.ts`, маршруты `POST /:id/exits`, `PUT|DELETE /exits/:id`, `exits` в ответе локации, `collectExits`/`linkLocationExits` в `settings.ts`, тест `routes/locationExits.test.ts` (8 проверок: оба конца, односторонний, отказы, правка/удаление, архив, выгрузка→разворот→слияние без удвоения); клиент — `PlaceExitForm.tsx`, блок в `PlaceCard`.
4. **«Что здесь было»**: сводный маршрут (сцены впереди, сессии со сценой / пультом / упоминанием), блок в карточке. — **Сделано**: `services/placeHistory.ts` + `GET /setting-locations/:id/history[?campaign_id&all=1]`, тест `services/placeHistory.test.ts` (8 проверок). Правила: место = само + неархивные вложенные; «Впереди» — сцены, которые кампания показывает (исходники её приключений с главами и «Сценами вне приключений», подменённые копиями, плюс свои), не `done`/`skipped`, место сцены — на строке содержимого (`library_scene_id ?? id`); «Было» — запуск сцены (любой статус), место в панели с `origin='live'` (любой статус), `planned` и упоминание — только у проведённой. Проверено на живой базе чтением: 23–49 мс, у Вотердипа 271 сцена впереди.
5. **«Партия здесь»**: вывод из журнала сцен + ручная отметка кампании (запись в локации сессии), строка в пульте, флажки в проводнике. — **Сделано**: таблица `campaign_party_place` (только ручная отметка, `set_at` с долями секунды), `services/partyPlace.ts`, `GET|PUT /sessions/:id/party-place`, `GET /settings/:id/party-places`, тест `services/partyPlace.test.ts` (7 проверок: сцена и «также в сцене», отметка перебивает сцену и пишет `origin='live'`, архивная отметка не считается, следующий запуск перебивает даже без места, отказы, путь флажков); клиент — `PartyHereStrip.tsx` над `SessionTimeStrip`, флажок на строках пути в проводнике, плашка «Партия здесь» в карточке, если партия ровно здесь. Проверено на живой базе чтением: партия «Вотердипа» — «2. Фреска увечий» из сцены 22.08, флажки на «Морском районе» и «Синем переулке». «Мы здесь» на живой базе не нажималось — переставило бы настоящую точку партии.
6. **Докстанция**: ветка `location` в `EntityPreviewContent` — карточка места в плашечном виде; клик по месту в пульте кладёт её в док. — **Сделано иначе, чем записано**: не через `EntityPreviewContent` (им пользуются и модалки упоминаний), а веткой в `PreviewDock.tsx` — `PlaceCard variant="dock"`. `openPreviewDockCard` в `previewDockStore.ts` кладёт наверх, разворачивает (событие `preview-dock-expand`) и снимает `live-hide-dock`. Клик по месту в панели «Локации» — `onEntityClick` у `SectionDropZone`, Ctrl+клик — полная страница новым окном. «Мы здесь» из дока шлёт событие `party-place-changed`, пульт перечитывает панели.

## 5. На будущее (не в этом заходе)

- **Выходы между сеттингами** (портал из Эстарии в Баровию). Мысль владельца — интересная, подумать отдельно. Что мешает сейчас: экспорт одного сеттинга оставит выход висящим в пустоту; импорт должен уметь «конец не найден» (выход-заглушка с именем цели? отказ? спрашивать?); карточка должна показывать, что цель в другом мире, и переход уводит из профиля. Решение по uid-ссылкам между модулями — вероятно, тот же механизм, что у глобальных ключей ссылок.
- Выходы на полотне «Дерево» и в графе связей.
- Выходы на стороне игрока (открытые, не тайные — на открытых локациях).
- Туман войны в проводнике: пометка «открыто игрокам» и переключатель в карточке (контекст кампании).
- Набор структуры текстом с отступами (outliner) — продолжение этапа 4 плана «Зоны».
- «Готовность к сессии»: места запланированных сцен без описания/жителей/наполнения.
- Заготовки мест (по образцу библиотеки сцен).
- Ручной порядок детей (`position`) — только если владелец натыкался.
- Карточка вместо вкладки «Информация о локации» на полной странице.

## 6. Находки

- `LocationMiller.tsx`: контекстное меню «Создать локацию» мёртвое — `setMenu({ createParent })` нигде не вызывается; правый клик по пустому месту колонки шлёт `id: pid ?? -1` (стр. 785): в корневой колонке ничего, во вложенной — меню родителя без «Создать». Чинится в рамках этапа проводника (колонка «Мир» всё равно уходит, меню пустого места — у колонок и полосы чипов).
- В переписке 2026-09-11 ошибочно утверждалось, что локации не участвуют в отношениях; на деле `location` в `RELATION_CREATABLE` (`server/src/db/entityKinds.ts:177`), вкладка «Отношения» у локации есть.
- Мёртвое меню пустого места починено в этапе 1: правый клик по пустому месту колонки — «Создать локацию здесь», по полосе чипов — «Создать корень мира».
- Этапы 1–3 (кроме блока выходов в клиенте) ушли в чужой коммит `0157fd6` «Профиль локации…» вместе с работой параллельной сессии: рабочее дерево общее, коммит забрал всё изменённое. Код цел, но история смешана — при разборе искать изменения карточки места и выходов там.
- Вне этой работы на 2026-09-11 висят чужие ошибки: `oxlint` — `rules-of-hooks` в `CampaignMilestones.tsx:77` (хук под условием); `tsc` клиента — синтаксис в `CampaignChaptersScenes.tsx:178`. Обе в незакоммиченных правках другой сессии.
- «Впереди» показывает сцену, которую уже запускали, но не отметили пройденной: у «Вотердипа» «2. Фреска увечий» запущена 22.08 и стоит «впереди», потому что `campaign_scene_state` не `done`. По решению так и есть (критерий — отметка), но за столом может читаться как ошибка. Правило «запущена — значит пройдена» было введено и сразу откачено: владелец указал, что запуски бывают тестовыми прогонами пульта. На живой базе это подтверждается — оба запуска 22.08 сделаны в 03:27:49 и 03:28:07, с разницей 18 секунд. **Решено владельцем 2026-09-11 (вариант 1):** критерий «Впереди» остаётся отметкой; у запускавшейся в сессиях кампании сцены — подпись «запускалась 22.08» (`launched_at`, копия и исходник сверяются) и кнопка «пройдена» прямо в строке (`PUT /story/scenes/:id/state`, `status: 'done'`). Отвергнуто: «запущена — значит пройдена» (тестовые прогоны) и «запущена в проведённой сессии» (прогон 22.08 как раз в проведённой).
- Пульт на живой базе открывался только чтением (в сети при загрузке — одни GET). Проверено: строка «Партия здесь», клик по месту в ней кладёт карточку наверх дока развёрнутой, плашка с флажком, путь без корня, «Партия здесь» неактивна, история только своей кампании, свернуть/развернуть плашкой. **Не проверено в интерфейсе:** клик по месту в панели «Локации» (у сессии 142 панель пуста — чтобы проверить, пришлось бы добавить связь) и нажатие «Мы здесь» (переставило бы настоящую точку партии; покрыто `partyPlace.test.ts`).
- **Сверка с планом 2026-09-11** (по вопросу владельца «всё ли по плану»). Отличия от записанного: (1) узкий режим включается при ширине рабочей области < 640px, а не < 560px — карточка теперь постоянной ширины 320px и в аккордеон не входит, колонкам при 560 не остаётся места; (2) в бейдже строки проводника — `kind`, а роль словом только когда `kind` пуст (роль всегда видна иконкой); (3) «активные кампании» для флажков и «Впереди» в профиле — все, кроме `completed` (приостановленные включены: к ним возвращаются); «Было» статус кампании не фильтрует. Найдено и исправлено при сверке: панель «Локации» в окне попаута (`/sessions/:id/live/panel/locations`) после этапа 6 при щелчке ничего не показывала — клала место в док главного окна; теперь в док только на самом пульте, в попауте — превью, как раньше.
- Узкий режим проводника на скрытой панели браузера «залипает»: ResizeObserver не срабатывает без отрисовки кадра, первая перерисовка компонента выправляет. Артефакт проверки, не ошибка кода.
- Проверка в браузере на ширине 1440 через эмуляцию вьюпорта: скриншот снимается мелко, а увеличение фрагмента в панели не работает — раскладку проверять замерами (`getBoundingClientRect`), а клик по элементу по ссылке из `find` при эмуляции промахивается.
