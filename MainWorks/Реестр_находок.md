# Реестр находок и дефектов

> **Заведён:** 2026-09-03 · **Ветка:** `restore/2026-08-28-simple-tasks`
> **Что это:** одно место для всего, что найдено попутно и не выдумано из
> плана. Находки до этого расползались по трём документам
> (`Аудит_4_перспективы.md`, `Отчёт_о_слиянии_2026-09-02.md`,
> `План_оставшихся_работ.md`) и терялись между сессиями.
>
> **Как читать:** статус говорит, что с находкой делать. `Починено` — закрыто,
> оставлено для истории, чтобы не переоткрывать. `Открыто` — дефект живой,
> решения не требует, только работы. `Ждёт решения` — работать нельзя, пока
> владелец не выберет. `Наблюдение` — не дефект, но знать полезно.
>
> Цифры проверены командой в день записи, а не взяты из памяти. Если цифра
> разошлась с текущей — перемерить, а не спорить.

---

## Сводка

| № | Что | Статус | Где |
|---|---|---|---|
| F-01 | Проверка типов клиента не проверяла ничего и всегда возвращала 0 | Починено | §1 |
| F-02 | «Отменить» не отменяла: повторный `DELETE` вместо `restore` | Починено | §2 |
| F-03 | Тост отмены умирал вместе со страницей | Починено | §3 |
| F-04 | В `CampaignDetailPage` тост не был отрисован вовсе | Починено | §3 |
| F-05 | У дерева локаций была отдельная, своя отмена | Починено | §4 |
| F-06 | zip-статблок: тип не знал форматов, 40 ошибок сборки | Починено | §5 |
| F-07 | zip-форматы не проверялись на разбор JSON на сервере | Починено | §5 |
| F-08 | Кнопки «Экспорт» и «Импорт приключения» вели в никуда | Починено | §6 |
| F-09 | Отмена удаления для изображения галереи невозможна | Починено | §7 |
| F-10 | Три функции удаления объявлены и нигде не вызываются | Починено | §8 |
| F-11 | ~~Локацию нельзя удалить насовсем~~ | **Опровергнуто** | §9 |
| F-12 | 50 нативных `confirm()` браузера в 32 файлах | Починено | §10 |
| F-13 | ~~`ImportAdventurePage` — не импорт приключения~~ | **Опровергнуто** | §11 |
| F-14 | ~~Недостижимая ветка `isMechanicsSection`~~ · переформулирована | **Опровергнуто**, см. F-28 | §12 |
| F-15 | `DndCoinsEdit` / `DndCoinsView` объявлены, не вызываются | Починено | §12 |
| F-16 | Пять ошибок линтера `rules-of-hooks` | Починено | §13 |
| F-17 | `narrativeCallbacks` потеряны при слиянии | Починено, 8 → 2 рендера | §14 |
| F-18 | Выгрузки и загрузки приключения нет ни одной половины | Починено | §15 |
| F-19 | Перетаскивание без тач-дублёра — подтверждено замером | Ждёт `grilling` | §16 |
| F-20 | Сырые hex в CSS не следуют смене темы | Открыто | §16 |
| F-21 | Шрифты прибиты глобально, мимо режима | Открыто | §16 |
| F-22 | Тем шесть, режимов пять; пользовательские не проверяются | Открыто | §16 |
| F-23 | Скевоморфные тени в `soundConsole.css` | Открыто | §16 |
| F-24 | Три файла-гиганта | Открыто | §17 |
| F-25 | Vite отдавал устаревший модуль при живой правке | Наблюдение | §18 |
| F-26 | Синтетические клики не доходили до элементов | Наблюдение | §18 |
| F-27 | Дублирующиеся ключи `day` в React | Починено | §19 |
| F-28 | Фильтр категорий справочника недостижим — стоит после раннего возврата | Починено | §20 |
| F-29 | «+ Добавить mechanics» — англицизм в интерфейсе Мастера | Открыто | §21 |
| F-30 | 10 нативных `prompt()` — тот же класс, что F-12 | Починено | §22 |
| F-31 | `confirm({…})` без `await`: подтверждение не работало | Починено | §23 |
| F-32 | Приключение и сцена уходили в архив без обратного пути | Починено частично | §24 |

---

## 1. F-01 · Проверка типов клиента была фикцией

**Статус:** починено 2026-09-03 · **Важность:** наивысшая из всех записанных.

Проверки клиента шли командой `npx tsc --noEmit -p tsconfig.json`. Но
`client/tsconfig.json` — солвент-конфиг: `"files": []` и одни `references`. Он
**не проверяет ни одного файла и всегда возвращает 0**. Правильная команда —
`tsc -b`.

Из-за этого в ветке накопилось **68 ошибок TypeScript в 13 файлах**, и все
проверки всё это время рапортовали успех. Серверные проверки были настоящими
(`include: ["src"]`), поэтому расхождение никто не замечал.

Рядом та же ловушка, уже записанная в `CLAUDE.md`: `npx tsc … | head`
возвращает код `head`, а не компилятора.

**Что делать впредь:** клиент проверяется только `tsc -b`, без пайпа.

---

## 2. F-02 · «Отменить» возвращала 200 OK и не отменяла

**Статус:** починено 2026-09-03 · коммит `253caa1`

`SessionDetailPage.tsx` и `CampaignDetailPage.tsx` передавали в хук
`restoreFn: () => api.del(…)` — то есть **повторный `DELETE`**.

Но удаление на сервере — это не удаление:

```
sessions.ts:259    UPDATE sessions  SET archived_at = datetime('now') WHERE id = ?
campaigns.ts:334   UPDATE campaigns SET archived_at = datetime('now') WHERE id = ?
```

Второй `DELETE` просто переписывал дату новой. Сущность оставалась в архиве,
запрос возвращал 200 OK, интерфейс показывал успех. Отмены не происходило
никогда.

Эндпоинты `PUT /:id/restore` (`archived_at = NULL`) существовали всё это
время — их просто не звали. Обе страницы переведены на них.

**Чем опасен класс:** дефект неотличим от работы. Запрос успешен, ошибок нет,
проверить можно только глазами на следующем экране.

---

## 3. F-03, F-04 · Тост отмены не доживал до нажатия

**Статус:** починено 2026-09-03 · коммит `253caa1`

Состояние отмены жило в самой странице, и тост рисовался её же разметкой. Но
удаление сессии, кампании и персонажа заканчивается `navigate(…)`: страница
размонтировалась и уносила тост с собой. Мастер видел вспышку или не видел
ничего.

Отдельно (F-04): в `CampaignDetailPage` тост **не был отрисован вообще** —
`undoToast` брали из хука и не использовали ни разу. Архивация кампании была
необратима на практике, хотя механизм отмены formally присутствовал.

Починено переносом в оболочку: `UndoDeleteProvider` живёт в `AppShell`, тост
висит поверх новой страницы все 8 секунд. Заодно ушла четырёхкратная копия
одной и той же разметки тоста.

**Проверено живьём** (scratch-база, порт 5174): архивация локации →
`DELETE /api/setting-locations/611` → 200, клик «Отменить» →
`PUT /api/setting-locations/611/restore` → 200, локация вернулась в дерево.

---

## 4. F-05 · У дерева локаций была своя отмена

**Статус:** починено 2026-09-03 · коммит `253caa1`

`LocationTree.tsx` держал собственный механизм: состояние `lastArchived`,
карточка с кнопкой «**Восстановить**» вместо тоста с «**Отменить**», и
**без таймера** — висела, пока не закроешь крестиком.

То есть на одно действие в приложении было два разных ответа, в двух разных
видах, с разными словами. Ровно то дублирование, которое разбирает аудит
(M-02). Сведено к общему механизму.

---

## 5. F-06, F-07 · zip-статблок

**Статус:** починено 2026-09-03 · коммит `281ced7`

`StatblockFormat` не знал `zip_character` и `zip_creature`, хотя модуль был
дописан — **40 ошибок из 68**. Расширен тип, заведены `zipValue` и два быстрых
сохранения. Убраны два недостижимых блока `zip_creature`: существо рисуется
ранним возвратом (`StatblockList.tsx:929`), как `dnd_creature`.

F-07 отдельно: на сервере zip-форматы не были в `JSON_FORMATS`, то есть их
содержимое **не проверялось на разбор JSON** — битый статблок молча уехал бы в
базу.

---

## 6. F-08 · Кнопки вели в никуда

**Статус:** починено 2026-09-03 · коммит `281ced7` · связано с F-18

«Экспорт» и «Импорт приключения» в `AdventuresTab` вызывали
`POST /story/arcs/import` — эндпоинта нет. Как и `GET /story/arcs/:id/export`.
Нет **обеих** половин. Кнопки убраны: интерфейс не должен обещать
несуществующего.

---

## 7. F-09 · Отмена для изображения галереи требует серверной работы

**Статус:** починено 2026-09-03, коммит `48783b1`

`galleryRouter.delete` (`gallery.ts:169`) делал `DELETE FROM gallery_images`.
Строки больше нет; файл ушёл в архив хранилища либо стёрт совсем — решает
`removeOrArchive` и ответ Мастера на диалог «удалить навсегда / в архив».

Дырка была шире, чем «нет кнопки»: файл-то в `_Archive` лежал, а вернуть его
было нечем — ни владельца, ни подписи, ни позиции в `archived_files` нет,
только имя и путь.

Сделано после `grilling`:

- таблица `gallery_image_undo` — короткая запись на время тоста;
- `PUT /gallery/undo/:id` возвращает файл на прежнее место и создаёт строку
  заново, с той же подписью и той же позицией;
- `offerUndo` в `useUndoDelete` — тост без собственного удаления, потому что
  в галерее между нажатием и удалением стоят два диалога выбора;
- после «удалить навсегда» тоста нет: отменять нечего.

Отдельная таблица, а не `archived_at` у самой картинки: `gallery_images`
читают шесть мест (галерея, раздел игрока, выгрузка сеттинга, сироты,
диагностика, доступ игрока), и добавлять всем фильтр ради восьми секунд
несоразмерно.

**Побочная находка.** `removeOrArchive` уносил файл в `_Archive` только если
это была последняя жёсткая ссылка; при общих байтах он молча делал `unlink`,
что бы вызывающий ни просил. Из-за этого обещание `systems.ts:563` — «аватар
записи компендиума уходит в архив, чтобы случайное удаление можно было
отменить» — не выполнялось ровно в том случае, когда картинка встречалась
дважды. Теперь `choice === "archive"` уважается всегда, а `rename` сохраняет
inode, так что соседняя ссылка цела и места не прибавляется.

---

## 8. F-10 · Три функции удаления, которые никто не зовёт

**Статус:** починено 2026-09-03, коммит `612d8d9`

Решение владельца: кнопки не возвращать, а повесить действия на правую кнопку
мыши и долгое зажатие — «в ПКМ много инструментов можно быстро закинуть».
Функции подключены через контекстное меню плиток Населения, бестиария и
сообществ; архивация из списка идёт через `deleteWithUndo`, потому что из
списка промахнуться легче, чем из профиля.

Ниже — как выглядела находка.

В `SettingDetailPage.tsx` были объявлены и **нигде не вызывались**:

| строка | функция | что должна делать |
|---|---|---|
| `:1666` | `deleteBeing` | архивировать личность |
| `:1924` | `deleteBeing` | архивировать запись бестиария |
| `:2076` | `deleteCommunity` | архивировать сообщество |

Проверено `grep` по всему `src/`: ни одного вызова, включая инлайновые
`onClick`.

Значит **из списков сеттинга удалить существо или сообщество нечем** — только
зайдя в профиль. Либо кнопки потерялись при какой-то правке, либо так и
задумано; тогда функции надо убрать.

Отмена удаления в них не добавлялась намеренно: незачем чинить то, что не
вызывается. Решать надо было сначала: вернуть кнопки или удалить код.

Заодно убран брошенный импорт `BeingEntityRowList` — кто-то начал переход на
плитки и не довёл.

---

## 9. F-11 · Опровергнуто: удалить локацию насовсем можно

**Статус:** опровергнуто 2026-09-03, при проверке перед работой.

Находка утверждала, что `DELETE FROM setting_locations` в сервере нет вовсе,
а значит архив локаций нечем вычистить.

Неверно. Удаление насовсем идёт через страницу «Архив» — общий роут
`DELETE /archive/:type/:id` (`archive.ts:392`), где таблица берётся из
`ARCHIVE_TABLES`, и там есть `location: "setting_locations"` (`:378`). Сам
запрос собирается шаблоном `DELETE FROM ${table} WHERE ${key} = ?` — поэтому
поиск по `DELETE FROM setting_locations` его и не нашёл.

Роут устроен аккуратнее, чем я предполагал: удалить можно **только из
архива** (иначе 400), всё в одной транзакции вместе с `sweepOrphans`, папка на
диске сносится последней, после коммита.

Проверено живьём: на странице «Архив» есть фильтр «локация» и у каждой записи
кнопки «Восстановить» и «Удалить навсегда».

**Урок тот же, что у F-13 и F-14:** отсутствие текста в коде не доказывает
отсутствия поведения. Шаблонная строка грепом не ищется.

---

## 10. F-12 · Системные окна браузера · починено

**Статус:** починено 2026-09-03, коммит `f53c594`. Было **50** нативных
вызовов в 32 файлах при 65 уже переведённых. Стало **0**.

Первоначальная цифра в этом реестре (52) была завышена: `grep` не отличал
`window.confirm` от `confirm(...)` хука. Признак точный — `useConfirm`
возвращает Promise, поэтому его вызов всегда с `await`.

### Почему это не косметика

В `electronApi.ts` живёт патч `installNativeDialogFocusFix`, и его комментарий
объясняет цену вопроса: после нативного диалога Windows не возвращает фокус
webContents — «в поле курсор стоит, а буквы не появляются», помогает только
переключиться на другое окно и обратно. Патч поставили на сам
`window.confirm`, «а не на каждый из девяноста вызовов».

Теперь для `confirm` первопричины не осталось. Плюс системное окно блокирует
процесс целиком и не следует ни теме, ни шрифтам приложения — за столом это
хуже диалога внутри окна.

### Заодно ушёл текст, который ничего не говорил

«Вы уверены, что хотите удалить ЭТО?» стоял в двадцати с лишним местах. Теперь
диалог называет предмет разговора: «Удалить атаку «Укус»?», «Убрать «Огненный
шар» из списка заклинаний?», «Удалить главу вместе с её текстом?», «Убрать
чувство «Тёмное зрение»?».

Где имя недоступно — текст называет хотя бы род: «Удалить задачу?», «Удалить
напоминание?», «Удалить заметку о связи?», «Убрать участника из очереди хода?».

---

## 11. F-13 · Опровергнуто: `ImportAdventurePage` — именно импорт приключения

**Статус:** опровергнуто 2026-09-03, при проверке перед правкой.

Находка утверждала, что имя файла не отражает содержимое: маршрут `/import`,
вызовы `/import/plan` и `/import/apply` — значит «универсальный импортёр».

Неверно. Заголовок страницы, который видит Мастер, — **«Импорт приключения»**
(`ImportAdventurePage.tsx:305`), а вход в неё подписан «Импорт книги
приключений» (`ModulesTab.tsx:311`). Эндпоинты `/import/*` — это внутренний
механизм разбора книги на батчи, а не другая функция.

**Что из этого следует для F-08 и F-18.** Импорт приключения в приложении
**есть** — из внешнего файла книги, через `ModulesTab`. Нет другого: обмена
готовым приключением между базами SoyMan (`/story/arcs/export|import`). Кнопка,
убранная в F-08, звала именно этот отсутствующий путь, так что убрана
правильно — но формулировку «импорта приключения нет» надо читать как «нет
обмена между базами».

Проверено, что вход в работающий импорт не пострадал: ссылка из `ModulesTab`
на месте, других входов не было.

---

## 12. F-14 опровергнуто · F-15 починено

### F-14 · Ветка `isMechanicsSection` живая

**Статус:** опровергнуто 2026-09-03. Код чуть не был удалён по неверной
находке — спасла проверка перед правкой.

Находка опиралась на то, что `kind === "mechanics"` рисует `MechanicsSection`
(`SystemDetailPage.tsx:321`), а значит `CompendiumSection` механик не видит.

Но `MechanicsSection` — **тонкий шов**: `return <CompendiumSection {...props} />`
(`MechanicsSection.tsx:11`), и пропсы передаются целиком, включая `section` с
`kind === "mechanics"`. Внутри `CompendiumSection` флаг **истинен**, а ветка
`if (isMechanicsSection)` (строки 1205–1333) — это и есть та разметка
справочника, которую Мастер видит на экране.

Проверено живьём: справочник D&D 5.5 (589 записей) рисуется блоками
категорий — «Магические эффекты · 6», «Навыки и владения · 7»,
«Мироустройство · 10».

Отсюда же следует, что состояние `mechanicsCategoryFilter`, возвращённое при
починке сборки, было возвращено **правильно** — удалять его было бы ошибкой.
Настоящая проблема с ним другая, см. F-28.

### F-15 · `DndCoinsEdit` / `DndCoinsView` удалены

**Статус:** починено 2026-09-03.

Обе функции локальные (не экспортируются) и не вызывались нигде — `HEAD` убрал
редактор монет намеренно. Вместе с ними ушли `COIN_LABELS` и `COIN_ORDER`,
которые использовались только внутри них. 44 строки.

---

## 13. F-16 · Пять ошибок линтера · починено

**Статус:** починено 2026-09-03. `npx oxlint src` теперь даёт **0 ошибок** —
впервые за всё время работы по этой ветке.

Оба случая — хуки под условием, класс дефекта, который однажды уже ронял
раздел.

**`InworldCalendar.tsx`.** `useRef` для долгого нажатия стоял **после** раннего
возврата `if (months.length === 0) return <p>Календарь сеттинга не настроен.</p>`.
При ненастроенном календаре компонент выходил до этого хука — два хука; как
только месяцы приходили — три. React получал другой порядок между рендерами.
`useRef` поднят выше возврата.

Проверено живьём на обеих ветках: сеттинг без календаря (Рейвенлофт) рисует
«Календарь сеттинга не настроен», сеттинг с календарём (id 1) рисует все
двенадцать месяцев и сетку дней. Ошибок React о порядке хуков в консоли нет.

**`litm/ThemeCard.tsx`.** В `ThemeKitPicker` строка `if (!isOpen) return null`
стояла **перед** тремя `useState` и `useEffect`. Закрытая модалка — ноль хуков,
открытая — четыре. Хуки подняты выше возврата; чтобы закрытая модалка при этом
не грузила справочник, в эффект добавлен ранний выход `if (!isOpen) return`, а
`isOpen` — в зависимости. Сам `return null` перенесён вниз, после хуков.

---

## 14. F-17 · `narrativeCallbacks` восстановлены

**Статус:** починено 2026-09-03, **но числами не подтверждено** — см. оговорку.

`MentionTextarea` объявлен через `memo` (`MentionTextarea.tsx:53`), однако
`onChange` собирался заново на каждый рендер прямо в разметке:

```
onChange={(v) => setDraftDossier({ ...draftDossier, [dossierKey]: v })}
```

Новая ссылка на функцию при каждом рендере означает, что `memo` не работал
вовсе: нажатие клавиши в «Идеалах» перерисовывало и «Черты характера», и
«Привязанности», и «Слабости».

Восстановлено не тем способом, каким было в `wip`: колбэки берут прежнее
состояние через функциональный сеттер
(`setDraftDossier((prev) => …)`), поэтому у `useMemo` **пустые зависимости** и
ссылки стабильны на всю жизнь компонента. Вариант с `[draftDossier]` в
зависимостях выглядел бы мемоизацией, но пересоздавал бы колбэки на каждый
набранный символ — то есть не дал бы ничего.

**Замер сделан 2026-09-03.** Во временный счётчик в теле `MentionTextarea`
считалось число рендеров на 10 нажатий клавиши в поле «Идеалы», три прогона
подряд, значения совпали до единицы:

| | рендеров на 10 нажатий | на одно нажатие |
|---|---|---|
| до правки | 80 | **8** |
| после | 20 | **2** |

Вчетверо меньше. Восемь — это четыре поля, каждое дважды (React в dev-режиме
рендерит дважды); два — только то поле, в котором печатают.

Отдельно стоит записать, чего замер **не** показал: по времени разницы не
видно. Медиана синхронного рендера — 3.2–3.5 мс и до, и после, разброс между
прогонами больше самой разницы. `MentionTextarea` дёшев, и лишние рендеры
тонули в шуме. То есть правка верна и подтверждена числом рендеров, но обещать
Мастеру «станет заметно быстрее» на этих данных нельзя.

---

## 15. F-18 · Выгрузки и загрузки приключения нет целиком

**Статус:** починено 2026-09-03, коммит `79db8b7`

Не было ни `GET /story/arcs/:id/export`, ни `POST /story/import`. При этом обе
рабочие функции — `buildAdventureExportData` и `importAdventureExport` — были
написаны давно; позвать их снаружи было нечем, они использовались только
выгрузкой сеттинга целиком. Девятый случай «объявлено, но не подключено», и
самый дорогой: тут не забыли кнопку, а не довели до конца целую возможность.

Сделано после `grilling`: маршруты, «Выгрузить в файл» на странице
приключения и в контекстном меню списка, «Загрузить приключение» с галочкой
«с изображениями» на вкладке Приключения. Совпадение имени — 409 и вопрос
Мастеру, три ответа названы последствием: «Создать новое», «Заменить»,
«Слить». Копия перед слиянием делается тем же кодом выгрузки-загрузки и
уезжает в архив, откуда её возвращает `restore` из F-32.

**Формат не работал для русского приключения.** Ключи внутри
`adventure-export/1` строятся из имён, а `slugify` чистил строку правилом
`[^a-z0-9]` — кириллица вычищалась целиком, и КАЖДАЯ глава получала ключ
`chp.unnamed`. При загрузке все сцены сваливались в ту главу, что записалась в
карту последней, а переходы, вехи и тайны указывали на случайную сцену.
Проверено живьём до правки: приключение из двух глав приезжало с обеими
главами, но все три сцены лежали в одной. Правило заменено на `\p{L}`.

Рядом — вторая мина того же места: две сцены с одинаковым именем давали один
ключ, и вторая затирала первую в карте. Добавлен счётчик `_2`, `_3`; выгрузка
и загрузка идут по спискам в одном порядке, поэтому нумерация совпадает.

**Картинки не доезжали.** `importAdventureExport` подставлял `null` вместо
обложки и целиком пропускал `canvas.images` — при том, что выгрузка их
честно клала в файл. Касалось это и выгрузки сеттинга целиком: приключения
приезжали без обложек и без картинок схемы. Файлы теперь пишутся до
транзакции (в неё `await` не поставить), в
`Settings/<сеттинг>/Adventures/<имя>`.

**Что решено НЕ переносить при слиянии:** переходы, вехи, тайны и схему. Они
связывают сущности между собой, и накладывать их поверх чужого дерева значит
рвать местные связи ради чужих. Приходят они только с новым приключением
(«Создать новое» и «Заменить»). В диалоге обещаны ровно тексты — так и
сделано.

---

## 16. F-19 · Перетаскивание на планшете не работает · замер

**Статус:** ждёт `grilling` · подтверждено проверкой 2026-09-03

Перетаскивание живёт в **15 компонентах**, тач-обработка — в двух, и та не про
перетаскивание (`InworldCalendar` — долгое нажатие, `PlayerContentReader` —
чтение).

Проверено эмуляцией планшета (Android, 5 точек касания): жест
touchstart → touchmove → touchend на элементе с `draggable="true"`
**не вызывает `dragstart`**. То есть это не «неудобно», а недоступно совсем.

Где альтернатива есть:

| действие | обходной путь |
|---|---|
| перевесить локацию | селект родителя в профиле (`LocationDetailPage:361`) |
| положить в мешок | кнопка «В мешок» |

Где альтернативы нет — везде, где перетаскиванием меняют **порядок**:
приключения (`AdventuresTab`), ресурсы (`ResourcesSection`), плейлисты
(`SoundSetsTab`), галерея (`GalleryTab`), разделы для игроков
(`CampaignPlayerSectionsTab`), записи компендиума (`CompendiumSection` —
17 мест), сцены и главы (`AdventureDetailPage`), узлы Полотна (`CanvasPage`).

**Почему кодом не начинать.** Замена HTML5 drag-and-drop на Pointer Events —
это новое поведение существующего во всём приложении сразу, а не правка внутри
принятого решения. Плюс развилка, которую должен решить владелец: чинить
перетаскивание на тач или дать кнопочный дублёр («выше/ниже»), который заодно
работает с клавиатуры и не требует точности пальцем за столом.

## 16a. F-20…F-23 · Из аудита, не разобранное

- **F-20.** 194 сырых hex в `src/*.css` (на 2026-09-03). Не следуют смене
  режима: тема меняется, эти цвета — нет.
- **F-21.** Квартет голосов не принадлежит режиму: `--font-ui` и `--font-mono`
  прибиты глобально (D-07).
- **F-22.** Тем шесть, а режимов в `design_revision.md` пять. Пользовательские
  темы не проверяются ни на один инвариант (D-08).
- **F-23.** `soundConsole.css` — скевоморфные тени вне заявленных исключений
  §1.2. Убрать либо задокументировать как второе исключение.

---

## 17. F-24 · Файлы-гиганты

**Статус:** открыто · Фаза 5, по остаточному

| файл | строк |
|---|---|
| `pages/CanvasPage.tsx` | 8016 |
| `index.css` | 7848 |
| `components/dnd/DndCharacterForm.tsx` | 5275 |

Плюс автоподстановка версии при сборке: сейчас дату в `package.json`
проставляют руками, и забытое обновление даёт релиз, подписанный датой
предыдущей сборки.

---

## 18. F-25, F-26 · Наблюдения о среде проверки

**Статус:** наблюдение, не дефект приложения

- **F-25.** Vite отдавал устаревший модуль (`DndCharacterView` «не
  экспортируется»), пока владелец правил `DndCharacterForm.tsx` в соседнем
  окне. `tsc -b` и `npm run build` при этом проходили — код был валиден,
  испорчен был кэш dev-сервера. Перезагрузка страницы не помогала; помог
  переход на другую пару серверов.
- **F-26.** Синтетические клики панели браузера не доходили до элементов
  (кнопки открытия модалок не срабатывали), тогда как программный
  `element.click()` работал. Проверка велась через `javascript_tool`.

**Порядок проверки, который сработал:** dev-серверы `client`/`api` на
5173/3001 смотрят в рабочую базу владельца, менять её нельзя. Проверка шла на
`client-scratch` (5174) + `api-scratch-alt` (3002) — отдельная
`server/data-scratch`. Заведённые для проверки записи убраны после работы.

---

## 19. F-27 · Дублирующиеся ключи · починено

**Статус:** починено 2026-09-03

Источник нашёлся не в календаре, а в `Timeline.tsx` — список ступеней
масштаба:

```
{ key: "day", label: "День",  pxPerDay: 14  },
{ key: "day", label: "3 дня", pxPerDay: 280 },
```

Обе ступени работают в точности «день», поэтому `key` у них одинаковый, а
рендер кнопок шёл как `key={z.key}`. React считал две кнопки одной: при смене
масштаба подсветка могла достаться не той, а сама кнопка — пропасть.

Ключ переведён на `z.label` — метки уникальны и не зависят от порядка в
массиве. Сплошной поиск по константным массивам с полем `key` показал, что
случай единственный.

Проверено живьём на хронике сеттинга: все шесть ступеней на месте, каждая
подсвечивается при своём нажатии, включая «День» и «3 дня»; предупреждение из
консоли ушло.

Искал я его сперва в `InworldCalendar`, потому что предупреждение всплывало на
вкладке с календарём. Ключ `c.day` там ни при чём — просто оба компонента
живут на одной вкладке.

---

## 20. F-28 · Фильтр категорий справочника · подключён

**Статус:** починено 2026-09-03, коммит `9471e75`

Ветка `if (isMechanicsSection)` в `CompendiumSection` заканчивается `return`, и
всё, что стояло ниже с той же проверкой, не исполнялось никогда — включая
**кнопки фильтра по категориям**. Фильтр был написан целиком: состояние
`mechanicsCategoryFilter`, кнопки со счётчиками, ветка отображения. Не хватало
только того, чтобы он оказался внутри живой ветки.

Перенесён под строку поиска. Повторный клик по активной кнопке снимает отбор.
Счётчики считаются по тем же спискам, что и блоки, — иначе «Магические
эффекты · 6» показывало бы шесть, а раскрывалось пять: избранные уходят из
категорий в отдельный блок наверх. Ряд появляется, только когда категорий
больше одной.

Заодно удалено остальное недостижимое из-под того же возврата: второй набор
«Выбрать всё / Печать / Показать игрокам» (внутри ветки давно свой, с
«Верстаком»), дубль счётчика «Выбрано», ветка отображения по категориям и
`useMemo` `mechanicsByCategory`, ставший ненужным.

Проверено на справочнике D&D 5.5: «Все · 44», далее шесть категорий
(6 + 7 + 10 + 11 + 5 + 5 = 44). Клик по «Мироустройство · 10» оставляет один
блок из шести, повторный клик возвращает все.

---

## 21. F-29 · Англицизм в интерфейсе Мастера

**Статус:** открыто · мелочь, но заметная

Пустой справочник системы предлагает кнопку **«+ Добавить mechanics»** —
латиницей, служебным словом из кода. Рядом всё остальное по-русски:
«Заведите списки — типы существ, школы магии, свойства оружия…».

Видно на системе без записей в справочнике (например, City of Mist).
Скорее всего, подпись собирается из `section.kind`; проверить, нет ли того же
в других разделах — «+ Добавить monster», «+ Добавить vehicle».

---

## 22. F-30 · Нативный `prompt()` · заменён

**Статус:** починено 2026-09-03, коммит `269dae6`

Живых вызовов оказалось 10, а не 14: остальные совпадения — комментарии, в том
числе объясняющие, почему `prompt()` там уже не используют.

Заведён `usePrompt` рядом с `useConfirm`: возвращает Promise со строкой или
`null`, поэтому вызывающий код почти не менялся. Enter сохраняет, Esc
отменяет, текст выделен целиком сразу — при переименовании старое имя чаще
заменяют, чем дописывают.

Переведены: переименование приключения, главы и сцены
(`AdventureDetailPage`); быстрые сцена, приключение, доска, стикер и
переименование узла (`CanvasPage`); показ упоминания при недоступном буфере
обмена (`LocationTree`) — для него у хука есть режим `readOnly`: поле только
для чтения и одна кнопка «Закрыть».

Цена вопроса та же, что у F-12, и в коде она уже была описана: в
`CanvasPage:1904` от `prompt()` отказались вручную, потому что «тот блокирует
окно и в части окружений просто не работает». Решение было найдено — не
хватало общего компонента.

Проверено живьём: диалог открывается с выделенным именем (14 из 14 символов),
Enter сохраняет, «Отмена» и Esc закрывают без изменений.

---

## 23. F-31 · Подтверждение, которое не подтверждало

**Статус:** починено 2026-09-03, коммит `f53c594`

`ModulesTab.tsx:111` вызывал `confirm({...})` от `useConfirm` **без `await`**:

```
if (!confirm({ title: …, message, … })) return;
```

`confirm` возвращает Promise — всегда истинный, поэтому `!promise` всегда
ложно, `return` не срабатывал никогда. Модуль удалялся **что бы Мастер ни
ответил**, а диалог оставался висеть уже поверх свершившегося факта.

Найдено при сплошном переводе на `useConfirm`. Проверены все 65 вызовов —
случай единственный.

Класс тот же, что у F-02: дефект неотличим от работы. Диалог показан, кнопки
нажимаются, ошибок нет — только результат не зависит от ответа.

---

## 24. F-32 · Приключение и сцена уходили в архив без обратного пути

**Статус:** починено 2026-09-03, коммит `714e922`

`DELETE /story/arcs/:id` и `DELETE /story/scenes/:id` ставят `archived_at` —
запись остаётся в базе. Но вернуть её было **нечем**:

- маршрута `PUT /:id/restore` у них не было (он есть у 12 других типов);
- в `ARCHIVE_TABLES` (`routes/archive.ts:369`) ни `story_arcs`, ни
  `story_scenes` не входят, поэтому страница Архива их не показывает вовсе.

То есть «Архивировать» для приключения и сцены было односторонней дверью, а
подтверждение — единственной защитой. Данные не терялись, но добраться до них
Мастер не мог.

Найдено при подключении контекстного меню к спискам: чтобы дать «Отменить»,
понадобился `restoreFn` — и оказалось, что вызывать нечего.

Добавлены оба маршрута; архивация в списке приключений и в списке сцен идёт
через `deleteWithUndo`. Главы и сцены своей отметки при архивации приключения
не получают — списки берут их через `parent_id`/`arc_id`, так что уходит и
возвращается всё одной строкой.

Оговорка: `DELETE /story/scenes/:id` перед архивацией материализует вставки
заготовки (`detachFromLibrary`). Отмена возвращает саму сцену, но вставки
остаются самостоятельными — обратной операции у `detachFromLibrary` нет, и
придумывать её ради отмены значило бы второй раз молча переписать чужие
приключения.

**Остаётся открытым:** приключений и сцен по-прежнему нет на странице Архива.
Отмена спасает от промаха в ту же минуту, но не через день.

---

## 25. F-33 · Имена растровых ассетов в разном регистре · починено

**Статус:** починено 2026-09-12 (работа по растру)

`client/public/conditions/` и `schools/` держали имена вперемешку:
`Invisible.png`, `Paralyzed.png`, `Incapacitated.png` рядом с `blinded.png`,
`charmed.png`. На Windows это работает, на Linux — нет, а самостоятельный
хостинг предусмотрен (`deploy/nginx.conf`, systemd-юнит). Там значок
состояния отдавал бы 404 через раз.

Плюс в имени была закреплена опечатка — `posioned.png`, и тест
`conditionIcons.test.ts` её фиксировал, то есть защищал от исправления.

Владелец назвал находку случайностью. Закрыто реестром: все имена в нижнем
регистре, `posioned` → `poisoned`, руками пути больше не пишут — их отдаёт
`client/src/rasterAssets.ts`. Барьер `check-raster-assets.mjs` валит сборку
на прописной букве в имени файла.

---

## 26. F-34 · Растровые ассеты в шесть-десять раз крупнее нужного · починено

**Статус:** починено 2026-09-12 (работа по растру)

41 файл на 8,5 МБ при том, что вся сборка клиента — 7,3 МБ. Все `350×350`
(жетоны `460×460`) при показе в 30–44 px: `familiar.png` 494 КБ,
`stunned.png` 249 КБ. Девять десятых каждого файла не видел никто.

Переведены в `webp` с уменьшением до двойного экранного размера:
**8,5 МБ → 190 КБ**, каталог `public/` целиком 8,5 МБ → 1,8 МБ. Видимой
разницы нет — сверено до и после на листе персонажа.

Кость сохранена без потерь (`lossless`): у неё мягкий контурный край, на
котором сжатие с потерями даёт ореол.

---

## 27. F-35 · Дубль заклинания и потерянные ссылки на листе · открыто

**Статус:** открыто, найдено 2026-09-12 попутно

На листе персонажа 22 («Эрвин Лотт (ДнД)», живая база):

- в 5 круге **дважды** стоит «Связь с иным планом / Contact Other Plane» —
  две отдельные записи с одинаковым содержимым;
- внизу листа висит предупреждение «10 ссылок на справочник потеряны».

К дизайну отношения не имеет, в работе по растру не чинилось. Живая база не
менялась — запись только просмотрена.

---

## 28. F-36 · Ступень 9px на узком экране оказалась лишней · починено

**Статус:** починено 2026-09-12

`.dnd-triad .sb-label` под `@media (max-width: 360px)` снимал трекинг **и**
уходил на 9px. Комментарий объяснял: на 320 подписи вылезают из колонки 56, а
разрядка и один пункт кегля возвращают их в 47–48 px.

Замерено в браузере на 320 px: при `--fs-micro` (10px) и `letter-spacing: 0`
самая широкая подпись — «Пасс. воспр.», **53 px**, обрезки нет ни у одной из
пяти. Хватило одного снятия трекинга; ступень вниз была перестраховкой.

Ниже 10px в шкале ступеней нет, так что 9px был единственным местом, которое
шкала не могла выразить вовсе. Теперь не нужно.

---

## 29. F-37 · У Системы «Обзор» не был вкладкой · починено

**Что.** В `SystemDetailPage` полоса вкладок рисовала только разделы
справочника. Обзор системы (`activeTab === "overview"`, состояние по
умолчанию) в полосе отсутствовал: вернуться на него можно было **только**
щелчком по названию системы — приёмом, о котором неоткуда узнать.

**Как нашлось.** При переводе страницы на каркас я убрал щелчок по названию
как несистемный — и тем сделал обзор недостижимым. То есть регресс внёс я,
но обнажил он дефект, который был и до меня: единственная дорога к целому
разделу держалась на необъявленном приёме.

**Починено.** «Обзор» стоит первой вкладкой, как и требует словарь вкладок:
у вместилища первая вкладка — «Обзор».

**Урок.** Убирая приём, надо проверять не «красивее ли стало», а «не был ли
он единственной дорогой куда-то».

## 30. F-38 · Список «Ещё ▾» обрезался полосой вкладок · починено

**Что.** У сеттинга без кампаний часть вкладок прячется под «Ещё ▾».
Выпадающий список раскрывался (`details.open === true`, высота 206 px), но
не был виден: `.tabs` объявлен `overflow-x: auto; overflow-y: hidden`, то
есть клиппит всё, что торчит из полосы вниз, а список позиционирован
`top: 100%` внутри неё.

**Сколько жил.** Ровно столько, сколько обе части рядом; правило `.tabs`
старше моего первого коммита в этой сессии. Не попадался потому, что виден
только у сеттинга **без единой кампании**.

**Как нашлось.** Щелчок в живом приложении не дал ничего видимого. Замер
через DOM показал, что элемент есть и имеет размер; проверка гипотезы —
временно `overflow: visible`, и точка на месте списка стала кнопкой.

**Починено.** Список идёт тем же меню на уровне документа, что и «…».

**Урок.** «Не открылось» и «открылось, но обрезано» с экрана неотличимы.
Различает их только замер прямоугольника.

## 31. F-39 · Правка имени щелчком по заголовку · заменена

**Что.** Четыре карточки (Сеттинг, Существо, Персонаж, Сессия) правили имя
щелчком по заголовку. У двух была подсказка пунктиром, у двух — ничего.
Приём нигде не объявлен, и узнать о нём можно было только случайно.

**Заменено** названным действием «Переименовать» в меню «…».

## Что из этого стоит взять первым

1. **F-19** — на планшете перетаскивание не работает совсем, и там, где им
   меняют порядок, другого пути нет. Требует `grilling`: чинить тач или дать
   кнопочный дублёр.
2. **F-20…F-23** — тема и шрифты: сырые цвета не следуют режиму.
3. **F-29** — «+ Добавить mechanics» латиницей в интерфейсе Мастера.

**Не начинать кодом:** F-19 — сначала решение владельца или
`grilling`.

## Уроки реестра

**Находка — это гипотеза, а не факт, даже своя собственная.** Три находки из
взятых в работу оказались **неверными**: F-13, F-14 и F-11. По F-14 рабочий
код был бы удалён. Все три опровергнуты проверкой перед правкой.

**Отсутствие текста в коде не доказывает отсутствия поведения.** F-11 держался
на том, что `grep "DELETE FROM setting_locations"` ничего не находит. Запрос
собирается шаблоном `DELETE FROM ${table}`, и удаление работает.

**Цифру надо мерить, а не помнить.** F-12 записан как «52 вызова», на деле
50. F-30 записан как 14 `prompt`, живых оказалось 10. F-20 записан как 164
сырых hex, намерено 194.

**Замер отвечает не на тот вопрос, который задавали.** По F-17 время рендера
не показало ничего — разброс между прогонами больше разницы. Число рендеров
показало ровно вчетверо: 8 против 2 на нажатие. Метрику надо выбирать под
механизм, а не брать первую попавшуюся.

**Самые дорогие дефекты — те, что неотличимы от работы.** F-02 (кнопка
«Отменить» возвращала 200 OK и не отменяла), F-31 (подтверждение удаляло
модуль независимо от ответа), F-28 (фильтр написан и не подключён) выглядели
исправными с любой стороны, кроме результата.

**Класс «объявлено, но не подключено» живёт до сих пор.** Восемь
подтверждённых случаев, последний — F-28. Автоматически не ищется: поиск
неиспользуемых экспортов дал 754 позиции, включая заведомо живые. Ищется
только чтением при заходе в файл.

### F-40 — Ошибка загрузки без «Повторить» в важных датах локации

**Что.** `components/LocationImportantDatesTab.tsx` показывает карточку
ошибки тем же приёмом, что и остальные 21 место, но **без кнопки
«Повторить»**. Из двадцати двух мест без неё только два: это и
`LoginScreen`, где повторять нечего по смыслу — там форма входа.

**Почему заметно.** Сбой при загрузке дат оставляет Мастера на странице,
которую нечем оживить, кроме перезагрузки всего приложения.

**Откуда.** Перепись для каркаса списка · 2026-09-12
**Не чинилось:** правка попадает в шаг выноса состояний (`<Loadable>`), где
кнопка появится сама. Отдельно трогать не стал.

### F-41 — F-40 опиралась на неверный факт: там ошибка формы, а не загрузки

**Что.** Единственное вхождение подписи ошибки в
`components/LocationImportantDatesTab.tsx` — `errors.form` в модалке
создания/правки даты. Ошибки загрузки списка там нет вовсе: даты приходят
пропсами, три фоновых запроса гаснут в `.catch(() => {})`. Кнопке
«Повторить» у ошибки валидации нечего делать — повторяет отправку сама
модалка своей кнопкой сохранения.

**Что сделано.** При выносе состояний (шаг 1 каркаса списка, 2026-09-12)
место переведено на общий `LoadErrorCard` **без** кнопки — поведение
сохранено один в один. Предсказание F-40 («кнопка появится сама») не
сбылось и сбываться не должно: это была бы выдуманная механика, а не
починка.

**Откуда.** Шаг 1 каркаса списка · 2026-09-12

### F-42 — Баннеры ошибок поверх живого содержимого не стали обёрткой

**Что.** В списках кампаний/сеттингов/систем/игроков и у «Не удалось
обновить обитателей» карточка ошибки — аддитивный баннер: обновление
упало, данные на месте, список продолжает работать. Обёртка `<Loadable>`
по решению Q35 заменяет тело целиком — перевод этих мест на обёртку
прятал бы рабочую страницу при transient-ошибке (Мастер за столом с
плохим вайфаем). Решение Q35 буквального различия «баннер/замена» не
проводило.

**Что сделано.** Эти места переведены на общий кусок `LoadErrorCard`,
семантика «баннер поверх» сохранена. Обёртка применена там, где ошибка
и скелет сторожат одно тело: три секции сеттинга («Население»,
«Бестиарий», «Сообщества»). Если владелец хочет обёртку и в списках —
куски уже есть, правка дешёвая, но это решение, а не доделка.

**Откуда.** Шаг 1 каркаса списка · 2026-09-12

### F-43 — Вкладка «Вне групп» показывает всё, а не вне групп

**Что.** В списках с группами (`CampaignsListPage`, судя по коду — та же
схема у Сеттингов/Систем/Игроков) выбор «Вне групп» не грузит состав:
`loadGroupMembers` на этом ключе возвращается раньше с пустым множеством,
а фильтр `!groupMemberIds.has(id)` по пустому множеству пропускает всё.
Итог неотличим от «Все».

**Почему заметно.** Мастер, раскладывающий каталог по группам, не может
проверить, что осталось неразложенным, — инструмент наведения порядка
молчит.

**Не чинилось:** поведение сохранено один в один при переводе Кампаний на
каркас (`ListPage`, шаг 2–3). Починка — решением владельца: либо грузить
состав всех групп и вычитать, либо завести признак «вне групп» на сервере.

**Откуда.** Пилот каркаса списка (`CampaignsListPage`) · 2026-09-12

### F-44 — «Назад к списку» возвращает в начало списка, а не на то же место

**Что.** В `ListPage` предпросмотр прячет содержимое правилом
`.list-page.is-preview .list-page__content { display: none }` — на любой
ширине, не только ниже 900 px. Прокручиваемый контейнер (`main.app-content`)
от этого схлопывается, и браузер сбрасывает его `scrollTop` в 0 ещё в момент
открытия предпросмотра. Комментарий в `ListPage.tsx` и в `index.css`
(«список под ним не размонтируется, и место сохраняется само») не
соответствует поведению: не размонтируется — да, но спрятан, и место
теряется.

**Замер.** Кампании, ширина 590: прокрутка 1880 из 1880 → тап по последней
плитке → `scrollTop` уже 0 при открытом предпросмотре → «Назад к списку» →
0 из 1880.

**Почему заметно.** Ровно то, чего требовал Q43 и от чего предупреждало
задание проверяющему: вернуться в начало длинного каталога из середины —
на телефоне это каждый раз.

**Откуда.** Проверка каркаса списка · 2026-09-18
**Не чинилось:** проверка, а не правка.

### F-45 — Системный «назад» на телефоне уводит со страницы, а не из предпросмотра

**Что.** Выбор в `ListPage` не пишется в адрес: до и после тапа по плитке
`location.href` — `/campaigns`. На узком экране предпросмотр занимает весь
экран и выглядит отдельным экраном, но жест «назад» телефона закрывает не
его, а всю страницу Кампаний.

**Почему заметно.** Решение Q43 — «два экрана, а не две панели». Второй
экран, который не отвечает на «назад», — половина решения. Держать выбор в
адресе умеет `useTabState` у карточки; здесь нужен такой же приём. Решение
владельца, будет ли это `?selected=`.

**Откуда.** Проверка каркаса списка · 2026-09-18

### F-46 — Во «Выдаче» под каждой строкой дерева — полный путь на диске

**Что.** `CampaignIssuanceTab`, режим «Включение в панель»: под именем
каждой локации выводится `folder_path` целиком —
`E:\RPG-Vault\Settings\Эстария\Geography\Таленор\Королевство Сандар\…`.
В дереве путь повторяет то, что уже показано отступом, и удваивает высоту
каждой строки (у Эстарии 56 локаций).

**Почему заметно.** Жалоба владельца, ради которой вкладка и строилась, —
«сплошной список без структуры». Дерево теперь есть, но шум путей его
забивает; кроме того, это раскладка диска Мастера на рабочем экране.

**Откуда.** Проверка кабинета игрока · 2026-09-18

### F-47 — Мёртвые ручки `/player/settings` хранят снятое правило

**Что.** После удаления `PlayerSettingsListPage` и `PlayerSettingPage`
клиент не зовёт `GET /player/settings` и `GET /player/settings/:id`, но в
`server/routes/player.ts` они остались, а с ними `unionGrantLevels` в
`services/playerContent.ts` и тест «'open' в одной кампании побеждает
'mentioned' в другой» (`visibilityLevels.api.test.ts`). Это объединение
выдачи по всем кампаниям на одном сеттинге — ровно то, что решение 5
кабинета («один сеттинг в двух кампаниях — два разных Мира») отвергло.

**Почему заметно.** Правило, которое сняли, лежит в коде и защищено тестом:
следующий, кому понадобится «Мир игрока по сеттингу», найдёт готовую ручку
и вернёт её в строй.

**Откуда.** Проверка кабинета игрока · 2026-09-18

### F-48 — Шапка `ListPage.tsx` ссылается на барьер, которого нет

**Что.** Раздел «ПРИНУЖДЕНИЕ» в `client/src/components/ListPage.tsx`
называет `client/scripts/check-list.mjs` и перечисляет, что он ловит. Файла
нет, в `npm run lint` и `build` по-прежнему четыре барьера. Шаг 4 задания
исполнителю не выполнен.

**Почему заметно.** Комментарий, обещающий принуждение, опаснее отсутствия
комментария: читающий верит, что отступление поймают.

**Откуда.** Проверка каркаса списка · 2026-09-18

### Закрыто 2026-09-18, вместе с возвратом полосы вкладок (Q52–Q59)

- **F-43** — «Вне групп» у Кампаний вычитает состав всех групп. Сверено с
  базой: 5 кампаний вне групп, те же пять на экране.
- **F-44, F-45** — сняты вместе с предпросмотром каталога: щелчок по плитке
  ведёт в карточку, возвращаться некуда. У Игроков выбор в адресе, «назад»
  проверен.
- **F-48** — барьер `check-list.mjs` написан и подключён. Проверен
  подкладкой нарушения: ловит все четыре правила.

### F-49 — Пустая метка отступления проходила барьер

**Что.** В `check-entity-page.mjs` (и в новом `check-list.mjs`, списанном с
него) метка искалась регуляркой `намеренно\s*[—-]\s*(\S…)`: `\s*` после
тире захватывал перевод строки, и метка без причины брала причиной
**следующую строку кода**. Барьер, требующий причину, пропускал её
отсутствие. Найдено подкладкой пустой метки при проверке нового барьера.

**Что сделано.** В обоих барьерах пробелы вокруг тире — только `[ \t]`.
Все 12 настоящих причин у каркаса карточки проходят по-прежнему.

**Не чинилось:** та же регулярка стоит в `check-type-scale.mjs`
(«кегль мимо шкалы намеренно»). Правка та же, но это другой барьер — отдельно.

**Откуда.** Барьер каркаса списка · 2026-09-18

### F-50 — Две строки крошек на каждой карточке сущности

**Что.** Оболочка (`layout/AppShell.tsx`, `buildCrumbs`) рисует крошки для
любого адреса, а каркас карточки (`EntityPage`) — свои. На карточке это две
строки подряд: «Главная / Кампании» и «Кампании / Эстария». Так у Мастера
на всех шестнадцати карточках и у игрока в дневнике.

**Что сделано.** У игроцких страниц без каркаса (Дневники, Кабинет,
Персонажи, состояния ошибки дневника) дубль был слово в слово — своя строка
убрана, оставлена строка оболочки. Заодно оболочка научилась подписывать
`/sheets` («Персонажи», было «sheets»).

**Починено 2026-09-18** (решение владельца: остаётся строка карточки — в
ней имя, а в строке оболочки только разделы). Оболочка метит свою строку
`.shell-crumbs`, и на странице с `.entity-page` CSS прячет её след; значок
хранилища Мастера остаётся, у игрока строка уходит целиком.

**Откуда.** Проверка кабинета игрока глазами Лизы · 2026-09-18

### F-51 — Пустую вкладку дневника завести нельзя

**Что.** Вкладка игрока — это `folder_path` у записей: она появляется, когда
в неё переложили запись, и пропадает, когда последнюю вынули. Кнопки «новая
вкладка» нет — только пункт «Новая вкладка…» в выпадашке у существующей
записи. В пустом дневнике игроку не с чего начать раскладку.

**Почему заметно.** Решение 2 кабинета: «вкладки игрока — его папки»,
готово — «игрок заводит папку и раскладывает записи». Заводит он её только
задним числом.

**Решено 2026-09-18:** кнопка «+ вкладка» в полосе дневника, как «+» у
групп в каталогах. Где хранить пустую папку — модель данных, идёт через
разбор.

**Откуда.** Проверка кабинета игрока глазами Лизы · 2026-09-18

### F-52 — «От мастера» шире решённого, «Хроника мира» в двух вкладках

**Что.** Решение 3 кабинета: «От мастера» = `campaign_player_sections`
(записки, книги). Исполнитель свёл туда же всё, что кампания раскрывает
игроку по старому механизму: сессии, хронику кампании, статьи локаций и
НПЦ, раскрытые тайны. У Лизы во «От мастера» — «Хроника мира · 12» и
«Локации · 4», а в «Мире» — свои 2 локации. Хроника мира бывает и в «Мире»
(события календаря сеттинга по выдаче), и во «От мастера» (хроника
кампании) — одно имя у двух источников.

**Почему заметно.** Игрок ищет локацию в двух местах и не знает, почему она
здесь, а не там. Перенос всего старого был правильным — иначе раскрытое
пропало бы, — но граница между вкладками размылась.

**Починено 2026-09-18.** Граница по источнику (решение владельца): всё из
сеттинга — в «Мир», написанное Мастером по ходу кампании (сессии, тайны,
разделы) — в «От мастера». При разборе выяснилось, что посылка находки
была неверна: «Хроника мира» во «От мастера» — не хроника кампании, а те же
события календаря сеттинга, открытые старой галочкой «Видно игрокам», а
«Локации» — главы локаций сеттинга под той же галочкой. Переименовывать
было нечего: всё открытое галочкой перенесено в «Мир» и слито с выданным
(`buildSettingReaderGroups(setting, flagged)`, событие, открытое и так и
так, — один раз). Превью «Глазами игрока» галочку не знало вовсе и врало
игроку в меньшую сторону; теперь сервер отдаёт ему `flagged` той же
функцией `getFlaggedSettingContent`, что и `/player/campaigns/:id/visible`,
совпадение закреплено тестом.

**Откуда.** Проверка кабинета игрока глазами Лизы · 2026-09-18

### F-53 — Два механизма открытия игроку: галочка и выдача

**Что.** Сеттинг открывается игроку двумя путями. Выдача
(`player_visibility_grants`) — на кампанию и на игрока, со ступенями.
Галочка «Видно игрокам» (`visible_to_players` у глав локаций и существ и у
событий хроники; в живой базе 9 глав и 12 событий) — на саму запись
сеттинга, то есть сразу всем игрокам всех кампаний на этом сеттинге, без
ступеней. Галочка живёт в `ChapterList`, `LocationInfoTab`,
`SettingChronicleEventRow`, `EventDetailPage` и фильтре хроники сеттинга;
вкладка «Выдача» о ней не знает.

**Почему заметно.** Мастер, раздающий доступ в «Выдаче», не видит там
половины того, что игрок уже видит, и не может закрыть это одному игроку
или одной кампании. Две кампании на одном сеттинге откроют друг другу
главы, открытые для одной из них.

**Не чинилось:** оставить ли галочку, перевести ли её в выдачу (и кому
тогда выдать уже открытое) — модель данных, решение владельца, через
разбор.

**Откуда.** Починка F-52 · 2026-09-18
