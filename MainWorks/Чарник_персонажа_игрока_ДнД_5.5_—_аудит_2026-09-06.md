# Чарник персонажа игрока ДнД 5.5 — аудит с 4 сторон + план

Дата: 2026-09-06. Ветка `main`, репо `E:\Claude\01_Blender`.
Статус: **только аудит, кода не трогал, откатов через git не делал**. Жду обсуждения плана.
Правило: **НИКОГДА не восстанавливать через git без спроса перед КАЖДЫМ откатом.**

Источники: `design_revision.md` (инвариант §1–§5), `DESIGN.md`, `MainWorks/Карта_персонажа_—_спецификация_2026-09-04.md`, `MainWorks/Чарник_ДнД_5.5_—_план_доработки_2026-09-05.md`, код (`DndCharacterForm.tsx` ~8220 строк, `dnd-sheet.css` ~2718 строк, `StatblockList.tsx`, `CharacterSheetPage.tsx`, `CharacterDetailPage.tsx`, `DndCharacterWizard.tsx`, `server/.../signedUrl.ts`, `characters.ts`, `systems.ts`, `player.ts`).

Уточнение: в задаче сказано «по всем трём пунктам», но перспектив перечислено четыре — ниже все четыре (дизайн / код / пользователь / Мастер). Ранжирование сквозное: P0 — ломает игру/данные/систему, P1 — сильно мешает, P2 — полировка.

---

## P0 — критично (обсуждаем первым)

### Дизайн (перфекционист, по design_revision.md)
- **D-C1. Рамка карты 5px** — `dnd-sheet.css:891-893` `.dnd-card-frame{border-left/right:5px}`. Инвариант §1.3: только 1px везде. На 375px крадёт ширину у костей.
- **D-C2. Рубашка 5/2px + анимация** — `dnd-sheet.css:1655-1663` `.dnd-card-back`. 4 толщины на одном объекте.
- **D-C3. Кромка 3px = 4 смысла** — `dnd-sheet.css:1434,1993,2457`, `CharacterDetailPage.tsx:170` (закладка/трата/магический/ошибка одной формой). Нарушение §1.7 (тип — формой, формы не переиспользуются).
- **D-C4. Тень текстом на картуше** — `dnd-sheet.css:1055-1057` `text-shadow`. §1.2: теней нет.
- **D-C5. Кости 1.5/2/2.5/3.5px** — `dnd-sheet.css:1151-1171`. Один примитив — 4 толщины, §1.3.
- **D-C6. Круги-жетоны 2px** — `dnd-sheet.css:1105-1106,1514-1515,1546-1547` (`border-radius:50%; border:2px`). §1.1 (радиусов нет) + §4 (радиус только внешний угол карточки). Комментарий про §6.2 ссылается на несуществующий параграф эталона.
- **D-C7. Нет шапки-плашки на первой карте** — `DndCharacterForm.tsx:7405,7320-7324` (`sb-head` удалена). §1.4: шапка обязана быть инверсией.
- **D-C8. Секции — капс на черте** — `DndCharacterForm.tsx:1739,2124,2453`, `statblock.css:236-244,660-672` (комментарий «не вторая плашка» переопределяет §1.4).
- **D-C9/C10/C11. Моно не по назначению** — снаряжение-сводка (`DndCharacterForm.tsx:2454-2457`), закладки с `description` (`7592-7595` + `dnd-sheet.css:1458`), слово «спас» (`AbilitySavesSkills.tsx:159` + `dnd-sheet.css:1193`). §1.5: моно — только числа/даты/счётчики.
- **D-C12/C13. Капс длиннее строки** — картуш `origin` в 2 строки (`dnd-sheet.css:1073-1091`), «Характеристики и спасброски» на 375px (`AbilitySavesSkills.tsx:72`). §1.5.
- **D-C14. Тип цветом, не формой** — навыки `skill-source-*` (`dnd-sheet.css:509-525`, сознательное отступление в комментарии). На ч/б печати гаснет. §1.7.
- **D-C15. Бюджет акцента превышен** — `cardColor` сразу в 6–8 местах (`DndCharacterForm.tsx:7264,7298,7418,7449,4946,7579,7664`) + transfer-заливки + `crimson` счётчик. §1.8: один горячий объект ≤15%.
- **D-C16. Дуотон подменён** — `DndCharacterForm.tsx:7386,7363` + `dnd-sheet.css:982-1019` (`saturate/contrast/grayscale + grain + fade + ghost 7% зеркало`). §3 требует дуотон `--ink→--paper` + халфтон верхняя треть; портрет-содержимое трогать нельзя (§1.13).

### Код (шероховатости/уязвимости/перф)
- **K-P0-1. Потеря данных при размонтировании очереди** — `hooks/useQueuedSave.ts:~100-110`: `leftover` досылается `void ...catch(()=>{})` без ожидания. Навигация = тихая потеря правки.
- **K-P0-2. Гонка двух окон без версий** — `statblockPatch.ts:17-36` + `StatblockList.tsx:1055-1068` + `server/routes/statblocks.ts:274-294`: `topLevelPatch` поверхностный, `contentPatch`-путь без `baseUpdatedAt`/409. Last-writer-wins.
- **K-P0-3. Фолбэк-секрет signed URL** — `server/services/signedUrl.ts:12` `fallback-signed-url-secret`. Проверено грепом. Подделка 60-с URL на `/files`.
- **K-P0-4. Загрузка в RAM + доверие mimetype** — `server/routes/characters.ts:11-18`, `systems.ts:42-49` (+~12 роутеров): `memoryStorage` 15МБ, `fileFilter` по клиентскому `mimetype`, `ext` из `originalname`. DoS / неожиданные типы на диске.
- **K-P0-5. Миграция инвентаря неидемпотентна** — `CharacterDetailPage.tsx:634-662`: повторный клик дублирует, источник не чистится, без версии, мёртвая ветка `647-650`.
- **K-P1-1. Монолит 8220 строк** — `DndCharacterForm.tsx:230,6387` тянется ради `normalizeDndCharacter` в `CharacterDetailPage`, `StatblockList`, `EntityPreviewModal`. Бандл/HMR/tsc тяжелеют.
- **K-P1-2. Ключи по индексу** — `DndCharacterForm.tsx:700,685,689,4807` (`key={i/si/ii}`): удаление/перетаскивание = правка не той строки.
- **K-P1-3. Молчаливые catch → «пусто»** — `DndCharacterForm.tsx:682,5543,5646,5724,2081,5762`: вместо «ошибка сети» видят пустой список, ретрая нет.
- **K-P1-4. CORS private origins широко** — `server/index.ts:140-170`. Компенсируется JWT-не-в-cookie, но граница широкая.
- **K-P1-5. `/entries/batch` режет до 900 молча** — `server/routes/systems.ts:460-467` без `truncated:true`.
- Проверено как ОК: SQL параметризован (инъекций нет), `dangerouslySetInnerHTML` — 0 совпадений, права player/GM на reminders/inbox есть (`player.ts:347-385`, `playerAccess.ts`), пометка кампании первым читателем — осознанная семантика. `DndCharacterViewMini`/`ConditionsBox` живы (чистить нечего), `kind:"manual"` — только фолбэк ключа.

### Пользователь («КАК ЭТО НЕТ?!») + Мастер (игра завтра)
- **U-P0-1. Оборот-невидимка** — `DndCharacterForm.tsx:6466-6471,7663` + `DndCardBack.tsx`: тап только по треугольнику, онбординга нет, блик/30с по спеке. Игрок не замечает секретку.
- **U-P0-2. Трата в один тап без undo** — `SpendAction:4028-4118`, вдохновение `7445`, истощение `7528`: мгновенный `onQuickUpdate`, отмена только у корзины (8с). Промах = минус слот.
- **U-P0-3. Upcast не считает урон** — `DndCharacterForm.tsx:4041-4045` (честный комментарий). Кнопки кругов есть, подсказки `+1к6/круг` нет.
- **U-P0-4. На Карте нет поиска** — `3932-4006`, решение 4.2. Мастер за КЗ/ХП/скоростью поиска не находит; 8 табов на 375px влезает 6.
- **U-P0-5. Выход только внизу** — `8390` + решение 4.3 (липкую ← убрали). На длинном «Снаряжении» выход после скролла; свайп-назад конфликтует со свайпом карт `6527-6546`.
- **U-P0-6. Визард 375px до 5 уровня** — `DndCharacterWizard.tsx:47-57,881-1049`: 9 шагов, мишени ~24px, без пакетного выбора/прогресса/стиcky «Назад/Далее». Этап 6 ещё не сделан.
- **U-P0-7. Офлайна нет** — нет `serviceWorker/manifest/onLine`: вайфай лёг = ХП/ячейки не отмечаются.
- **U-P0-8. Печати нет** — `@media print` только правит слово-подпись; карта 9:16 на А4 не ложится. Телефон сел = персонажа нет.
- **U-P0-9. Истощение 6 = смерть в один тап** — `7528-7535` `(exhaustion+1)%7`, без confirm. Случайный тап = «смерть».
- **U-P0-10. Мастер не знает жестов** — `6520-6576`: свайп/дабл-тап-веер/тап-угол/клик-кость=сейв/`dropUp`. КЗ/ХП только на Карте, инициатива на «Действиях», кубы хитов на «Ресурсах». Хинт только внутри веера `6202`.

---

## P1 — сильно мешает (второй эшелон)

- **D-M1. 1.5/2px в статблоке** — `statblock.css:312,347,403,428,493,550,665,711,761` (эталон после 2026-08-30 — 1px везде).
- **D-M2. Внутренние радиусы от токена** — `dnd-sheet.css:58,218,431` + `statblock.css:76,152,203` (при пользовательском радиусе скругляется внутренность, §4 запрещает).
- **D-M3. Пустые блоки показывают пустоту** — `DndCharacterForm.tsx:1426,2022,2610,4216,5911`, `StatblockList.tsx:1487` («Пусто», «Мешок пуст»). §1.11: скрывать, исключение — главный блок с `EmptyState+кнопка`.
- **D-M4. Лента спутников висит в правке** — `DndCharacterForm.tsx:7607-7608` + `dnd-sheet.css:1491`.
- **D-M5. Кегль ×1.6 не держится** — `index.css:143-144` (10 vs 12 = ×1.2), `.dnd-die-value vs sub` (×1.4 в одном голосе), `sb-label 11 vs sb-prop-label 12` (§1.6).
- **D-M6. Хардкод-контрасты на портрете** — `dnd-sheet.css:997-1003,1115-1119,1556-1560` (в тёмных режимах шов/жетоны теряются).
- **D-M7. Полоска колоды 2px** — `dnd-sheet.css:906-942` + `DndCharacterForm.tsx:7290-7299` (вместо бесшовного язычка §5 `inset 0 -1px`).
- **K-P2-1. Нет виртуализации** — `slice(0,8/12/3)` без пагинации (`react-window` отсутствует).
- **K-P2-2. Нормализация дублируется** — `DndCharacterForm:230` vs `DndCreatureForm:203` без общего хелпера.
- **K-P2-3. `dnd-sheet.css` глобальный 2718 строк** — утечка `sb-*/dnd-*` на другие страницы.
- **U-P1-1. `?card=/?edit=1` — секретная правка** — `DndCharacterForm.tsx:6848-6851,6251`: карандашей нет сознательно, за столом опечатку не поправить.
- **U-P1-2. Порог 700px** — `useIsMobile.ts:7`: 701px планшет получает инлайн вместо фулскрина. Предложение: 760–800px или `pointer:coarse`.
- **U-P1-3. Спутники за `+` без аффорданса** — `3601` + `dropUp`; КЗ волка в тултипе нет.
- **U-P1-4. Монеты/инвентарь/досье закопаны** — монеты мелким моно, `Имущество` дублирует чарник (`CharacterDetailPage:601-737`), Досье в двух местах.
- **U-P1-5. Мёртвые ссылки внизу** — `6444-6458` + `deadLinks.ts`: бейджа `!N` в шапке нет, никто не доскроллит.
- **U-P1-6. Концентрация текстом «есть»** — `7508-7519`: СЛ видна только в `HpEditModal:5028`.
- **U-P1-7. Пикеры без «рекомендовано»/сортировки** — `4412-4650`: фильтр класс/круг есть, сортировки урон/ритуал нет.
- **U-P1-8. Пакетный выбор в листе, не в визарде** — лист `4505 isPicked`, визард `969-1004` по одному.
- **U-P1-9. Пустые состояния без CTA** — веер `6233 Пусто`, карты «Пусто» без кнопки в пикер (спека требует вход из пустого).
- **U-P1-10. Табы без стрелок, фокус теряется** — `DND_VIEW_TABS 6205`; `Esc` закрывает, фокус не возвращает.
- **U-P1-11. Мишени <44px, контраст на цвете класса** — звезда/булавка ~28px, `dnd-die-sub` 10px на заливке; проверить `textOnClassColor` на WCAG AA.
- **U-P1-12. Опасные молча, безопасные с confirm** — отдых спрашивает дважды (`DndRestModal:6050,6067`), удаление/вытеснение/смерть — молча. Инвертировать.

## P2 — полировка
- **D-m1. Две системы «число на кости»** — `dice-value Display` vs `dnd-die-value Mono`; вьюбоксы `46×53/60×69` vs `sbc__die 42px`.
- **D-m2. Жетоны трёх калибров** — 44/38/18px, обводки `ink` vs `line`.
- **D-m3. Поля 10px + отрицательные margins** — `dnd-sheet.css:897-900,1327-1331`; на 375px карта впритык (§1.9: внешние щедрые).
- **D-m4. Точка на табе — круг** — `statblock.css:1086-1094` 6px круг (§1.1).
- **D-m5. Десктоп-сплит 406px без max-width** — `dnd-sheet.css:2055-2097`; мера `78ch` только у `sbc__entry`.
- **U-P2-1. Reduced-motion наполовину** — блик гаснет, зеркало/веер/свайп — нет; опрос оборота `6647-6654` каждые 10с.
- **U-P2-2. Портрет 60с протухает в бою** — `6484-6497` + `CharacterSheetPage:refreshPortrait`. Нужен префетч + скелетон-герб.
- **U-P2-3. Закладки-авто молча меняются** — вытеснение 4-й молча; нужна подпись «Авто» + тост.
- **U-P2-4. Скорость одной цифрой** — в кости ходьба, полёт мелким; Мастер ищет «долетит?».
- **U-P2-5. RemindersWidget без контекста** — только на `?tab=statblock` у Мастера, без селекта адресата/превью оборота; из живой сессии не попасть.
- **U-P2-6. Дабл-тап воюет с iOS-зумом** — `6548-6576`; на карте без портрета тапать некуда; дублировать видимой кнопкой `DndFanButton`.

---

## План исправления (на обсуждение, без кода до аппрува)

**Волна 0 — безопасность/данные (сначала, малыми PR):**
0.1 `signedUrl` фолбэк: падать без секрета (или требовать `JWT_SECRET`), ротация. 0.2 Загрузка: `diskStorage` + проверка сигнатуры/расширения по содержимому (не `mimetype`), лимит параллелизма. 0.3 `useQueuedSave`: дождаться `leftover` + показать ошибку, не глотать. 0.4 Патчи: `baseUpdatedAt`/409 и для `contentPatch`, идемпотентная миграция инвентаря (флаг + очистка источника + confirm). 0.5 `/entries/batch`: `truncated:true` + бейдж `!N` в шапке.

**Волна 1 — не сорвать игру (UX-минимум):**
1.1 Оборот: подпись `Входящие · N` + `aria-live` + тур-подсказка + статус Мастеру (`доставлено/прочитано`). 1.2 Undo-тост 8с на трату/вдохновение/истощение (как у корзины); confirm на `5→6`/`6→0`. 1.3 Upcast-подсказка `Урон на N круге: X (база Y +Z)` или «не размечено». 1.4 Лупа и на Карте + `только колода/весь лист`. 1.5 Мини-`←` на всех картах кроме лицевой + хинт про свайп-вправо-назад. 1.6 Визард: прогресс `шаг N/9`, тапы 44px, sticky «Назад/Далее», «выбрать рекомендованное», пакетный `Добавить N`. 1.7 Пустые = CTA в пикер. 1.8 Печать А4-стопкой + офлайн-бейдж (оптимистик-минимум).

**Волна 2 — дизайн-система (привести к design_revision):**
2.1 Обводки 1px везде (карта/рубашка/кости/жетоны/полоска/статблок), убрать `text-shadow`, круги → квадраты (кроме аватаров/жетонов по §6.2 — решить с владельцем, что считать жетоном). 2.2 Шапки-плашки: вернуть `sb-head` на первой карте, секции — инверсией (снять локальный «не вторая плашка»). 2.3 Голоса: моно только числа (сводка/закладки/«спас» — в Body/Label), капс ≤ строки. 2.4 Акцент: один горячий объект (решить: цвет класса = координатная отметка, не акцент; transfer-цвета приглушить). 2.5 Дуотон `--ink→--paper` + халфтон верхняя треть, убрать глянец/зеркало 7% (или вынести в характер режима с тумблером). 2.6 Пустые скрывать (`EmptyState` + одна кнопка только в главном блоке), ленту спутников прятать. 2.7 Кегли ×1.6, поля щедрые, точка таба квадратом.

**Волна 3 — код-здоровье:**
3.1 Разбить `DndCharacterForm.tsx` (вынести `normalize` + типы в `.ts`, пикерам/модалкам — свои файлы; убрать импорт-монолит). 3.2 Ключи списков стабильные (id, не индекс). 3.3 Ошибки вслух + ретрай (как `entryCache`). 3.4 Виртуализация/пагинация пикеров. 3.5 Общий хелпер нормализации character/creature. 3.6 CSS-модули/скоуп для `dnd-sheet.css` (долгая цель).

**Волна 4 — Мастер/навигация:**
4.1 Строка-подсказка на Карте (`Инициатива → Действия, Кубы → Ресурсы`), десктоп-сплит и для планшета, счётчики в веере (монеты/спутники). 4.2 Видимый `✎`/`···` с правкой (`?edit=1`) + веер-кнопка всегда. 4.3 Порог 760–800px / `pointer:coarse` + баннер «Открыть как лист». 4.4 `RemindersWidget`: селект адресата + превью + кнопка из шапки листа и живой сессии. 4.5 Клавиатура (`←/→`, возврат фокуса, `:focus-visible`), мишени 44px, `role=checkbox` пипсам, `prefers-reduced-motion` глобально.

Проверка каждой волны: 375px + десктоп, панк + нуар, `tsc -p tsconfig.app.json --noEmit` + `oxlint` (база 0 ошибок), scratch 3003/5175 для пишущих проверок, живую базу не трогать.

Открытые вопросы владельцу (нужны до кода): 1) цвет класса — это «акцент» или «координата» (влияет на D-C15)? 2) Круги-жетоны оставляем как исключение §6.2 или уводим в квадрат? 3) Зеркало-портрет 7% — убрать или оставить как характер? 4) Порядок волн менять? 5) Макет `CardCanvas` подтягивать или пометить историческим?

---

## Отчёты о проделанной работе (дописывать сюда же)

- 2026-09-06: создан настоящий документ, аудит завершён, жду обсуждения плана. Кода не менял.
- …

