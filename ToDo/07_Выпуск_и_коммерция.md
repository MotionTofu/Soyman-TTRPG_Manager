# ToDo/07 — Выпуск и коммерция (хвосты из отчётов, 2026-09-12)

## Выписанное в первом проходе (screenw, SB-серия)

### П1.2 — Блокеры записи скринкастов: промпт в сборке и текст «О программе»

**Что.** Прежде чем писать ролики: (1) промпт/подсказка, упомянутая в
`03-импорт-системы.md:28`, должна быть в сборке; (2) текст «О программе»
должен быть утверждён.
**Где.** Сборка приложения, экран «О программе»
**Откуда.** `screenw/00-о-серии.md` §«Найденные проблемы… ДО записи» (2 шт.);
дубль: `screenw/03-импорт-системы.md:28` («Пока это не сделано…») · 2026-09
**Проверено сегодня.** `grep` маркеров по `screenw/` 2026-09-12 — в `03` строка
«Пока это не сделано» на месте, т.е. блокер жив; содержимое промпта и текст
«О программе» не проверялись
**Готово, когда.** Оба пункта закрыты и серия скринкастов разблокирована.

### П2.6 — Возврат каталога модулей только на текущем формате

**Что.** Каталог `soyman-modules` пуст (битые выгрузки убраны решением).
Возвращать — только на текущем формате экспорта плюс `minAppVersion/code`.
**Где.** `server/src/routes/systems.ts` (`buildSystemExportData`), витрина модулей
**Откуда.** `SideWorks/SB_modules.md` §«Что осталось» · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Каталог либо остаётся пустым по решению (зафиксировать),
либо возвращён с версионированным форматом.

## Проход 3 (2026-09-12)

### П2.16 — Docker + Caddy + Tailscale-инструкция

**Что.** Селфхост 24/7 — источник истины; один способ запуска: образ +
compose (volumes data/vault/config/backups), авто-HTTPS, Tailscale как
рекомендованный доступ.
**Где.** `deploy/`, инструкция
**Откуда.** `MainWorks/Архитектура_расчёты_селфхост_коммерция_—_план_2026-09-06.md` §§3–5 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Домашний сервер поднимается по инструкции и доступен
извне без открытых портов.

### П2.17 — Electron remote-mode + бэкапы с ретеншеном

**Что.** Electron — лучший клиент к домашнему серверу (настраиваемый URL) +
встроенный fallback без сети + «залить локальное»; плановые бэкапы
с проверенным restore.
**Где.** `electron/`, бэкапы
**Откуда.** `MainWorks/Архитектура_расчёты_селфхост_коммерция_—_план_2026-09-06.md` §3 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Стол без сети работает локально, возврат домой — одной
кнопкой, restore проверен.

### П2.18 — Коммерческий контур: демо, витрина, CSV, модули

**Что.** `dist:empty` — тупик пустоты, `Goods.md`/лендинг не связаны
с приложением, финансы не выгружаются, каталог модулей вне приложения.
**Где.** `package.json` (dist), `AboutPage`, `routes/finance.ts`
**Откуда.** `MainWorks/Аудит_4_перспективы.md` фазы 4 (B-01–B-04) · 2026-09-02
**Проверено сегодня.** не проверялось
**Готово, когда.** Покупатель видит ценность, мастер выгружает деньги в CSV.

### П3.1 — Монетизация = один Boosty без сторителлинга

**Что.** Кнопка доната без объяснения «за что плачу»; финансов нет экспорта
для платного Мастера.
**Где.** `client/src/layout/AppShell.tsx` (`BOOSTY_URL`), `Goods.md`, `landing/`
**Откуда.** `SideWorks/Аудит_4_перспективы.md` §1 B-01old · 2026-09-02
**Проверено сегодня.** не проверялось
**Готово, когда.** После гриллинга: витрина/экспорт финансов или
зафиксировано «не делаем».

## Личная перепроверка 2026-09-12

### П2.19 — Сборка легче и честнее (O-4)

**Что.** `adm-zip` + `archiver` дублируют либу (+80 MB portable);
`tsc -b && vite build` гоняет `tsc` дважды; монолит `index-*.js 345 kB`.
**Где.** `server/package.json:12`, `client/package.json:7`
**Откуда.** `SideWorks/Настройки.md` O-4 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** Одна zip-либа, `splitChunks`, `tsc` один раз.

### П2.20 — Замер production-сборки (C-11)

**Что.** Все числа ревизий сняты на dev-сборке; разрыв с production
не проверялся.
**Где.** `design_revision`, сборка
**Откуда.** `SideWorks/Всё_приложение.md` C-11 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** Числа подтверждены на production-билде.
