# ToDo/06 — Сервер и данные (хвосты из отчётов, 2026-09-12)

## Выписанное в первом проходе (SB-серия)

### П1.1 — Панели вне `LoginGate` при протухшем токене

**Что.** Токен синхронизируется между окнами через `storage` + reload,
но панели, живущие вне `LoginGate`, при 401 остаются с протухшим токеном.
**Где.** `client/src/api/client.ts` (`TOKEN_KEY`, `isBusyEditing`)
**Откуда.** `SideWorks/SB_token.md` §«Что осталось» (баннер/панели) · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Протухший токен в любой панели ведёт к понятному
состоянию (перевход или баннер), а не к тихим 401.

### П2.5 — Правило «не гонять `INSERT OR IGNORE` в AUTOINCREMENT на старте»

**Что.** Сид `systems` двигал `sqlite_sequence` даже при игноре дубликата
(4× за перезапуск); починено флагом и проверкой `SELECT` перед `INSERT`.
Проверить прочие сиды на тот же паттерн.
**Где.** `server/src/db/db.ts` (сиды, `default_systems_seeded`)
**Откуда.** `SideWorks/SB_seq.md` §«Что осталось» · 2026-08-30
**Проверено сегодня.** не проверялось
**Готово, когда.** Все стартовые сиды идут через «проверка → вставка»,
повторный старт не двигает `sqlite_sequence`.

## Проход 2 (настройки, здоровье, Canvas, LitM, 2026-09-12)

### П3.7 — Сброс и экспорт/импорт настроек «Внешний вид»

**Что.** Накрученные радиус/дуотон/тамбнейлы не вернуть к дефолту одним
действием; нет экспорта настроек (темы, мешок, navWidget) для переноса между
хранилищами.
**Где.** —
**Откуда.** `SideWork/Nastroiki.md` §«Крик души» п.7 · 2026-08-30
**Проверено сегодня.** не проверялось
**Готово, когда.** Кнопки «Сбросить к дефолту» и «Экспорт/Импорт настроек»
работают, дефолт радиуса возвращается.

### П1.11 — Прогресс и валидация импорта бэкапа до отправки

**Что.** Импорт zip 500 МБ висит без прогресса; перетаскивание app.db даёт
сырой 400 без подсказки; проверки «папка пустая» до POST нет.
**Где.** `StoragesSettingsPage.tsx:218-239`, `server/src/index.ts:142`
**Откуда.** `SideWork/Nastroiki.md` §«Крик души» п.5 + §Г · 2026-08-30
**Проверено сегодня.** не проверялось
**Готово, когда.** Валидация типа файла до отправки, виден % прогресса,
ошибка читается по-русски.

### П2.13 — В «Здоровье» перенести битые ссылки и отчёт о модулях

**Что.** `HealthPage` MVP показывает сирот/seq/пути, но
`GET /links/broken`/`dangling` и отчёт «каких модулей не хватает» туда
не переехали.
**Где.** `server/src/routes/health.ts`, `client/src/pages/HealthPage.tsx`
**Откуда.** `SideWork/Done.md` §П1.1 · 2026-08-30
**Проверено сегодня.** не проверялось
**Готово, когда.** Раздел показывает битые ссылки и недостающие модули
с действиями.

### П1.12 — Вкладка до входа молча получает 401 на записи

**Что.** Токен читается один раз при загрузке модуля; во вкладке, открытой
до входа в другом окне, запись отвечает 401 без сообщения.
**Где.** `client/src/api/client.ts:8`
**Откуда.** `Canvas/02_CanRev_Rep.md` Н1 · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Старая вкладка либо подхватывает токен, либо честно
показывает вход при 401.

### П1.13 — У свободной доски id и scope_id всегда равны

**Что.** Две колонки живут как разные, код адресует ими вперемешку;
совпадение маскирует ошибки до первого нестандартного создания
(засев, импорт).
**Где.** `server/src/routes/canvas.ts` (POST `/canvas/free-boards`)
**Откуда.** `Canvas/02_CanRev_Rep.md` Н11 · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Одна колонка-истина, все маршруты и ссылки `?free_id=`
работают.

### П2.14 — Кодовые хвосты LitM: seed и childKinds

**Что.** `seedMechanicsGroups` сыплет днд-группы не-DnD системам;
`KIND_DEFS.childKinds` не разрешает тип-под-ступень и тембук-под-тип
из нового дерева.
**Где.** `seedMechanicsGroups`, `compendium.ts` (`KIND_DEFS`)
**Откуда.** `LitmSys.md` «Следующие шаги» п.6 + `LitM/LegendsRework.md` гл.3
**Проверено сегодня.** не проверялось
**Готово, когда.** Сид не трогает чужие системы, дочерние записи создаются
из дерева.

### П2.15 — Экспорт модуля litm в каталог

**Что.** JSON-модуль собирается и заливается локально через
`applyLitmModule`/`importFile`, но в каталог `soyman-modules` не выгружен —
раздачи нет.
**Где.** `server/data/litm-module.json`, `server/src/routes/systems.ts`
**Откуда.** `LitmSys.md` «Открытые шаги» п.5 + `LitM/LegendsRework.md`
**Проверено сегодня.** не проверялось
**Готово, когда.** Модуль code litm выгружен и ставится из каталога штатным
импортёром.

## Проход 3 (SideWorks, DnD-аудит, архитектура, карты, 2026-09-12)

### П0.1 — Скан здоровья вешает event loop

**Что.** `/scan` делает синхронные обходы 2–8 сек. Все `/api` висят, клиент
ловит таймаут 10с.
**Где.** `server/src/routes/health.ts:50/562/588/624`
**Откуда.** `SideWorks/Здоровье.md` C-P0-1 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** Обходы асинхронны с лимитом, timeout 30с и Abort на клиенте.

### П0.2 — `/auth/players` отдаёт чужие username в клиент

**Что.** Страница тянет всех игроков и ищет `find(player_id)`; username всех
видны в Network/DevTools.
**Где.** `server/src/routes/auth.ts` (`/auth/players`)
**Откуда.** `SideWorks/Профиль Игрока.md` §II C-P0-2 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** Клиент получает только целевого игрока; массовый список закрыт.

### П0.3 — Аплоад аватара игрока без whitelist и лимита

**Что.** Расширение из `originalname`, без MIME-списка и лимита; SVG/HTML →
stored-XSS, большой файл → OOM.
**Где.** `server/src/routes/players.ts` (avatar/thumbnail)
**Откуда.** `SideWorks/ProfilIgroka.md` §1.1 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** Белый список типов, лимит размера, ресайз.

### П0.4 — Потеря очереди и гонка двух окон без версий

**Что.** `leftover` досылается без ожидания (навигация = тихая потеря),
`contentPatch` без `baseUpdatedAt`/409 — last-writer-wins.
**Где.** `hooks/useQueuedSave.ts`, `statblockPatch.ts`, `routes/statblocks.ts`
**Откуда.** `MainWorks/Чарник_персонажа_игрока_ДнД_5.5_—_аудит_2026-09-06.md` K-P0-1/2 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Правка не теряется при навигации, конфликт виден.

### П0.5 — Фолбэк-секрет URL и загрузка в RAM по mimetype

**Что.** Подделка 60-с URL на `/files` при фолбэк-секрете; `memoryStorage`
15МБ + `fileFilter` по клиентскому mimetype.
**Где.** `server/services/signedUrl.ts`, `routes/characters.ts`, `systems.ts`
**Откуда.** `MainWorks/Чарник_персонажа_игрока_ДнД_5.5_—_аудит_2026-09-06.md` K-P0-3/4 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Без секрета сервер падает, чужой файл не ложится на диск.

### П0.6 — Неидемпотентная миграция инвентаря + молчаливый batch

**Что.** Повторный клик дублирует инвентарь; `/entries/batch` режет до 900
молча.
**Где.** `CharacterDetailPage.tsx`, `server/routes/systems.ts`
**Откуда.** `MainWorks/Чарник_персонажа_игрока_ДнД_5.5_—_аудит_2026-09-06.md` K-P0-5 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Повтор не дублирует, обрезка видна клиенту.

### П0.7 — Фундамент синка: uid, updated_at, sync_log, пути

**Что.** Везде `AUTOINCREMENT`, uid частично, пути с `\` на Linux не встанут.
Пока окно ломающих изменений открыто.
**Где.** Схема БД, `server/src/db/`, `vaultPaths`
**Откуда.** `MainWorks/Архитектура_расчёты_селфхост_коммерция_—_план_2026-09-06.md` §3 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Все синкабельные сущности по uid, бэкап Windows
восстанавливается на Linux.

### П0.8 — Стрип секретов чистит только blob v3

**Что.** Клиент пишет v4 — игроки получают секретные двери/ловушки открытым
текстом; тест покрывает только v3.
**Где.** Сервер `GET /maps/:id`, `stripCellsForPlayer`
**Откуда.** `MainWorks/Maps/MapsAudition_06.md` P0-1 · 2026-09
**Проверено сегодня.** не проверялось
**Готово, когда.** Стрип по наличию полей (v3+v4); тест v4 зелёный.

### П0.9 — Жёсткое удаление карты + DELETE отдаёт ok:true вместо 404

**Что.** Удаление одним confirm, каскад привязок, undo-тост отсутствует;
DELETE несуществующего отдаёт `ok:true`.
**Где.** `DELETE /maps/:id`, список карт и редактор
**Откуда.** `MainWorks/Maps/MapsAudition_06.md` P0-3 + P2-9 · 2026-09
**Проверено сегодня.** не проверялось
**Готово, когда.** Минимум 404 на несуществующий id; дальше — решение (Р32).

### П0.10 — Сломан tsc в чужом месте (dndFeatures)

**Что.** `dndFeatures.ts` импортирует неэкспортированные типы — блокирует
build целиком, к картам не относится.
**Где.** `client/src/components/dnd/`, сборка
**Откуда.** `MainWorks/Maps/Находки.md` тикет 02 · 2026-09
**Проверено сегодня.** не проверялось
**Готово, когда.** Отдельный заход; проверка карт — прогоном через run.

### П1.14 — Rate limit только на auth

**Что.** Write-endpoints без лимита; спам POST кладёт синхронный event loop.
**Где.** `server/src/index.ts`
**Откуда.** `SideWorks/Всё_приложение.md` C-03 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** Лимит на записи, спам отвечает 429.

### П1.15 — LIKE не экранирует % и _

**Что.** Поиск с `%/_` становится wildcard, «100%» находит «100500».
**Где.** `server/src/routes/search.ts:45`, `settingBeings.ts:198-199`
**Откуда.** `SideWorks/Всё_приложение.md` C-04 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** `escapeLike` с `ESCAPE '\'` во всех LIKE-запросах.

### П1.16 — Проверки роли только в гейте, внутри maps-роутов нет

**Что.** Запись режет только глобальный `apiRoleGate`; перестановка
монтирования откроет игрокам запись.
**Где.** Серверные роуты maps POST/PUT/DELETE включая bindings
**Откуда.** `MainWorks/Maps/MapsAudition_06.md` P1-5 · 2026-09
**Проверено сегодня.** не проверялось
**Готово, когда.** Пояс-защита в мутирующих роутах; регресс-тест зелёный.

### П2.16 — Пагинация мёртвых UID режет 51+ групп

**Что.** `slice(0,50)` тихо, rows slice 500 без ORDER BY — часть групп
потеряется при росте.
**Где.** `server/src/routes/health.ts:521/469`
**Откуда.** `SideWorks/Мёртвые UID — ручная проверка.md` §9 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** Пагинация или предупреждение, срез после ORDER BY.

### П2.17 — Ререндер 33 групп на каждый выбор

**Что.** `setDeadChoices` новым объектом перерисовывает все селекты.
**Где.** `client/src/pages/HealthPage.tsx:468-469`
**Откуда.** `SideWorks/Мёртвые UID — ручная проверка.md` §9 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** Рендер только изменённой группы.

### П2.18 — Ancestors циклом N+1 вместо CTE

**Что.** `GET /:id` собирает ancestors while-циклом по SELECT на уровень.
**Где.** `server/src/routes/settingLocations.ts:162-171`
**Откуда.** `SideWorks/Важные_даты_профиля_локации.md` P-P1-2 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** Один `WITH RECURSIVE ancestors` вместо цикла.

### П2.19 — Refresh тянет всю географию зря

**Что.** После add/delete дат refresh тянет все локации сеттинга; хук
календаря без Abort.
**Где.** `LocationDetailPage.tsx:125-145`, `hooks/useSettingCalendar.ts`
**Откуда.** `SideWorks/Важные_даты_профиля_локации.md` P-P1-3/4 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** Инвалидируются только даты, календарь с AbortController.

### П2.21 — Смена сортировки бестиария ~1.6с

**Что.** Пересчёт всего без мемоизации/виртуализации на 535 записях.
**Где.** `client/src/components/CompendiumSection.tsx`
**Откуда.** `SideWorks/Аудит_4_перспективы.md` §1 C-03old · 2026-09-02
**Проверено сегодня.** не проверялось
**Готово, когда.** Смена сортировки без секундной просадки.

### П2.22 — Нет `short_name`-индекса для поиска существ

**Что.** Поиск по `name_original`/алиасам идёт O(n) хайлайтом без индекса.
**Где.** `server/src/db/schema.sql` (`setting_beings`)
**Откуда.** `SideWorks/Аудит_4_перспективы.md` §1 C-04old · 2026-09-02
**Проверено сегодня.** не проверялось
**Готово, когда.** Поиск существ индексирован.

### П2.23 — Архивные статблоки копятся без пути очистки

**Что.** Мягкое удаление оставляет строки и портреты навсегда.
**Где.** `server/src/routes/statblocks.ts`, экран Архива
**Откуда.** `SideWorks/Статблок_персонажа_DnD_5.5.md` §7 · 2026-09-03
**Проверено сегодня.** не проверялось
**Готово, когда.** Есть путь окончательного удаления архивных статблоков.

### П2.24 — Монолит формы, ключи по индексу, молчаливые catch

**Что.** 8220 строк тянутся ради normalize, ключи `key={i}` ломают правку,
catch рисуют «пусто» без ретрая.
**Где.** `client/src/components/dnd/DndCharacterForm.tsx`
**Откуда.** `MainWorks/Чарник_персонажа_игрока_ДнД_5.5_—_аудит_2026-09-06.md` K-P1 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Форма разбита, сеть не выглядит пустотой.

### П2.25 — Виртуализация, общий normalize, скоуп CSS

**Что.** Пикеры режут `slice(0,8/12/3)` без пагинации, нормализация
дублируется, 2718 строк глобального CSS текут на другие страницы.
**Где.** Пикеры, `DndCreatureForm`, `dnd-sheet.css`
**Откуда.** `MainWorks/Чарник_персонажа_игрока_ДнД_5.5_—_аудит_2026-09-06.md` K-P2 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Тысячи записей не тормозят лист, стили не утекают.

### П2.26 — Правила расчётов: общий пакет + сервер-авторитет денег

**Что.** Три клона `per_table/per_month` врут о долгах; `proficiencyBonus`/
`armorClass` хранятся рядом с вычислением.
**Где.** `packages/rules/`, `services/finance.ts`, статблоки
**Откуда.** `MainWorks/Архитектура_расчёты_селфхост_коммерция_—_план_2026-09-06.md` §§1–2 · 2026-09-06
**Проверено сегодня.** не проверялось
**Готово, когда.** Клиент и сервер считают одним кодом.

### П2.27 — Честный slice и подсветка поиска

**Что.** `slice 40/8` молча режет, подсветки нет, сортировка строковая.
**Где.** Бестиарий, справочник, палитра холста
**Откуда.** `MainWorks/MechaStorm.md` §4 · 2026-09-05
**Проверено сегодня.** не проверялось
**Готово, когда.** Мастер знает об обрезке и видит совпадения.

### П2.28 — oxlint rules-of-hooks в чужих файлах

**Что.** Ошибки хук-под-условием роняли раздел тем же классом дефекта.
Починить в рамках этапов, не откладывать.
**Где.** `components/InworldCalendar.tsx`, `components/litm/ThemeCard.tsx`
**Откуда.** `MainWorks/Аудит_4_перспективы.md` §7 · 2026-09-02
**Проверено сегодня.** не проверялось
**Готово, когда.** Линтер чист от ошибок hooks.

### П2.29 — Долги структуры клиента карт

**Что.** God-компонент ~4040 строк; дубли кроп/трим, рёбра дверей,
create-vs-place; инлайн-стили; разбросанные ключи localStorage; хоткеи дважды.
**Где.** Клиент слоя карт, стили, префы, реестр хоткеев
**Откуда.** `MainWorks/Maps/MapsAudition_06.md` P2-1–P2-5 · 2026-09
**Проверено сегодня.** не проверялось
**Готово, когда.** По одному выносу за заход, тесты зелёные.

### П2.30 — Перфоманс карт: PNG, выбор, тесты, сид

**Что.** PNG морозит вкладку на 100×100; выбор сбрасывается при каждом
изменении; клиентских тестов не видно; seed без диапазона.
**Где.** Экспорт PNG, выбор объектов, grid/generate/dungeon
**Откуда.** `MainWorks/Maps/MapsAudition_06.md` P2-6–P2-9 · 2026-09
**Проверено сегодня.** не проверялось
**Готово, когда.** Чанкинг/воркер, сброс выбора только при смене карты,
roundtrip-тесты и детерминизм по сиду.

## Личная перепроверка: Настройки.md прочитан целиком 2026-09-12

Фазы 0–1 закрыты по журналу (отброшено: P0-1…P0-6, C-P1-1/7, D-P1-1/2/6/8,
жанр-пикер, дуотон 0.13, пустота-497). Открыты Фазы 2–4 и Остаточное O-1…O-6.
Крошки — дубль П1.1 (`ToDo/01`), не завожу.

### П0.11 — multer до конца на диск (O-1)

**Что.** `settings.ts:43` (background/thumbnail), `gallery.ts:13` (image),
`resources.ts` — ещё `memoryStorage() 15 MB`. 8 параллельных загрузок —
120 MB в RAM.
**Где.** `routes/settings.ts:43`, `gallery.ts:13`, `resources.ts`
**Откуда.** `SideWorks/Настройки.md` O-1 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** `diskStorage(tmpdir)` + `cleanupFile` везде, `rg memoryStorage` пуст.

### П0.12 — Миграции в транзакции + integrity_check (C-P1-3)

**Что.** `ALTER TABLE` по одному `exec`, `foreign_keys=OFF` без
`BEGIN IMMEDIATE`. Отключение света между `DROP` и `RENAME` — база без таблицы.
**Где.** `server/src/db/db.ts:340-451`
**Откуда.** `SideWorks/Настройки.md` C-P1-3 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** Весь `openDatabase` в транзакции, `integrity_check` на старте.

### П1.17 — JWT в localStorage + half-CSP (C-P1-4/5)

**Что.** Токен без `HttpOnly` — XSS=захват на 7 дней. `helmet` есть, полного
CSP нет (только `X-Content-Type-Options` на `/files`).
**Где.** `client/src/api/client.ts:8`, `server/src/index.ts:214`
**Откуда.** `SideWorks/Настройки.md` C-P1-4/C-P1-5 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** HttpOnly-хранилище токена, CSP закрывает inline.

### П2.32 — Батч gallery/orphans + вайтлист таблиц (O-2)

**Что.** `gallery.ts:52` цикл `SELECT ... WHERE id=?` по `owner_id`;
`orphans.ts:98` `${table}` без вайтлиста. Галерея 200 картинок — 200 запросов.
**Где.** `routes/gallery.ts:52`, `services/orphans.ts:98`, `vaultPaths.ts:11`
**Откуда.** `SideWorks/Настройки.md` O-2 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** `WHERE id IN (...)`, `assertAllowedTable` везде.

### П2.33 — `?token=` в URL до конца (O-3)

**Что.** `fetch+blob` работает для плиток, но `<audio>` и часть `<img>` ещё
на `withFileTokens ?token=` — токен в истории/логах/Referer.
**Где.** `utils/fileUrl.ts:45`, `api/client.ts:59`, `index.ts:198`
**Откуда.** `SideWorks/Настройки.md` O-3 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** Нигде `?token=`: `blob:` + signed URL HMAC TTL 60s fallback.

### П2.34 — Тесты auth/storages/files + audit в CI (O-5)

**Что.** `vitest`/`playwright` — 0. Сломанный `auth` узнаётся от игрока.
**Где.** CI, `auth`, `storages`, `files`
**Откуда.** `SideWorks/Настройки.md` O-5 · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** 3 теста зелёных (setup/login 429, ZipSlip 400, relink 403),
`npm audit` на каждый PR.

### П2.35 — Гигиена кода P2: storage, Abort, any, zod, логи

**Что.** `localStorage` без `try/catch` (Safari private крашит рендер);
`addEventListener` без remove; половина запросов без Abort; 132 `any`;
`console.log` в проде; 60% роутов без `zod`; дубли `isSafeFolderPath`.
**Где.** `CompendiumSection.tsx:430`, `CanvasPage.tsx`, маршруты
**Откуда.** `SideWorks/Настройки.md` P2-код · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** Хелпер `safeGetItem`, Abort везде, `any` тают, один валидатор путей.

### П2.36 — Схема: FK-транзакции, version tracking, лимиты длины (C-08–C-10)

**Что.** Rebuild таблиц с `foreign_keys=OFF` без транзакций; у миграций нет
версий — не аудитить запуски; текстовые поля без лимитов длины на сервере.
**Где.** `server/src/db/db.ts:427,458`, маршруты CRUD
**Откуда.** `SideWorks/Всё_приложение.md` C-08–C-10 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** Миграции версионированы и транзакционны, поля с лимитами.

### П2.31 — Приёмка ручной проверки UID (8 критериев)

**Что.** Чек-лист инструмента: вёрстка 1920/390, бейдж `doubtful`, backdrop/
Esc, поиск вне топ-10, развёрнутый список с `hostRoute`, «Применить»
с бэкапом, `/scan` < 200ms.
**Где.** `HealthPage.tsx`, `routes/health.ts`
**Откуда.** `SideWorks/Мёртвые UID — ручная проверка.md` · 2026-08-31
**Проверено сегодня.** не проверялось
**Готово, когда.** Критерии пройдены, боксы проставлены.

### П2.37 — CORS private origins широко (K-P1-4)

**Что.** `server/index.ts:140-170` — граница широкая; компенсируется
JWT-не-в-cookie, но сузить стоит.
**Где.** `server/src/index.ts:140-170`
**Откуда.** `MainWorks/Чарник_персонажа_игрока_ДнД_5.5_—_аудит_2026-09-06.md` K-P1-4 · 2026-09-06
**Проверено сегодня.** не проверялось (файл вычитан целиком 2026-09-12)
**Готово, когда.** Граница сужена, мобильные клиенты не сломаны.

### П2.38 — Поиск: классы записей вне охвата (U-04/Фаза 2.3)

**Что.** Поиск не видит `important_dates`, `gm_reminders`, события
календарей, `world_exploration_entries`, `link_notes`, подписи галереи,
`statblocks`. Написанное Мастером не находится.
**Где.** `server/src/routes/search.ts`
**Откуда.** `MainWorks/Аудит_4_перспективы.md` Фаза 2.3 · 2026-09-02
**Проверено сегодня.** не проверялось (файл вычитан целиком 2026-09-12)
**Готово, когда.** Написанное Мастером находится всегда.

### П2.39 — Техдолг Фазы 5: god-файлы, версия (C-01/C-03)

**Что.** `index.css` 7797 строк, `CanvasPage` 8013, `DndCharacterForm` 4486 —
на модули; версия в `package.json` проставляется вручную.
**Где.** CSS, god-компоненты, `package.json:4`
**Откуда.** `MainWorks/Аудит_4_перспективы.md` Фаза 5 · 2026-09-02
**Проверено сегодня.** не проверялось
**Готово, когда.** Модули, автоподстановка версии при сборке.

### П2.40 — Мелочи §5: prompt, дубли, search, темы (Аудит_4)

**Что.** `AppShell:190` резервный ввод пути через `window.prompt`;
`LibraryPage` дублирует списки read-only; `search.ts` не покрывает
`statblocks`; семантика `dark`/`light` мимо режимов.
**Где.** `AppShell.tsx:190`, `LibraryPage`, `search.ts`, `themes.ts:79-80`
**Откуда.** `MainWorks/Аудит_4_перспективы.md` §5 · 2026-09-02
**Проверено сегодня.** не проверялось
**Готово, когда.** Промпт убран, дубли сняты, поиск видит статблоки.

### П0.13 — SELECT * отдаёт игроку лишнее (Фаза 0.2 Плана)

**Что.** Игрок не должен видеть `statblock_full`, `behavior`, `tags`,
`location_id`, `created_at` — сейчас `SELECT *`.
**Где.** `server/src/routes/player.ts:550-561`
**Откуда.** `SideWorks/Профиль_Сеттинга_План.md` Фаза 0.2 · 2026-09-01
**Проверено сегодня.** не проверялось (файл вычитан целиком 2026-09-12,
статус «На обсуждении»)
**Готово, когда.** Только минимальные поля по типам сущностей.

### П2.41 — N+1 grants видимости → один запрос (Фаза 0.3)

**Что.** Цикл по кампаниям за `player_visibility_grants` — N запросов.
**Где.** `server/src/routes/player.ts:535-546`
**Откуда.** `SideWorks/Профиль_Сеттинга_План.md` Фаза 0.3 · 2026-09-01
**Проверено сегодня.** не проверялось
**Готово, когда.** Один `SELECT DISTINCT ... WHERE campaign_id IN (...)`.

### П2.42 — Поиск хранилищ, папка, превью переключения (Крик 1/3/4)

**Что.** 4–6 хранилищ без поиска; путь текстом без «Открыть в проводнике»;
переключение вслепую (без кампаний/размера/даты).
**Где.** `StoragesSettingsPage.tsx:141,155,175,71`
**Откуда.** `SideWork/Nastroiki.md` Крик 1/3/4 · 2026-08-30
**Проверено сегодня.** не проверялось (файл вычитан целиком 2026-09-12)
**Готово, когда.** Поиск, «Открыть», превью содержимого до переключения.

### П2.43 — Сеттинги Фаза 3: мемоизация, Abort, валидация, токены

**Что.** `filteredSettings` без useMemo; `refresh` без Abort; контроллеры
плодятся; визард без валидации; роуты без zod; genre-hex мимо тем;
`isSafeImageUrl` не покрывает blob/data.
**Где.** `SettingsListPage.tsx`, `SettingDetailPage.tsx`, `SettingWizard.tsx`,
`routes/settings.ts`, `genreData.ts`
**Откуда.** `SideWorks/Сеттинги.md` Фаза 3 (18–24) · 2026-08-31
**Проверено сегодня.** не проверялось (файл вычитан целиком 2026-09-12)
**Готово, когда.** Мемо, Abort везде, визард валидирует, токены тем.
