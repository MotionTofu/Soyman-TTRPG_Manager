# Вердикт об актуальности ToDo/ (2026-09-12, код на main `c66f973`)

Основание: поручение сверить содержимое шапки `ToDo/` с актуальным кодом и
вынести вердикт, какие пункты уже не актуальны (решены / решены иначе /
утратили ценность).

Методика: каждый из 322 пунктов `### ...` в файлах 01–09 сверен с кодом на
текущем `main` (четыре независимые сверки + точечная перепроверка 7 заявлений
вручную по коду: `DndCardBack.tsx`, `withFileTokens`, `parent_key`,
`source_event_id`, `minAppVersion/tooOldFor`, `useUndoDelete`, `btn-capsule` —
все 7 подтвердились). Отчётам на слово не верили — только код.
Оговорка: вердикты ниже — с указанием доказательств, но перед удалением любого
пункта из `ToDo/` его стоит перепроверить вручную.

## Сводка

- Всего пунктов: 322 (01:33, 02:67, 03:42, 04:55, 05:9, 06:53, 07:8, 08:46, 09:9).
- Актуальны (дыра/долг жив в коде): 202.
- Неактуальны (решены / решены иначе / утратили ценность): 52 — раздел 2.
- Не проверяемо статикой (рантайм, БД владельца, ответ владельца): 68 — раздел 3.
- 52 + 68 + 202 = 322, сходится.

## 1. Вердикт по самой шапке (`ToDo/README.md`)

- Строка 3 «разборка ~175 рабочих документов» — УСТАРЕЛО. Факт области: 142
  (175 включало ~45 двойников `.claude/worktrees/`, исключения и серии,
  посчитанные как один объект). Исправить на 142.
- Строки 32–33 «каркас заведён до первого перемещения; файлы 01–09 создаются
  только непустыми по мере находок» — УСТАРЕЛО. Все 9 файлов созданы и непусты
  (от 8 до 67 пунктов). Строка описывает прошлое состояние.
- Строки 10–18 (состав 01–09) — АКТУАЛЬНО: все файлы на месте, непусты. Но
  список станет неполон с появлением этого файла вердикта — добавить строкой.
- Строки 20–22 (приоритеты П0–П3) — АКТУАЛЬНО: дословно совпадает
  с `ToDo.md:12–15`.
- Строка 5 «каталог не заменяет `ToDo.md`» — АКТУАЛЬНО: корневой `ToDo.md`
  (548 строк) на месте, последние правки — старые коммиты, разборка его
  не трогала.
- Строки 24–26 (формат + «Откуда» обязательна) — ЧАСТИЧНО. «Откуда» есть у
  100% проверенных пунктов. Но заголовки вида `### ПX.N` нарушены:
  файл 08 штатно ведёт Р-серию, файл 09 — Х-серию (задумано интро файлов,
  но формату шапки не соответствует); плюс чужеродные заголовки с буквенными
  суффиксами — `П1.к1`, `П2.о3` (01), `П2.р1`, `П2.р2` (02), `Р-цвет` (08),
  `Х2 (выяснено)` (09) — следы параллельной правки.
- Строка 28 «дубликаты пишутся один раз» — НАРУШЕНО. Одни номера с разным
  смыслом живут в двух файлах: П1.2, П2.6, П3.1–П3.3 (01↔07); П1.11–П1.13,
  П2.13–П2.15, П2.18–П2.19, П1.17, П2.28–П2.33 (04↔06).
- Строки 29–30 (отброшенное — с записью в манифест) — СОБЛЮДЕНО:
  в `Archive/2026-09-12/MANIFEST.md` 3 записи «отброшено».
- Прочее по нумерации: в 01 пропущен П2.9 (прыжок П2.8→П2.10); в 03 порядок
  не монотонен (П2.13 перед П2.1; П2.15/П2.19–П2.24/П2.14 после П3.5);
  в 08 дыра Р38/Р39 (после Р33 идёт Р40), блок Р34–Р37 стоит после Р47.

## 2. Неактуальные пункты (52)

Классы: РЕШЁН — код уже делает требуемое; РЕШЁН ИНАЧЕ — проблема закрыта
другим решением; УТРАТИЛ ЦЕННОСТЬ — контекст исчез. Класса «утратил ценность»
нет ни у одного пункта: ни одна фича/экран из хвостов не удалены.

### 2.1. Файл 01 + 07 (18)

- 01-П2.2 — РЕШЁН. Панель в `client/src/audio-player.css`: радиус 0 (:16),
  края/высота переменными (:3–6), разделитель `var(--line)` (:95).
- 01-П2.3 — РЕШЁН. Метка на `var(--muted)` (`canvas.css:341`); muted ≈4.8:1
  (`themes.ts:183–186,414–415`).
- 01-П2.4 — РЕШЁН ИНАЧЕ. `emptyState`-проп + «Пока пусто.» + кнопка
  «+ Добавить» (`ChapterList.tsx:146–149`); именованного EmptyState нет,
  но действие к созданию ведёт.
- 01-П1.2 — РЕШЁН ИНАЧЕ. Каркас `EntityPage` требует `crumbs` с именем, все
  карточки передают (`SessionDetailPage.tsx:572–576`,
  `CampaignDetailPage.tsx:616`); фолбэк `buildCrumbs` для списков id
  по-прежнему пропускает (`AppShell.tsx:387–391`) — остаток уже учтён
  в других пунктах.
- 01-П1.4 — РЕШЁН. Muted ≥4.5: noir ≈4.8 (`themes.ts:183–186`), riot ≈5.7
  (`themes.ts:388–390`), neon ≈4.7 (`themes.ts:401–403`).
- 01-П2.6 — РЕШЁН. `SettingsListPage` на `EmptyState` (:250,274) и CSS-классах
  шапки (:159–166); `anarchyStar` остался только в палитре иконок жанров
  (`genreData.ts:107`).
- 01-П2.7 — РЕШЁН. `TagChips` рисует `.badge.tag` (`TagChips.tsx:27,45`).
- 01-П2.10 — РЕШЁН. В `dnd-sheet.css` все `border:` — 1px; text-shadow выключен
  (`statblock.css:29`); круги-жетоны узаконены §6.2 (`dnd-sheet.css:1473`).
- 01-П2.11 — РЕШЁН ИНАЧЕ. Принят «капс на разделителе»
  (`creature-card.css:132`), а не инверсия; акцент класса ужался до 2 мест
  (`DndCharacterForm.tsx:2742,2749`); у портрета тон/зерно
  (`dnd-sheet.css:1311–1353`).
- 01-П2.30 — РЕШЁН. `--sb-paper` в скопе (`index.css:190,312`); `btn-capsule` —
  Label-голос + моно-счётчик (`index.css:3132–3145`); системный шеврон
  (`index.css:8807–8819`); пикер переписан (`PlayerVisibilityPicker.tsx:86–88`).
- 01-П2.27 — РЕШЁН ИНАЧЕ. Статблок объявил свою пару 1.5/2px с записью
  (`statblock.css:17–18,78`), барьер принимает (`check-spacing.mjs:13–18`) —
  требование «всё в 1px» отменено решением.
- 01-П2.15 — РЕШЁН. Табы `nowrap`+скролл (`index.css:5121–5123`); мера 1440px
  (:3881); саб-заголовки uppercase (:3753); мобильный порядок
  (`home.css:525–529`).
- 01-П2.26 — РЕШЁН. Чипсы монохром (`index.css:2952–2964`); `on-surface-muted`
  (`themes.ts:200,321`); на главной `OnboardingHero`
  (`HomeCalendarPage.tsx:409`).
- 01-П2.31 — РЕШЁН. Бейдж-инверсия (`StoragesSettingsPage.tsx:553`); пустое имя
  с объяснением (:312–314) + Enter/Esc (:542).
- 01-П2.32 — РЕШЁН. См. П2.2 + «Пульт»-акцент только при данных
  (`SoundBarExtras.tsx:67`); капсула только мобильная (`index.css:3928–3936`).
- 01-П2.о1 — РЕШЁН. Вердикт «ЗАКРЫТО» подтверждается: барьер `check-spacing`
  прогнан — сырых значений нет.
- 01-П2.о2 — РЕШЁН. Барьер вшит в `build`/`lint` (`client/package.json:8–9`)
  и зелёный.
- 07-П2.6 — РЕШЁН. Каталог на версионированном формате: GitHub-манифест +
  `minAppVersion` + гейт `tooOldFor` (`server/src/routes/modules.ts:17,43,50,85–87`).

### 2.2. Файл 02 (10)

- 02-П1.5 — РЕШЁН. Ветка `format==="litm_character"` открывает визард
  (`StatblockList.tsx:416–433`).
- 02-П0.5 — РЕШЁН. `pickBackground` резолвит навыки ключами
  (`DndCharacterForm.tsx:4363–4377`, комментарий фиксирует починку 15/72).
- 02-П1.26 — РЕШЁН. Ростер: уровень/КЗ/хиты/скорость + `CharacterPeekModal` +
  `CampaignSquadSummary` (`CampaignDetailPage.tsx:1642,1646,1784,1824`).
- 02-П1.30 — РЕШЁН. Оборот листа: `DndCardBack.tsx:1–80`, инбокс
  `characterInbox.ts:36–43`, переворот/блик/жест
  (`DndCharacterForm.tsx:9528,9679,9833,9897–9898,11365`).
- 02-П1.36 — РЕШЁН. Сводка мёртвых ссылок внизу листа (`dnd/deadLinks.ts:3–4`,
  `DndCharacterForm.tsx:12254–12261`).
- 02-П2.17 — РЕШЁН. Touch-альтернатива drag: кнопки «Выше»/«Ниже»/«Переместить
  в раздел» (`DndCharacterForm.tsx:2298–2302,3473`).
- 02-П2.40 — РЕШЁН. `stepLevel` считает от синхронного `pendingLevels`-ref
  (`DndCharacterForm.tsx:1922,1944,1947`).
- 02-П2.38 — РЕШЁН. Кадрирование drag-and-drop в `?edit=1` через `useFrameDrag`
  (`DndCharacterForm.tsx:10042–10048,10859`, `dnd/portraitFrame.ts`).
- 02-П1.19 — РЕШЁН ИНАЧЕ. Не авто-пересчёт после импорта, а кнопка
  «Пересчитать выдачи» + `ApplyLssSlots` + `mergeAfterRecompute`
  (`LssImportWizard.tsx:484,500–503,984,1018,1325`).
- 02-П1.22 — РЕШЁН ИНАЧЕ. Вместо prune инвентаря: общий `entryCache` +
  сводка мёртвых ссылок + фолбэк «Нет описания.»
  (`dnd/entryCache.ts`, `dnd/deadLinks.ts`, `DndCharacterForm.tsx:126,1383,12254`).

### 2.3. Файлы 03 + 05 + 09 (11)

- 03-П1.2 — РЕШЁН. PUT правит и `setting_calendar_events` при
  `source_event_id` (`server/src/routes/settingLocations.ts:582–598`).
- 03-П1.3 — РЕШЁН. Счётчик в табе (`GalleryTab.tsx:269`).
- 03-П1.7 — РЕШЁН. Предупреждение «Максимум 100 пинов» (`LocationMap.tsx:553`).
- 03-П1.10 — РЕШЁН. «По строке на точку» с preview + чеклист конвертации
  (`LocationDetailPage.tsx:536–622`).
- 03-П2.7 — РЕШЁН. Счётчики видимости в шапках
  (`SettingPlayerContentTab.tsx:184–188,403–404`).
- 03-П2.8 — РЕШЁН. Точки мимо графа по умолчанию + тумблер
  (`GraphPage.tsx:32–34,161`); поиск ведёт на родителя с `?spot=`
  (`SearchPanel.tsx:20–26`).
- 05-П2.1 — РЕШЁН. Дроп/вставка проставляет `parent_key`
  (`CanvasPage.tsx:4676–4684`; сервер `POST /board/node`: `canvas.ts:2768–2813`).
- 05-П1.10 — РЕШЁН. Ctrl/Cmd-клик добавляет к выделению
  (`CanvasPage.tsx:4978–4984`).
- 09-Х2 — РЕШЁН. `beaa712` — предок `main` (`git merge-base --is-ancestor`);
  прав фиксплан.
- 09-Х2 (выяснено) — РЕШЁН. Вердикт подтверждён тем же git-фактом; действий
  не требует.
- 09-Х7 — РЕШЁН. Правило на месте (строка съехала 340→1038):
  `client/src/index.css:1038`, используется
  (`CampaignPlayerSectionsTab.tsx:194`).

### 2.4. Файлы 04 + 06 + 08 (13)

- 04-П1.6 — РЕШЁН ИНАЧЕ. Герой ведёт в карточку сессии, пульт — из навиджета,
  а не дублем поверх героя (`HomeCalendarPage.tsx:350–354`).
- 04-П1.8 — РЕШЁН. Капсула уже `div[role=group]`, «Развернуть» отделена
  (`audioPlayer.tsx:766–767`).
- 04-П1.9 — РЕШЁН. Кнопки с aria-label/title, слайдер `role=slider` ±5с,
  пустое состояние `role=status` (`audioPlayer.tsx:607–657,573`).
- 04-П1.10 — РЕШЁН. «Предпросмотр как игрок» открывает читалку-модалку
  (`CampaignPlayerSectionsTab.tsx:203`).
- 04-П1.11 — РЕШЁН. `reorderSections/reorderArticles` + кнопки ↑/↓
  (`CampaignPlayerSectionsTab.tsx:101,418–421`).
- 04-П1.17 — РЕШЁН ИНАЧЕ. «Показать игрокам» закрыто презентационным слоем
  второго экрана, а не кнопкой (`Verstak.tsx:513`, `SessionLivePage.tsx:7`,
  `ShowEntriesListener.tsx:5`).
- 04-П2.23 — РЕШЁН. `useUndoDelete` уже на существах, сообществах, артефактах,
  персонажах, локациях (`BeingDetailPage.tsx:101`,
  `CommunityDetailPage.tsx:45`, `ArtifactDetailPage.tsx:35`,
  `LocationDetailPage.tsx:190`).
- 06-П1.12 — РЕШЁН. Вкладка до входа подхватывает смену токена через
  `storage`-слушатель (`client/src/api/client.ts:21–36`).
- 06-П2.33 — РЕШЁН. `?token=` убран; файлы — подписанные HMAC-URL 60с
  (`client/src/api/client.ts:65–72`).
- 06-П0.2 — РЕШЁН ИНАЧЕ. `/auth/players` закрыт ролью `gm`
  (`server/src/routes/auth.ts:106`); массовый список — штатная GM-панель
  (`PlayerProfilePanel.tsx:47`).
- 06-П2.26 — РЕШЁН ИНАЧЕ. Единый расчёт в `shared/` (`deriveSheet`), а не
  `packages/rules` (`shared/src/dnd/derive.ts:346`,
  `server/src/routes/player.ts:14`).
- 06-П2.37 — РЕШЁН. CORS сужен до private-диапазонов + `ALLOWED_ORIGINS`
  (`server/src/index.ts:154–184`).
- 08-Р37 — РЕШЁН ИНАЧЕ. `/appearance` оставлен алиасом-редиректом с баннером,
  а не удалён (`App.tsx:219`, `StoragesSettingsPage.tsx:452`).

## 3. Не проверяемо статикой (68) — что с ними делать

Общее правило: это не долг кода, а очередь на рантайм/БД/владельца. Не
закрывать заочно; закрывать только прогоном или ответом.

- 07-П1.2 — исходников `screenw/` нет в репо; «утверждённость» текста
  «О программе» — ответ владельца.
- 07-П2.20 — нужны замеры на production-билде в рантайме.
- 02-П3.1, П3.2, П3.3, П0.6, П1.28, П1.29, П2.20, П2.21 — контент/разметка БД
  (лавка, регионы, правила магии, алиасы, мёртвые ссылки, подклассы, наборы,
  флаг `cursed`): нужна БД владельца.
- 02-П2.18, П2.30, П3.4, П2.р2 — отложенные/забранные владельцем решения
  (кости на Главной, сетка «Действий», рамка ярусов, значки школ).
- 02-П2.25 — нужен визуальный дифф (файлы на месте в `MainWorks/CardCanvas/`).
- 02-П1.38 — ссылка на строки протухла (там теперь чипы групп); нужен рантайм
  с пустыми секциями.
- 03-П2.13 — приёмка важных дат: визуал ×3 разрешения + lint/build, рантайм.
- 03-П2.4 — перекрытие minimap на 390: только рантайм/CSS.
- 09-Х1 — ручка `PUT /groups/:arcId` жива (`canvas.ts:2688`); HTTP-проверка
  с auth — только рантайм.
- 09-Х6 — глазной прогон «Корень [beta]» — нужен браузер.
- 08-Р1…Р36, Р40–Р47, Р-цвет (46 шт.) — все как один требуют ответа/выбора
  владельца (имена, тексты, цвета, механики, границы, бизнес-решения). Это
  очередь вопросов, а не долг кода; держать отдельно от П-серий.
- 06-П0.7 — восстановление бэкапа Windows→Linux: нужны две ОС.
- 06-П2.31 — приёмка UID: ручной прогон с замерами.
- 04-П3.8 — онбординг-тур: нужен гриллинг.

## 4. Рекомендации

- 52 пункта из раздела 2 пометить закрытыми (прецедент зачёркивания в 01 уже
  есть: `~~П2.о1~~`, `~~П2.о2~~` с вердиктом «ЗАКРЫТО 2026-09-12»), НЕ удалять —
  нужны строке «Откуда» и истории.
- Обновить `ToDo/README.md`: «~175» → 142; переписать статусную строку 32–33;
  добавить этот файл в список; зафиксировать допустимость Р-серии в 08
  и Х-серии в 09 либо переименовать их в П-формат.
- Устранить дубли номеров между файлами (раздел 1, пункт про строку 28) —
  иначе ссылки из манифеста неоднозначны.
- Установить владельцев заголовков `П1.к1`, `П2.о3`, `П2.р1`, `П2.р2`,
  `Р-цвет` (следы параллельной правки) и либо вписать их в нумерацию,
  либо вынести.
- 68 непроверяемых не трогать до прогона/ответа; 08-серию держать как очередь
  вопросов владельцу.
- Самые весомые из оставшихся актуальными (ориентир, не список): сырые
  `z-index` у плеера, выбор клетки карты 2px против 1px из §7
  (`maps/render.ts:1141`), таб-точка 6px (`statblock.css:1095–1103`),
  бэкап-роутер без restore/ретеншена, `Goods.md` ниоткуда не линкуется,
  `adm-zip`+`archiver` обе в зависимостях.
