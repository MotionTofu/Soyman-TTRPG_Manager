# ToDo/09 — Расхождения (хвосты из отчётов, 2026-09-12)

Пункты, помеченные сделанными, но не подтверждённые кодом.
Каждый — перепроверить, затем либо закрыть, либо завести работу.

## Первый проход (SB-серия)

### Х1 — HTTP-проверка `PUT /groups/:arcId` не делалась

**Что.** Шаг заявляет сведение `PUT /groups/:arcId` к `UPDATE story_arcs`,
но HTTP-проверка пропущена (auth). Smoke был только на копии базы.
**Где.** `server/src/routes/canvas.ts` (`PUT /groups/:arcId`)
**Откуда.** `SideWorks/SB_step_drop_canvas_groups.md` · 2026-08
**Проверено сегодня.** не проверялось (сам факт пропуска — из текста шага)
**Готово, когда.** Ручка проверена вживую или зафиксирована как мёртвая.

### Х2 — Противоречие «закоммичено `beaa712`» vs «жду „делай“» (ЗАКРЫТ, см. «Х2 (выяснено)» ниже)

**Что.** `SB_canvas_fixplan.md` пишет «закоммичены `beaa712`», а пошаговые
отчёты — «коммит НЕ делался, жду „делай“». Одно из двух неверно;
серия `SB_step_*` поэтому целиком остаётся (правило серии).
**Откуда.** `SideWorks/SB_canvas_fixplan.md` vs `SideWorks/SB_step_P1…V1.md` · 2026-08
**Проверено сегодня.** не проверялось (`git log --oneline` — следующий проход)
**Готово, когда.** `git log` показывает, что реально закоммичено;
статусы в шагах и фиксплане совпадают.

## Проход 2 (§2.5-проверка 2026-09-12)

### Х3 — Revision.md: строка P-12 устарела (кэш применён)

**Что.** Файл пишет «P-12: кэш заявлен, в date.ts не применён», но кэш
применён (`utils/date.ts:17`). Остальные проверенные пункты сошлись.
**Где.** `utils/date.ts:17`
**Откуда.** `SideWorks/Revision.md` P-12; проверка кодом 2026-09-12
**Проверено сегодня.** ПОДТВЕРЖДЕНО расхождение: `date.ts:17` — кэш на месте
**Готово, когда.** Строка в отчёте исправлена либо файл архивируется
с пометкой о единственной устаревшей строке.

### Х4 — Graph.md: поузловая a11y пропала при переписывании на canvas

**Что.** Фаза 4.3/п.22 требовала `role="button"`, `aria-label`, `tabIndex`,
`onKeyDown` на узлах; после переписывания рендера SVG→canvas поузловой
разметки нет (`RelationGraph.tsx`: 0 хитов `role=`/`aria-`; лишь обёртка
с `tabIndex`+`onKeyDown`). Заодно: строковые адреса в проверочной таблице
файла устарели, агрегат «22 ✅» сам по себе непроверяем.
**Где.** `client/src/components/RelationGraph.tsx`
**Откуда.** `SideWork/Graph.md` фаза 4.3/п.22; проверка кодом 2026-09-12
**Проверено сегодня.** ПОДТВЕРЖДЕНО расхождение (механизм сменён)
**Готово, когда.** Либо a11y восстановлена на canvas, либо зафиксировано
решение «поузловой доступности нет».

### Х5 — LocTree/00: Q7 заявляет dagre, в коде свой layout

**Что.** Гриллинг Q7 фиксирует автолейаут `dagre` rankdir TB/BT/LR, но в коде
`dagre` не используется — свой `layoutForest`/`layoutRadial`/`layoutNested`
(`geographyRootLayout.ts:125` «без внешних зависимостей (beta: вместо
dagre)»). Q3 и Q6 сошлись.
**Где.** `geographyRootLayout.ts:125`, `LocationRootGraph.tsx`
**Откуда.** `MainWorks/LocTree/00_грилинг.md` Q7; проверка кодом 2026-09-12
**Проверено сегодня.** ПОДТВЕРЖДЕНО расхождение
**Готово, когда.** Либо замена зафиксирована решением владельца, либо отчёт
поправлен.

### Х6 — Ручной прогон «Корень [beta]» в браузере (LocTree)

**Что.** Код графа готов (layout без dagre, 3 ориентации, drop со сменой
parent), но глазной прогон из §5 плана — лес, drag с детьми, reload,
фильтры, 100+ нод — заявлен как «нужен браузер».
**Где.** `LocationRootGraph.tsx`, `geographyRootLayout.ts`, `SettingDetailPage.tsx`
**Откуда.** `MainWorks/LocTree/01_план.md` §5 + `02_реализация.md` §Проверки
**Проверено сегодня.** не проверялось (нужен браузер)
**Готово, когда.** Прогон пройдён на 2–3 корнях × 3 глубины, дефектов нет
либо заведены тикеты.

## Проход 3 (2026-09-12)

### Х2 (выяснено) — Коммит beaa712 реально в main

**Что.** Противоречие снято: `beaa712` «Полотно: legacy canvas_groups
выпилен целиком» — предок `main` (проверено `git merge-base --is-ancestor`).
Прав фиксплан; строки «коммит НЕ делался, жду „делай“» в шагах написаны до
коммита и не обновлены. Условие разблокировки серии «коммит» выполнено;
серия всё равно остаётся по остальным хвостам (`ToDo/05`, `ToDo/09` Х1).
**Откуда.** `SideWorks/SB_canvas_fixplan.md` vs шаги; `git log --all` 2026-09-12
**Проверено сегодня.** ПОДТВЕРЖДЕНО: `beaa712` в истории `main`
**Готово, когда.** — (выяснено, действий не требует; шагам можно проставить
пометки «исправлено»/«закоммичено» при касании).

### Х7 — CSS для игроков заявлен, но правило отсутствует

**Что.** Fix #1 заявил `.campaign-player-overview` в `index.css:340`, grep
даёт 0 совпадений. Статьи остаются без рамки, EmptyState без CTA.
**Где.** `client/src/index.css:340`, `CampaignPlayerSectionsTab.tsx:85`
**Откуда.** `SideWorks/Для игроков.md` Ревизия 2 · 2026-08-31
**Проверено сегодня.** не проверялось (заявлен grep источника)
**Готово, когда.** Правило на месте, рамки как в Хронике мира.

### Х8 — Документы противоречат коду и друг другу (Аудит_4)

**Что.** D-02 опровергнута (`--sp-*` нашлись), D-04 обвиняла не тот механизм,
D-05 завышена, две «мёртвые» страницы отозваны живыми, гриллинг-пункты
Остатка в поздних доках значатся сделанными.
**Где.** `MainWorks/Аудит_4_перспективы.md` §§2,7
**Откуда.** `MainWorks/Аудит_4_перспективы.md` §§7–8 · 2026-09-02
**Проверено сегодня.** не проверялось
**Готово, когда.** Каждое «сделано» либо подтверждено grep/чтением, либо
выписано как хвост, не молча.
