# ToDo/05 — Полотно и граф (хвосты из отчётов, 2026-09-12)

## Выписано в первом проходе (SB-серия, SideWorks)

### П2.1 — Пины/бандлы/картинки создаются вне фрейма

**Что.** POST для пинов, бандлов и изображений не принимает `parent_key` —
членство во фрейме появляется только перетаскиванием.
**Где.** `server/src/routes/canvas.ts` (POST), `client/src/pages/CanvasPage.tsx` (`frameAtPoint` в `handleDrop`).
**Откуда.** `SideWorks/SB_step_P2.md` §«Осталось» · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Создание объекта дропом/вставкой внутри фрейма сразу
проставляет членство, как это делает перетаскивание.

### П2.2 — Сравнение снапшота «память↔база» может пропустить запись

**Что.** Сверка идёт по `b.nodes`; при рассинхроне памяти и базы запись
может не уйти, автор шага считает это «допустимым».
**Где.** `client/src/pages/CanvasPage.tsx` (`LayoutSnapshot`, `restoreSnapshot` → `PUT /board/nodes`).
**Откуда.** `SideWorks/SB_step_P3.md` §«Ограничение/заметка» · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Рассинхрон либо невозможен по построению, либо явно
залогирован и перезаписан; решение зафиксировано.

### П2.3 — Терминология Ш3 в комментариях после выпила `canvas_groups`

**Что.** После `DROP TABLE canvas_groups` (главы — узлы `type='chapter'`)
в комментариях мог остаться словарь Ш3; шаг V1 закрыл вопрос решением,
чистку делал `SB_step_drop_canvas_groups.md` §5 — сверить, что вычищено.
**Где.** `client/src/pages/CanvasPage.tsx`, `server/src/routes/canvas.ts`
**Откуда.** `SideWorks/SB_step_V1.md` (остаток Ш3) · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** `grep` по `canvas_groups|arc_id|moveToChapter` не показывает
мёртвых упоминаний, кроме осознанных (`groups: []`, см. П2.4).

### П2.4 — Заглушка `groups: []` в `GET /board` оставлена сознательно

**Что.** Ответ до сих пор возит пустой `groups: []`; убрать — значит менять
контракт, клиенты могут его читать.
**Где.** `server/src/routes/canvas.ts` (`GET /board`)
**Откуда.** `SideWorks/SB_step_drop_canvas_groups.md` · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Либо поле убрано и клиенты проверены, либо зафиксировано
решение «оставляем как маркер совместимости».

## Проход 2 (Canvas-находки, 2026-09-12)

### П1.10 — Ctrl+клик заменяет выделение вместо добавления

**Что.** `onNodeClick` пишет focus в адрес, эффект `focusParam` ставит
выделение ровно на одну ноду — группу из разнесённых сцен рамкой не набрать.
**Где.** `CanvasPage.tsx` (эффект focus + `onNodeClick`)
**Откуда.** `Canvas/02_CanRev_Rep.md` Н3 · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Ctrl+щелчки добавляют ноды к выделению, панель свойств
определена при множественном.

### П3.5 — Снимок истории не хранит collapsed

**Что.** Свёртка свободной рамки живёт в `canvas_frames.collapsed` и в Ctrl+Z
не попадает; будущий механизм, двигающий соседей, будет врать при отмене.
**Где.** `CanvasPage.tsx` (`snapshot`/`restoreSnapshot`)
**Откуда.** `Canvas/02_CanRev_Rep.md` Н22 · 2026-08
**Проверено сегодня.** не проверялось
**Готово, когда.** Поле collapsed включено в снимок либо зафиксировано
решение не включать.

### П2.12 — `canvas_groups` без читателей: сверить с выпилом

**Что.** Находка требует удалить таблицу и мёртвые ветки; выпил заявлен в
`SB_step_drop_canvas_groups.md` (`DROP TABLE`, главы — узлы `type='chapter'`).
Сверить, что в коде не осталось мёртвых обращений.
**Где.** `server/src/routes/canvas.ts`, `canvas_groups`
**Откуда.** `Canvas/02_CanRev_Rep.md` Н24 · 2026-08; скрестить с `SideWorks/SB_step_drop_canvas_groups.md`
**Проверено сегодня.** не проверялось
**Готово, когда.** Строки и мёртвые ветки удалены, имя главы правится через
`PUT /story/arcs/:id`, `grep canvas_groups` пуст кроме осознанных мест.

### П3.6 — Живой фильтр типов в SettingGraphTab (Этап 1a)

**Что.** План графа требовал живой фильтр типов; в `SettingDetailPage`
состояние типов без сеттера — фильтр не сделан. Это пункт плана, а не
заявление о готовом.
**Где.** `SettingDetailPage.tsx:1455` (`const [activeTypes]` без setter)
**Откуда.** `SideWorks/Граф_Связей_Профиль_Сеттинга.md` Этап 1a · 2026-08
**Проверено сегодня.** `grep` 2026-09-12 подтверждает: сеттера нет
**Готово, когда.** Фильтр типов работает живьём либо пункт снят решением.

## Проход 3 (2026-09-12)

### П3.7 — Выходы на полотне «Дерево» и в графе

**Что.** В этом заходе выходы только в карточке; полотно и граф — следующим
шагом после гриллинга.
**Где.** Полотно, граф связей
**Откуда.** `MainWorks/Карточка_места_и_выходы_—_решения_2026-09-11.md` §4 п.8 + §5 · 2026-09-11
**Проверено сегодня.** не проверялось
**Готово, когда.** Выходы видны и кладутся на полотне и в графе или тикет
в очереди гриллинга.
