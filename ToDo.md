# ToDo

Собрано из трёх мест: `later.md` (сознательно отложенное), «Находки» ревизии
Полотна (`Canvas/02_CanRev_Rep.md`, Н1–Н24) и разделы «Найдено попутно, **не
исправлено**» в `design_revision.md`. Ничего нового здесь не придумано — это
те же задачи, разложенные по срочности.

Порядок мой, не владельца, и он спорный по построению: я ставлю выше то, что
**молча портит данные или врёт Мастеру**, и ниже то, что просто ещё не
сделано. Если приоритет не совпал с вашим — двигайте, файл для этого и заведён.

- **П0** — портит данные или ломает выпуск. Делать до нового.
- **П1** — врёт Мастеру или мешает за столом. Делать в ближайшую очередь.
- **П2** — долги и уборка. Делать, когда рядом.
- **П3** — новое. Требует гриллинга, а не кода с ходу.

Номер задачи — уровень плюс порядок внутри него (`П0.2`), чтобы вставка
новой строки не сдвигала все остальные. Источник помечен в конце: `later.md`,
`Н<номер>` (находка ревизии Полотна), `design_revision`, `оценка` — разбор
готовности к раздаче (2026-08-28, раздел в конце файла).

**Пометка «простая»** значит: задачу можно отдать исполнителю послабее — модели
попроще или человеку, впервые открывшему этот репозиторий. Под этим понимается
всё сразу: правка лежит в одном-двух местах и найдена по точному адресу;
рядом есть образец, с которого списать; готовность проверяется глазами или
одной командой; ошибка не портит данные необратимо. Всё остальное требует
решения — какое слово написать, что считать правильным поведением, что удалять,
— и такие задачи без разговора с владельцем начинать не стоит.

**«Простая после решения»** — исполнение механическое, но сначала нужен ответ
владельца на один вопрос, названный в самой задаче.

Сейчас простых шесть: **П0.3, П1.10, П2.9, П2.10, П2.11, П2.13**. Ещё четыре
ждут одного ответа владельца: **П1.6, П1.7, П2.3, П2.8**. Порядок работы с
коммитом, пушем и релизом — в `RELEASE.md`, он написан так, чтобы по нему мог
идти кто угодно.

---

## П0 — портит данные или ломает выпуск

### П0.1. `admin`/`admin` на каждой установке — ПОЧИНЕНО 2026-08-28

`bootstrapAdminAccount()` вызывался при **каждом** старте и заводил
пользователя `admin` с паролем `admin`, ролью `gm` и правами админа, если
такого имени ещё нет. Пароль был зашит в код.

Само по себе это было бы полбеды, но сервер слушает **все интерфейсы**:
`httpServer.listen(PORT)` без адреса (`server/src/index.ts:234`), в десктопной
сборке — порт 4732. Если брандмауэр разрешён (а его спрашивают при первом
запуске и разрешают обычно не глядя), любой в той же сети — общая квартира,
коворкинг, вайфай в кафе — открывал `http://<машина>:4732`, входил
`admin`/`admin` и получал полный доступ мастера: секреты, финансы, всё.

**Первое в этом файле по срочности**: цена ошибки не «неудобно», а «чужой
человек читает вашу кампанию».

**Осторожно с очевидной правкой.** Просто закрыть сервер на `127.0.0.1`
нельзя: доступ из локальной сети здесь сделан НАМЕРЕННО — CORS отдельно
разрешает частные диапазоны, чтобы телефон в том же вайфае работал клиентом
(`isPrivateLanHost`, `server/src/index.ts:91`). Закрыв порт, вы отключите
мобильных игроков и мастера в дороге.

Значит чинить надо не дверь, а ключ под ковриком: не заводить `admin` с
известным паролем вовсе, а права админа отдавать первому мастеру, которого
заводит первый запуск (в вашей базе у `tofu_ept` сейчас `is_admin = 0`, и
админ — только та самая учётка `admin`). Второй слой, если захочется, —
переключатель «пускать устройства из этой сети», выключенный по умолчанию.
Смена своего пароля уже есть: `PUT /api/auth/me`.

**И отдельно про вашу собственную базу:** учётка `admin` в ней есть
(`users.id = 16`, `is_admin = 1`) и будет убрана миграцией при следующем
запуске рабочего приложения — до этого момента дыра в ней открыта.

**Сделано 2026-08-28.** Заведение учётки `admin` убрано; права админа получает
первый мастер, которого заводит первый запуск (`POST /auth/setup`) или
переменные `ADMIN_USERNAME`/`ADMIN_PASSWORD`. Для уже существующих установок —
разовая миграция: права переезжают первому мастеру, а сама учётка удаляется
**только если её пароль всё ещё `admin`** (тогда это не аккаунт, а ключ под
ковриком; сменили пароль — значит ею пользуются, и её не трогают).

Проверено на копии рабочей базы: после старта `tofu_ept` получил `is_admin = 1`,
учётка `admin` удалена — то есть её пароль действительно оставался
`admin`, и дыра была настоящей, а не теоретической. Порт по-прежнему открыт
для локальной сети: это намеренная возможность, и телефоны игроков работают
как работали. `оценка`

### П0.2. Удаление записи компендиума оставляет статблоки-сироты

`DELETE /systems/entries/:id` удаляет одну строку. Статблоки ссылаются на
запись полиморфно (`owner_type`/`owner_id`), внешнего ключа нет, каскадом они
не уходят.

**Это уже не гипотеза.** В рабочей базе **583** статблока указывают на
несуществующие записи компендиума — при 4 системах и 3451 живой записи.
Каждый такой статблок занимает место, участвует в подсчётах и однажды
достанется чужой записи, если id переиспользуется.

Чинить: каскад в самом маршруте (статблоки, `generic_links`,
`entity_relations`, главы, избранное) плюс разовая уборка существующих сирот —
после того, как владелец посмотрит, что именно уберётся. `later.md`

### П0.3. `backfillResizeImages.ts` испортит все связанные картинки

`server/src/scripts/backfillResizeImages.ts:56` уменьшает картинку записью
**поверх файла**. Хранилище дедуплицировано жёсткими ссылками, поэтому один
прогон изменит байты у всех соседних ссылок: портрет статблока, аватар записи,
плитку — всё, что делит файл.

Скрипт разовый и пока не запускался, поэтому это П0 «до того, как», а не «уже
случилось». Правка — три строки: удалить файл перед записью, как теперь
делает `storeDeduped`. `design_revision`

**Простая.** Три строки по образцу `storeDeduped`: удалить файл перед записью.
Готовность видно проверкой `nlink` у соседнего файла. ✅ ВЫПОЛНЕНО 2026-08-29.

### П0.4. Счётчик `systems` ушёл в тысячи

`sqlite_sequence` для `systems` — **6936** при максимальном существующем id
**6064** и четырёх системах в базе. То есть тысячи систем были созданы и
удалены. Само по себе не вредит, но систему создают три места в коде, и ни
одно не должно срабатывать тысячи раз: если там цикл, он живой и сейчас.

Задача — не «поправить счётчик», а найти, кто это делает. Числа в `later.md`
устарели (там 5237 при max 416) — то есть за это время наросло ещё. `later.md`

### П0.5. Каталог модулей отдаёт битые выгрузки

`github.com/MotionTofu/soyman-modules` — витрина, из которой приложение ставит
модули. Лежащие там `dnd55.json` и `Set_Phandelver.json` выгружены версией,
которая теряла главы, связи, принадлежность и описания существ.

Единственная задача в списке, где страдает **не владелец, а посторонний
человек**, — потому и П0, хотя работы на один прогон выгрузки. `later.md`

### П0.6. Вкладка, открытая до входа, молча теряет право на запись

`client/src/api/client.ts:8` читает токен из `localStorage` один раз при
загрузке модуля. Вход во втором окне (кнопка «Новое окно» — штатная) первую
вкладку не касается: чтение местами работает, любая запись отвечает 401, и на
экране **не меняется ничего** — ни доски, ни сообщения.

Потеря работы без предупреждения. Требует решения, как синхронизировать токен
между окнами (событие `storage`, перечитывание при 401) и что показывать
Мастеру. `Н1`

---

## П1 — врёт Мастеру или мешает за столом

### П1.1. Раздел «Здоровье»

Экран рядом с настройками, где по кнопке идёт ревизия базы и файлов, а
результат показывается списком с предложением починить: битые пути, потерянные
файлы, сироты (см. П0.2), отчёт «каких модулей не хватает».

`later.md` сам помечает его «приоритетнее прочего в этом файле», и это верно:
половина задач ниже — это проверки, которым нужен дом. `later.md`

### П1.2. Мультивыделение на Полотне не работает

`Ctrl`+щелчок должен добавлять ноду к выделению, а заменяет его: щелчок пишет
`focus` в адрес, эффект по `focusParam` ставит выделение ровно на одну ноду.
Проверено — три щелчка подряд оставляют выделенной последнюю.

Набрать группу можно только рамкой, а для нод в разных углах холста это лишний
жест. `Н3`

### П1.3. Свойства существа объявляет общая таблица, а не система

`KIND_DEFS` в `client/src/compendium.ts` — одна таблица на все системы. Вид
«Существо» объявляет «Размер» и «Класс опасности», и они показываются у всех
девяти существ Legend in the Mist, где ни того, ни другого не бывает.

Пустое поле чужой механики — не «ещё не заполнено», а неверное утверждение о
правилах. `later.md`

### П1.4. Имена вводятся через `prompt()`

В `CanvasPage.tsx` осталось **14** таких вызовов: шесть `prompt` (имя быстрой
сцены, быстрого приключения, быстрой доски, переименование ноды, текст
стикера), шесть `confirm` и два `alert`. Системный `prompt` не показывается в
части окружений — действие тихо не выполняется. Плюс правило «простота для
Мастера»: модальное окно поперёк работы, которое нельзя ни отменить мышью, ни
исправить потом на том же месте.

Требует гриллинга: заводить объект сразу с именем по умолчанию и открывать
панель свойств с выделенным полем — это смена поведения экрана. `Н2`

### П1.5. Проза в «Соевом нуаре» набрана моноширинным

`themes.ts:342` — `fontBody: "'PT Mono', monospace"`, и `index.css` повторяет
это в предзагрузочном `:root`. §1.5 запрещает моноширинный под прозу
категорически, а нуар — тема, в которой владелец работает каждый день.

Задевает всё приложение, не статблок, и висит на неразобранной шкале характера
нуара. `design_revision`

### П1.6. Карточки по умолчанию скруглены и обведены 1 px

`buildTheme` берёт `cardRadius ?? 6` и `cardBorderWidth ?? 1`
(`themes.ts:186`, `:124`); нули проставлены только зину. То есть тема, в
которой владелец работает, нарушает §1.1 (радиусов нет) и §1.3 (обводка 2 px)
на всех карточках приложения разом.

Правка на две строки, но задевает каждый экран — поэтому отдельным шагом.
Рядом стоит `Н18`: глобальный радиус кнопок 6 px, тот же инвариант, то же
решение владельца — либо инвариант правится, либо радиус снимается.
`design_revision`, `Н18`

**Простая после решения.** Владелец говорит, что побеждает — инвариант или
нынешний вид; дальше две строки в `themes.ts`.

### П1.7. Подписи в панели свойств Полотна не проходят по контрасту

`.canvas-props__label` — `color: var(--muted)`, в теме `noir` замерено 3,57:1
при норме 4,5:1. Либо поднять `--muted` (и проверить все места, где он стоит),
либо перевести подписи на `--ink` с меньшим кеглем, как у чипа подсказки. `Н17`

**Простая после решения.** Владелец выбирает способ — поднять `--muted` или
перевести подписи на `--ink`; дальше правка в одном классе.

### П1.8. На узком экране палитра и поиск Полотна накладываются

Обе панели по 260 px; на холсте 360 px они перекрываются на 224 px. В плане
ревизии это блок B5 («узкий экран»), где решается судьба всех оверлеев ниже
порога 700 px. `Н7`

### П1.9. Палитра Полотна одна на все темы

`applyCanvasPaletteVars()` пишет цвета в `:root` один раз и не смотрит на
тему: в тёмном нуаре плашки нод остаются насыщенными, как на светлой бумаге, а
контраст считался против `#F8F6F1`.

Модуль к правке готов — `canvasPaletteVars()` возвращает готовый словарь,
достаточно сделать его зависимым от темы. Но выбор цветов для тёмной темы —
проектирование. `Н5`

### П1.10. Визард ссылок не запускается из кампании и системы

Ядро (`server/src/import/crossLinks.ts`) умеет четырёх владельцев, умолчания
для кампании и системы прописаны и работают, — а кнопки в интерфейсе нет:
визард висит только на сеттинге и приключении. Тексты кампании при этом
ссылок не получают. `later.md`

**Простая.** Кнопка в двух местах по образцу той, что уже стоит на странице
сеттинга; серверная часть готова и умолчания прописаны. ✅ ВЫПОЛНЕНО 2026-08-29.

### П1.11. Своя форма статблока для транспорта

`VehicleDetailPage` заводит статблок как `compendium_entry`, и формат ему
достаётся `dnd_creature` — лист существа с мировоззрением и размером в шапке.
Пустые графы §1.11 прячет, так что наружу торчит немного, но корабль остаётся
существом. Новая модель данных — значит гриллинг. `later.md`

---

## П2 — долги и уборка

### П2.1. Относительные пути к файлам вместо абсолютных

**1077 путей в 34 колонках двадцати таблиц, абсолютны все до одного**
(`E:\RPG-Vault\...`). Пока хранилище одно и машина одна — не болит; в тот день,
когда база переедет, не заработает ничего. Смежная задача — кнопка «полная
копия приложения» (П3). `later.md`

### П2.2. `canvas_groups` лежит без читателей

После блока G6.2 таблицу (33 строки) не читает никто: рамки главы нет, место
узла в `canvas_nodes`, свёрнутость потеряла предмет. Осталась запись через
`PUT /canvas/groups/:arcId`, где цвет уже никем не показывается.

Убирать после того, как новая раскладка проживёт несколько сессий. `Н24`

### П2.3. Восемь строк `canvas_nodes` с типом `chapter` неизвестного происхождения

Доска 13 («Глубоководье: Ограбить Дракона»). Записаны 2026-08-27 в 20:41–20:42
восемью перетаскиваниями подряд, координаты — ровная сетка 4×2, с
`canvas_groups` тех же глав не совпадают. Миграция выбрала их молча, как более
новые.

Решение владельца: посмотреть доску и сказать, та ли раскладка. Если нет —
восемь строк удаляются, миграция берёт места из `canvas_groups`. `Н23`

**Простая после решения.** Владелец смотрит доску 13 и говорит, та ли
раскладка; дальше — удаление восьми строк и повторный прогон миграции.

### П2.4. У свободной доски два одинаковых идентификатора

`id` и `scope_id` всегда равны по построению, но адресуются по-разному:
маршруты и ссылка `?free_id=` — по `scope_id`, содержимое доски — по `id`. Пока
числа совпадают, перепутать их безнаказанно, и это уже случалось на проверке.
Ломается в тот день, когда доска заведётся не этим маршрутом. `Н11`

### П2.5. Переименовать `Статблоки/` в `Statblocks/`

Единственная кириллическая служебная папка хранилища
(`server/src/routes/statblocks.ts:45`); новые `Bestiary/` и `Vehicles/`
заведены уже по общему правилу. Правка с переносом существующих папок и
переписыванием путей в базе. `later.md`

### П2.6. Оригинальное название и синонимы в формате импорта бестиария

У записи есть `aliases` и `name_original`, и поиск по ним ищет. Импорт их не
заполняет: оригинал вклеивается прямо в имя («Нимблрайт [Nimblewright]»),
синонимов в формате нет вовсе. `later.md`

### П2.7. Связи графа на глобальные ключи

`generic_links` держит локальные id, тексты на глобальные ключи уже переведены.
Тот же довод: локальный id верен ровно внутри одного файла базы. Пока связи не
ездят между устройствами — не болит. Рядом: имя источника ссылки перенести из
текста токена в служебную таблицу. `later.md`

### П2.8. Имя пина по умолчанию — «Пин»

После того как подпись пина появилась на холсте, безымянный пин стал
единственной фигурой без подписи (в базе 9 из 13 пинов зовутся «Пин»). Решение
владельца: оставить как есть или заводить пин сразу с полем имени. `Н21`

**Простая после решения.** Владелец говорит, оставить безымянный пин или
заводить его с полем имени.

### П2.9. Кнопки масштаба Полотна подписаны по-английски

`Zoom In` / `Zoom Out` / `Fit View` — имена ставит библиотека, пропов для
подмены нет. Глазами не видно (иконки), слышно при чтении с экрана. Дешёвый
выход: свой ряд из `<ControlButton>` с русскими `aria-label`. `Н9`

**Простая.** Свой ряд из трёх `<ControlButton>` с русскими `aria-label`,
вызывающих `zoomIn`/`zoomOut`/`fitView` из `useReactFlow()`. ✅ ВЫПОЛНЕНО 2026-08-29.

### П2.10. Правило о переводах строк в плане ревизии неверно

`Canvas/01_CanRev_Plan.md`, правило №4 делит файлы на «`CanvasPage.tsx` — CRLF,
остальные — LF». Проверено байтами: вперемешку. Переписать правило на
«определять по содержимому», список имён убрать. `Н20`

**Простая.** Правка текста документа, кода не касается. ✅ ВЫПОЛНЕНО 2026-08-29.

### П2.11. Превью текстового шаблона роняет разметку в консоль

`TemplatesTab` печатает `notes` внутри `<p>`, а `MentionText` разворачивает
markdown-таблицу в `<div><table>`: React ругается на каждый шаблон с таблицей.
`design_revision`

**Простая.** `<p>` меняется на `<div>`; готовность — консоль браузера молчит на
шаблоне с таблицей. ✅ ВЫПОЛНЕНО 2026-08-29.

### П2.12. Снимок истории Полотна не хранит свёрнутость

`snapshot`/`restoreSnapshot` держат позиции и `arc_id`, поля `collapsed` нет.
Пока свёртка не двигает ничего, кроме себя, — безобидно. Держать в голове:
если свёртка свободной рамки начнёт двигать соседей, поле придётся добавить.
`Н22`

### П2.13. Палитра Полотна и темы пишут в один `style` у `:root`

Сейчас безопасно: обе через `setProperty`, имена не пересекаются. Сломается
молча, если `applyTheme` когда-нибудь начнёт присваивать `style.cssText`
целиком — ноды станут бесцветными, и искать будут не там. Страховка:
перевызывать `applyCanvasPaletteVars()` после `applyTheme`. `Н6`

**Простая.** Одна строка: перевызвать `applyCanvasPaletteVars()` после `applyTheme`. ✅ ВЫПОЛНЕНО 2026-08-29.

### П2.14. Production-сборка не замерена

Все числа ревизии сняты на dev-сборке: `vite preview` поднимается на своём
порту и требует отдельного входа. Разрыв с production не проверялся.
`design_revision`

---

## П3 — новое, требует гриллинга

Здесь порядок внутри уровня — по моей оценке отдачи за работу, но это как раз
то, что решает владелец.

- **Обмен наработками между Мастерами** (блок G4). Разбор проведён, шестнадцать
  вопросов закрыты, код не писался — отложено по объёму. `later.md`
- **Кнопка «полная копия приложения»** — переносимый слепок настроек, базы и
  хранилища. Смежно с П2.17. `later.md`
- **Восстановление структурных связей после удаления системы.** Решено
  гриллингом 2026-08-20, требует `uid` у `systems`. `later.md`
- **Общая издательская личность системы** — `uid`, переживающий источник, чтобы
  модули разных Мастеров сходились по ключам. `later.md`
- **Слияние правок сеттинга в переписанное приключение кампании** — copy-on-write
  оставляет кампанию на старой версии, вернуться нечем. `later.md`
- **Копирование сущности между сеттингами.** `later.md`
- **Точки входа в узловой редактор из других разделов** и **системные ноды**
  (свои исходы проверки у Daggerheart, LitM, D&D). `later.md`
- **Настраиваемые панели пульта сессии**; **особый шаблон под нулевую сессию**.
  `later.md`
- **Звук:** встроенный пак иконок, обмен наборами между Мастерами, звук
  перехода в бой, пульт на отдельном устройстве и MIDI-контроллер. `later.md`
- **Вводный курс вместо пустой главной** — первый запуск как единственный
  момент, когда человек готов слушать. `later.md`
- **Верхнее положение навигационного виджета настоящей полосой.** `later.md`
- **«Перенести в главу…» пунктом меню** — вторая дверь к броску сцены на узел
  главы. `later.md`
- **Преимущество и помеха на кости характеристики** — вариант с Alt/Shift
  отклонён как «настройка в момент вождения», замены пока нет. `later.md`

---

## Перед тем как отдать приложение другим

Разбор готовности 2026-08-28. Пункты, у которых есть номер выше, здесь только
названы — чтобы порядок действий читался одним куском.

### А. До первой раздачи тестерам

Раздавать уже есть чем: репозиторий **публичный**, релиз выложен, ссылка на
установщик работает у любого, кому её дать. То есть техническая часть раздачи
не требует ничего — требуется вот это:

1. **Починить `П0.1`** — `admin`/`admin` и слушание всей сети. Пока не сделано,
   каждая розданная копия открыта для всех, кто рядом с тестером по сети.
   Единственный пункт, который тут действительно обязателен.
2. **Решить, какую сборку раздавать.** «Пустая» открывается совсем пустой:
   тестер увидит чистый экран и одну кнопку, и половина обратной связи будет
   «непонятно, с чего начать» — это верное наблюдение, но вы его и так знаете.
   «Полная» собирается из вашей рабочей базы (`scripts/build-seed.js`), то есть
   раздавать её нельзя, пока сид не сделан из заведомо чистой копии. Разумный
   средний путь: пустая сборка плюс один-два модуля из каталога, поставленные
   в три щелчка.
3. **Назвать одно место для находок.** Issues репозитория, чат, что угодно —
   но одно и названное прямо в письме к тестеру. Иначе половина обратной связи
   останется в устных разговорах и потеряется.
4. **Сказать, о чём спрашиваете.** Вас интересует удобство, а не список багов:
   «где вы застряли», «что искали и не нашли», «что пришлось делать дважды».
   Без этого приходят отчёты про опечатки, а не про то, ради чего раздавали.
5. **Знать, какая у тестера версия.** Уже есть: страница «О программе»
   показывает версию, автообновление доводит всех до последней.

**Чего на этом этапе НЕ нужно:** подписи кода, лицензии, инструкции про
игроков. Для десяти знакомых мастеров это лишняя работа; «неизвестный
издатель» в SmartScreen человек, которому вы написали лично, переживёт.

### Б. До открытой публикации

- **Подпись кода.** Сейчас установщик не подписан (в конфигурации сборки нет ни
  сертификата, ни `forceCodeSigning`), поэтому SmartScreen встречает словами
  «неизвестный издатель» — и при установке, и при каждом автообновлении. Стоит
  денег и требует времени на репутацию.
- **Лицензия.** В репозитории нет ни `LICENSE`, ни `CONTRIBUTING`, у GitHub
  лицензия не определена — формально «все права защищены», и человек не знает,
  что ему можно.
- **Права на содержимое.** Полная сборка и каталог `soyman-modules` несут
  переведённый компендиум D&D 5.5 — это текст из книг. В узком кругу обычное
  дело, при публикации — решение, которое надо принять сознательно (только
  SRD, только пустая сборка, модули отдельно).
- **Как дать доступ игрокам.** Требует хостинга, инструкции нет нигде. Для
  чужого мастера это сейчас нерешаемо без вас.
- **Уборка удаления (`П0.2`) и снимок базы перед миграциями.** Сейчас снимок
  снимается только перед одной, самой опасной. На чужих данных снимать стоит
  всегда.
- **Хоть какие-то автотесты.** Их нет ни одного: проверка вся ручная. Для
  одного пользователя это работает — вы же и проверяете; на сотне регрессии
  находят пользователи, и узнаёте вы постфактум. Первыми стоит закрыть импорт
  и миграции: они трогают чужие данные необратимо.
- **Назвать границы вслух:** только Windows, только русский язык.

### В. Решения, а не код

Эти три пункта нельзя «сделать» — их надо решить, и до публикации:

1. Под какой лицензией это отдаётся.
2. Что делать с компендиумом из книг.
3. Собирать ли обратную связь внутри приложения или снаружи.

---

## Не задача, но помнить

**Испорченные портреты статблоков не восстанавливаются.** Замена картинки
переписывала общий файл хранилища вместе со всеми ссылками на него (починено
2026-08-28 в `storeDeduped`). Там, где портрет статблока уже подменился
картинкой плитки, старых байтов на диске нет — их нужно загрузить заново,
дальше замена держится.
