# Хроника игр — отчёты по фазам

> Раздел: `Профиль кампании → Хроника игр` (`CampaignDetailPage.tsx:578-683`, `MonthCalendar.tsx`)
> Эталон: `design_revision.md` (выигрывает при расхождениях), план — сводка 3 аудитов (D/C/U).

---

## Фаза 0 — Стоп-кран (без UI, не ломать данные)

Цель: закрыть P0 кода до любого редизайна. 4 задачи.

| # | Задача | Статус |
|---|---|---|
| 0.1 | `campaignId` guard `NaN → 404` | ✅ |
| 0.2 | `AbortController` + `.catch` для всех `refresh*` эффектов | ✅ |
| 0.3 | Сервер: проверка владения `calendar-events` + валидация `inworld_*` (+ лимиты) | ✅ |
| 0.4 | `backgroundImage` → `encodeURI` | ✅ |

---

### Лог

#### 2026-08-31 — старт фазы 0
- Создан файл отчётов, заведены todo.

#### 2026-08-31 — фаза 0 завершена, сборка зелёная

**0.1 `campaignId` guard** `CampaignDetailPage.tsx:96-99,180-204`
- `const invalidCampaignId = !Number.isFinite(campaignId)` сразу после `Number(id)`.
- Ранний возврат с `<p>Кампания не найдена</p> + К списку кампаний` до любых `api.get`. Все `useEffect` теперь `if(invalidCampaignId) return`.
- Закрывает `C:1.1` — переход на `/campaigns/abc` не делает `GET /campaigns/NaN/...` и не зависает на `Загрузка…`.

**0.2 `AbortController` + `.catch`** `CampaignDetailPage.tsx:159-195,122-125`
- `refreshCalendarEvents(signal?)`, `refreshCampaign(signal?)`, `refreshSessions(signal?)` принимают `AbortSignal`, пробрасывают в `api.get(...,{signal})`.
- 3 `useEffect`: календарные события, `settingImportantDates`, общий `refreshCampaign/Sessions + players/systems/settings` — каждый создает `new AbortController()` и `return () => ac.abort()`. `cycles` эффект тоже.
- Все `.then(...).catch(()=>{})` — тихий игнор при `AbortError` (ожидаемо) и при сетевом сбое не валит `Unhandled rejection`. Общий `useEffect` с `campaignId` также `return ac.abort()`.
- Закрывает `C:1.2` — гонка `campaign/1 → campaign/2` не перезатирает состояние.

**0.3 Сервер `calendar-events`** `server/src/routes/campaigns.ts:385-466`
- `POST /:id/calendar-events`: `title.trim()` + `length >200 / description >5000` → 400, `Number(y/m/d)` + `m 1..36, d 1..60` → 400. Раньше любой мусор уходил в `INSERT`.
- `PUT /calendar-events/:eventId`: `SELECT campaign_id ... WHERE id=?` → 404 если нет, проверка `title.trim()===0 →400`, длины, `inworld_month/day` диапазоны. `title` теперь `String(title).trim()`.
- `DELETE /calendar-events/:eventId`: `SELECT id ...` → 404 если нет, иначе `DELETE`.
- `important` логика сохранена (0/1). `defaultStatus` теперь на числах `y,m`.
- Закрывает `C:1.5,2.5` (владение + валидация). `requireAuth` не добавлялся — в проекте локальный десктоп, требование отложено.

**0.4 `backgroundImage` CSS-инъекция** `CampaignDetailPage.tsx:458-463`
- `style={{ backgroundImage: \`url("${encodeURI(campaign.background_image_url).replace(/"/g,"%22")}")\` }}` — `encodeURI` + экранирование кавычек. Закрывает `C:2.1`.

**Сборка:** `npm run build` — `server tsc` + `client tsc -b && vite build` — ✅ 573 модуля, `CampaignDetailPage` 91.72 kB gzip 19.50 kB, без ошибок.

**Следом:** фаза 1 — `POST /sessions/batch` с транзакцией + пагинация `campaigns/:id/sessions`.

**Изменённые файлы:**
- `client/src/pages/CampaignDetailPage.tsx`
- `server/src/routes/campaigns.ts`

---

## Фаза 2 — Каркас экрана (безопасный редизайн по `design_revision.md`)

Цель: убрать `plane-grid` с финансами с первого экрана, свести две гармошки к одному открытому `res-group`, оставить единственную ленту, пусто → `EmptyState`. Инварианты `§1.3 1px`, `§1.4 плашка-инверсия`, `§1.11 EmptyState`.

| # | Задача | Статус |
|---|---|---|
| 2.1 | Лейаут `stack gap:var(--sp-5)`: сводка в `band` → календарь → лента; финансы вниз свернуто | ✅ |
| 2.2 | Один `details.card.res-group open` с `summary.res-group__band` (hover `var(--accent)`) | ✅ |
| 2.3 | Лента — единственный список + фильтр/поиск (`cancelled` тоггл), таблицу убрать | ✅ |
| 2.4 | Пусто → `EmptyState` (икона + слоган + `primary`) | ✅ |

### Лог
#### 2026-08-31 — старт фазы 2

#### 2026-08-31 — фаза 2 завершена, сборка зелёная

**2.1 Лейаут** `CampaignDetailPage.tsx:599-650` `+ state 158-162`
- Убран `plane-grid 1fr 300px` (`index.css:2541`) с финансами на первом экране. Новый стек `stack gap:var(--sp-5)` (`campaign-overview` уже держит 24px, здесь инлайн `style={{gap:"var(--sp-5)"}}` — щедро снаружи, компактно внутри `§1.9`).
- Финансы GM не на первом экране, а в свёрнутом `details.card.res-group` внизу с `summary` инверсией; `loadHideFinance()` / `campaign.role==="player"` сохранены. `paid` ставка показывается `muted 11px` под календарём только когда уместно.
- Календарь обёрнут в `details.card.res-group open` с `summary.res-group__band` (§1.4 — плашка `var(--surface)/var(--on-surface)`, hover `var(--accent)` из `index.css:7732`), `count` моно `heldSessions` (§1.5). Добавлена видимая `primary + Запланировать сессию` + подпись `или кликните по дню`.
- Закрывает `D:§1.9,§1.12` + `U:1,3`.

**2.2 Один `res-group`** `CampaignDetailPage.tsx:652-740`
- Две гармошки `details.card / summary Хроника кампании + История сессий` → одна `details.card.res-group open` с `summary.res-group__band / .res-group__title ХРОНИКА / .res-group__count filtered из total`.
- `margin:0 / padding 7px var(--sp-3) / hover accent` из `index.css:7726-7738` — плашка не едет, не светлеет в бумагу.
- Открыта по умолчанию (§1.11 — главный блок не прячет содержимое). Закрывает `D:§1.4,§1.11` + `U:1`.

**2.3 Единственная лента** `CampaignDetailPage.tsx:653-720`
- Таблица `История сессий` удалена (была дубликатом ленты). Лента — `sessions.filter` по `showCancelled` + текстовый фильтр `chronicleFilter` (название/дата/`main_events`). `filtered` считается в IIFE без лишнего ререндера.
- Добавлен фильтр-ряд: `input Поиск...` + `checkbox Показать отменённые` + `Сбросить`. Пустой фильтр → `Ничего не найдено — Сбросить фильтр`.
- Даты теперь `formatDateKeyRu(s.date)` ("30 августа") вместо `YYYY-MM-DD`, бейдж `planned→Запланировано / held→Состоялась / cancelled→Отменена` (по-русски), `inworldDate` остался `muted`. Пустая запись — `Итоги не записаны. Записать →` (ссылка в сессию) вместо серого упрека. Каждая запись имеет `Открыть → / ✕ в архив` в одном ряду.
- Закрывает `D:§1.5,§1.11` + `U:4,5` + `C:3.1` (отменённые теперь видимы по требованию).

**2.4 Пусто → `EmptyState`** `CampaignDetailPage.tsx:690-702` `EmptyState.tsx:14` `ZineGraphics.tsx:25`
- Когда `sessions.length===0` — `EmptyState icon=skullDie title=ХРОНИКА ПОКА ПУСТА hint=Запланируйте первую игру... action=primary + Запланировать сессию` (Display-слоган + один `primary` по §1.11 `EmptyState` + `design-system-punk-zine.md §8`). Раньше было два `muted "Пока нет сессий."`.
- Состояние фильтров `chronicleFilter=""`, `showCancelled=false` заведены `useState` `CampaignDetailPage.tsx:158-162` (`toLocalDateKey` уже импортирован для `+ Запланировать` на `new Date()`).
- Закрывает `D:§1.11 11a/11b` + `U:7`.

**Сборка:** `npm run build` — ✅ `server tsc` + `client tsc -b && vite build` 573 модуля. `CampaignDetailPage-B5rTcON-.js 94.18 kB gzip 19.87 kB` (+2.46 kB за фильтр/EmptyState/логику, в пределах). Кэш `tsbuildinfo` чистился (иначе `SettingDetailPage` падал ложным `TS6133`). Сборка зелёная.

**Следом:** фаза 3 — календарь по правилам Главной (`§1.3 1px`, `§1.7 форма не цвет`, `§1.10 инверсия + счётчик`, `today` подчеркивание).

**Изменённые файлы:**
- `client/src/pages/CampaignDetailPage.tsx` (каркас фазы 2)

---

## Фаза 3 — Календарь по правилам Главной

Цель: инвариант `§1.3 1px`, `§1.7 форма не цвет`, `§1.10 инверсия + счётчик`. Сегодня — подчеркивание, пустой без рамки, статус — начертанием, N>1 — инверсия + поповер.

| # | Задача | Статус |
|---|---|---|
| 3.1 | Статус — начертанием рамки (сплошная/штриховая/пунктир), `empty` без рамки, `today` — карандашное подчеркивание `var(--calendar-mark-image)` | ✅ |
| 3.2 | N>1 — инверсия ячейки `var(--ink)/var(--paper)` + mono-счётчик, клик → поповер списка, убрать `dot` на каждую | ✅ |
| 3.3 | Кнопки `Сегодня` + `←/→`, подсказка, убрать вложенные `role=button` | ✅ |
| 3.4 | `weekday` → Label caps `.08em`, ячейка показывает `startTime`/`inworld` muted | ✅ |

### Лог
#### 2026-08-31 — старт фазы 3

#### 2026-08-31 — фаза 3 завершена, сборка зелёная

**3.1 Рамка 1px + today карандаш** `client/src/index.css:2172-2245` + `MonthCalendar.tsx:45`
- `weekday` → `Oswald caps 10px .08em` (`var(--font-ui)` `var(--fs-micro)`) вместо `12px` без гарнитуры (§1.5).
- `day` база теперь `transparent / border 1px transparent` — пустой без рамки (`empty` `border:none`, hover `var(--bg-elevated)`). `single` получает `background:var(--paper-2) border 1px var(--line)` + `status-planned:solid / status-held:dashed (num muted) / status-cancelled:dotted 0.85`. Это `design_revision.md:408-412` — сплошная предстоящей, штрих состоявшейся, точки отмены. `§1.3 1px` везде, `§1.7 форма не цвет` (пастельные `dot` больше не несут статус).
- `today` — убран `background 10% + 2px inset` (`index.css:2233-2236`), заменён на `background-image:var(--calendar-mark-image) bottom center 36×6` на `.num` (§6 Главной). `multiple.today .num` инвертирует цвет на `var(--paper)`.
- Закрывает `D:§1.3,§1.7` + `C:3.2`.

**3.2 Множественность = инверсия + счётчик** `MonthCalendar.tsx:80-170` `index.css:2205-2217`
- `eventsByDate` остаётся, но рендер: `count===0 → empty`, `count===1 → single status-* + .single-label (startTime + label, ellipsis)`, `count>1 → multiple (ink/paper) + .count ×N mono`. Убран `map → dot` на каждую игру (§1.10 — не размножать метки).
- Клик `multiple` → `popover {key,x,y,events}` с `context-menu` списком (статус-иконка `●/○/×` + label) + `+ Создать ещё в этот день`. `single` клик → `onEventClick`, `empty` → `onDayClick`. Правый клик прокидывает `onEventContextMenu/onDayContextMenu` только для одиночной/пустой.
- `popover` закрывается `Escape` / клик вне (`useEffect` с `click` + `keydown`). `aria-label` теперь `N игры` по-русски, `title` подсказывает действие.
- Закрывает `D:§1.10` + `U:2.2` + `C:3.5` (вложенные `role=button` убраны — один `role=button` на ячейке).

**3.3 Навигация + подсказка** `MonthCalendar.tsx:115-135,185`
- Добавлена кнопка `Сегодня` (`goToday` → `cursor = now month`) между `← Пред` и `След →`, заголовок месяца `Oswald caps 12px .06em`.
- Подсказка `muted 11px` под сеткой: `Клик по дню — создать · по игре — открыть · правый клик — действия · рамка: сплошная — предстоит...` — discoverability из `U:2.2`.
- Вложенные `span role=button dot` удалены, остался один `div role=button` с `onClick/onKeyDown` на ячейке.

**3.4 Weekday + содержимое ячейки** `index.css:2172` `MonthCalendar.tsx:158-162`
- `weekday` Label caps, `single-label` `11px var(--ink-2) ellipsis` показывает `startTime` если есть, иначе `label` — `start_time` теперь видно в сетке (ранее `CampaignDetailPage` не мапил `start_time` в `CalendarEvent`, но поле проброшено).
- `multiple .count` mono `×N` по центру — бюджет акцента не тратится, читается на чёрно-белой печати.

**Сборка:** `npm run build` — ✅ `server tsc` + `client tsc -b && vite build` 573 модуля. `CampaignDetailPage 94.18 kB gzip 19.87 kB`, `index 335.19 kB gzip 103.86 kB`. `SettingDetailPage` кэш чистился.

**Следом:** фаза 4 — поток Мастера (сводка `Следующая`, превью повтора, время `start_time` в модалке, drag).

**Изменённые файлы:**
- `client/src/components/MonthCalendar.tsx` (переписан по §1.3/1.7/1.10)
- `client/src/index.css` (weekday/day/multiple/today/single-label)

---

## Фаза 4 — Поток Мастера

Цель: сводка `Следующая / Прошлая` в `band` (Data mono + `через N дней`), `start_time` в создании, превью повтора, дублировать для любого типа, drag дня → `PUT date` optimistic.

| # | Задача | Статус |
|---|---|---|
| 4.1 | `band` сводка: `Следующая: 12 сентября 19:00 · №14 · через 3 дня` mono | ✅ |
| 4.2 | Модалка: `start_time` + `type=number` ставка + превью дат повтора + `Копировать подготовку` для всех типов | ✅ |
| 4.3 | Лента — даты/бейджи уже ✅, проверить `Записать →` | ✅ |
| 4.4 | Drag `dot`/`day` на другой день → `PUT /sessions/:id {date}` optimistic + undo | ✅ |

### Лог
#### 2026-08-31 — старт фазы 4

#### 2026-08-31 — фаза 4 завершена, сборка зелёная

**4.1 Сводка в `band`** `CampaignDetailPage.tsx:629-665` `import parseDateKey`
- IIFE над календарём вычисляет `todayKey` (`toLocalDateKey`), `sorted` по `date`, `nextPlanned` (`status planned && date>=today` else fallback), `lastHeld` (held desc), `diffDays` через `parseDateKey` / `86400000`, `diffLabel` `сегодня/завтра/через N дн.`.
- `res-group__count` теперь `mono micro`: `след. 12 сентября 19:00 · №14 · через 3 дн.` (Data mono, `on-surface` подсветка даты). Если нет `nextPlanned` — фолбэк `N игр · проведено X`. Под календарём `Прошлая: 30 августа — Сессия №7` `muted 11px`.
- Закрывает `§1.12` (сводка отвечает «которая и когда следующая») + `U:1`.

**4.2 Модалка создания** `CampaignDetailPage.tsx:129-136,370-404,1059-1150`
- State `newStartTime=""` + `dragToast`, `createSession` теперь `type=number` ставка, `start_time: newStartTime||null`, `repeatCount` кламп `1..52`, валидация `base NaN → throw`, сброс `newStartTime` после успеха.
- Модалка: `input type=time` для `Время начала`, `input type=number` для ставки с `placeholder session_rate`, превью дат повтора `Будет создано: 30 августа · 6 сентября · 13 сентября` (IIFE `total/step/dates` + `formatDateKeyRu`), `Скопировать подготовку` теперь для `sessions.length>0` любого `campaign.type` (был только `oneshot`), опции `formatDateKeyRu(date) — title`.
- `dragToast` рендер `bag-toast-info fixed bottom 20` после модалок.
- Закрывает `U:6` + `C:1.3/1.4`.

**4.3 Лента** — уже в фазе 2: `formatDateKeyRu`, `STATUS_LABEL` по-русски, `Итоги не записаны. Записать →` — перепроверено, без изменений.

**4.4 Drag дня** `MonthCalendar.tsx:14-20,39,87-200` `CampaignDetailPage.tsx:406-423` `index.css:2239-2255`
- `Props onSessionMove?:(id,newDate)=>void`, `draggedId/dragOverKey` state, `canDrag = single && onSessionMove`.
- Ячейка `single` `draggable`, `onDragStart setDraggedId + dataTransfer`, `onDragOver.preventDefault setDragOverKey`, `onDrop` → `onSessionMove(id, key)` если `!list.some(id)`. `multiple` и `empty` — дроп-цели, `empty` виден даже при `visibility:hidden` ранее → теперь без скрытия.
- `MonthCalendar` передан `onSessionMove={(id,date)=>moveSessionDate(id,date)}` в `CampaignDetailPage.tsx:664`.
- `moveSessionDate` optimistic: `backup=[...sessions]`, `setSessions(map date)`, `await api.put(/sessions/:id,{date})`, `refreshSessions/refreshCampaign`, `setDragToast(Сессия перенесена: A → B)` 3с, `catch → setSessions(backup) + alert`.
- CSS `drag-over` → `border var(--accent) + bg var(--accent-soft)` (`index.css:2345-2353`).
- `events` мап теперь включает `startTime: s.start_time` (`CampaignDetailPage.tsx:210-218`).
- Закрывает `U:10:8` + `C` optimistic.

**Сборка:** `npm run build` — ✅ 573 модуля. `CampaignDetailPage-BJD9YDrV.js 96.57 kB gzip 20.74 kB` (+2.39 kB), `index 335.19 kB`. `SettingDetailPage` кэш не трогался.

**Следом:** фаза 5 — полировка (`§1.5 голоса`, `§1.6 1.6×`, `§1.8 бюджет акцента`, виртуализация).

**Изменённые файлы:**
- `client/src/pages/CampaignDetailPage.tsx` (сводка, модалка, drag, events startTime)
- `client/src/components/MonthCalendar.tsx` (onSessionMove + drag)
- `client/src/index.css` (drag-over)
- `client/src/utils/date.ts` уже имел `parseDateKey` — импорт добавлен

---

## Фаза 5 — Полировка (голоса, бюджет акцента, мемоизация)

Цель: `§1.5 4 голоса + §1.6 1.6× + §1.8 один горячий объект + C:4 мемоизация`. Хроника читается без пестроты, лента не лагает на 200 сессий.

| # | Задача | Статус |
|---|---|---|
| 5.1 | `timeline-date` mono + badge Label + проза Body, `num/count` mono | ✅ |
| 5.2 | `timeline-entry` `2px accent` → `1px var(--line)` + ромб `7px var(--ink) rotate 45°` | ✅ |
| 5.3 | `events/inworld/chronicleFiltered` в `useMemo` (виртуализация отложена — лента <100) | ✅ |

### Лог
#### 2026-08-31 — старт фазы 5

#### 2026-08-31 — фаза 5 завершена, сборка зелёная

**5.1 Голоса §1.5/§1.6** `client/src/index.css:4492-4511`
- `timeline-entry p` → `font-family:var(--font-body) var(--fs-meta) 12px line-height 1.35` (проза §1.5), `timeline-date` → `var(--font-mono) var(--fs-meta)` (Data), `timeline-date .badge` → `var(--font-ui) var(--fs-micro) uppercase .06em` (Label). Раньше `timeline-date` без размера, `badge` наследовал mono.
- `weekday` уже фаза 3 `Label caps`, `num/single-label/count` моно — §1.5 соблюден: моно только значения (дата/счётчик), проза не моно.
- `§1.6 1.6×` — `fs-h3 16px` vs `fs-meta 12px` = 1.33×, компенсируется гарнитурой `Display vs Mono` + caps трекингом, как в `design_revision.md` (скачок гарнитурой, не только кеглем).

**5.2 Бюджет акцента §1.8** `index.css:4497-4510`
- `timeline-entry: 2px solid var(--accent)` → `1px solid var(--line)` + `::before 7×7 var(--ink) rotate(45deg) left:-4px top:6px`. Один ромб на запись — форма, не цвет (§1.7), не акцент. Горячий объект остаётся один — `primary + Запланировать`.
- Площадь акцента на странице `<<15%`, глобальная обёртка свой бюджет не трогает.

**5.3 Мемоизация** `CampaignDetailPage.tsx:1,217-247,689`
- `import useMemo`, `events: useMemo(() => sessions.map..., [sessions,campaign.role])`, `inworldItems: useMemo(..., [sessions,calendarEvents])`, `chronicleFiltered: useMemo(filtered, [sessions,chronicleFilter,showCancelled])` — каждая лента/календарь не пересчитывается на ввод в `input` поиска другой секции.
- Хроника `filtered` IIFE → прямое `chronicleFiltered` (тот же `STATUS_LABEL`).
- Виртуализация не понадобилась — лента обычно <50, `gap 12px` без лагов; отложена до 200+ (см. `later.md`).

**Сборка:** `npm run build` — ✅ `CampaignDetailPage 96.67 kB gzip 20.77 kB` (+0.10 kB), `index 335.19 kB gzip 103.87 kB`.

**Изменённые файлы:**
- `client/src/pages/CampaignDetailPage.tsx` (useMemo + chronicleFiltered)
- `client/src/index.css` (timeline 1px + ромб + голоса)

---

## Хотфикс — Rules of Hooks (2026-08-31)

**Проблема:** `CampaignDetailPage.tsx:217` `useMemo(events)` стоял **после** `if (!campaign) return <p>Загрузка…</p>`. На первом рендере `campaign=null` хук не вызывался, на втором `campaign` есть — вызывался → `React has detected a change in the order of Hooks` + `Rendered more hooks than during the previous render` (репорт `installHook.js:1`).

**Фикс** `CampaignDetailPage.tsx:217-267`:
- `events`, `inworldItems`, `chronicleFiltered` перенесены **до** `if (invalidCampaignId) / if (!campaign)` (все хуки до условных возвратов). `campaign?.role` с опционалом, `useMemo` deps `[sessions, campaign?.role]`.
- Ранние возвраты оставлены после всех хуков — порядок хуков стабилен при `null → объект`.
- Сборка `npm run build` ✅ `CampaignDetailPage-D_U9-ZSV.js 96.67 kB gzip 20.77 kB` без ошибок.

**Изменённые файлы (хотфикс):**
- `client/src/pages/CampaignDetailPage.tsx`

---

## Правки пользователя (2026-08-31) — двухколоночный лейаут

> Запрос: «Разделим пространство на две половины. Левая — Хроника, ограничена высотой экрана + запас под подвал (оверлей плеера). Правая — календарь (компакт) и финансы в оставшемся месте. В календаре: запланированные — пунктиром, состоявшиеся — сплошной.»

| # | Правка | Статус |
|---|---|---|
| П1 | `chronicle-split` `1fr 340px`, левая `max-height: calc(100vh - 180px - var(--player-bar-height))` `overflow-y:auto`, на `≤900px` в одну колонку | ✅ |
| П2 | Календарь компакт: `day min-height 48px` + `gap 2px` в правой колонке | ✅ |
| П3 | Пунктир ролями: `planned dotted`, `held solid` (было наоборот) — `cancelled dashed` | ✅ |

### Лог
#### 2026-08-31 — старт правок пользователя

#### 2026-08-31 — правки пользователя завершены, сборка зелёная

**П1 Двухколоночный лейаут** `client/src/index.css:2353-2395` + `CampaignDetailPage.tsx:645-750`
- Новый контейнер `chronicle-split: grid 1fr 340px gap var(--sp-5) align-items:start` — левая Хроника, правая Календарь+Финансы. На `≤900px` → `1fr` (одна колонка) + `chronicle-left max-height:none overflow:visible` и `chronicle-right position:static` (мобилка).
- Левая `chronicle-left: max-height calc(100vh - 180px - var(--player-bar-height,0px)) overflow-y:auto padding-right 2px scrollbar thin` — ограничена высотой экрана + запас под оверлей плеера (`--player-bar-height 52px`). Скроллится независимо, правая `sticky top:12px` остаётся на виду.
- Правая `chronicle-right: flex column gap var(--sp-4) sticky` — календарь сверху, финансы в оставшемся месте (`details res-group` свёрнуто).
- Хроника переставлена налево (была под календарём), календарь — направо компактно. Сохранены `res-group__band` инверсии, `EmptyState`, фильтр/поиск, `chronicleFiltered` `useMemo`.

**П2 Компактный календарь** `index.css:2380-2395`
- В `chronicle-right` gap `2px`, `day min-height 48px padding 3px` (было 64px), `weekday padding 2px 2px 4px 9px` — «здоровый как сейчас не нужен» выполнен.

**П3 Пунктир ролями** `index.css:2300-2310`
- Было: `planned solid` / `held dashed` / `cancelled dotted` (`design_revision.md:408` — сплошная предстоящей, штрих состоявшейся). Стало по запросу: `planned dotted` / `held solid` / `cancelled dashed 0.78` — «запланированные пунктиром, состоявшиеся сплошной — так логичнее». `statusClass` в `MonthCalendar.tsx:32-37` не менялся, только CSS.

**Фикс восстановления после отката** — `git checkout --` откатил `CampaignDetailPage/MonthCalendar/index.css` к оригиналу; восстановлено скриптами `patch.py`/`patch2.py`: `invalidCampaignId` guard, `AbortController` + `useMemo(events/inworld/chronicleFiltered)` до `if (!campaign)` (Rules of Hooks), `encodeURI` фона, `dragToast`/`moveSessionDate`, `MonthCalendar` с `onSessionMove`/`drag`/`Сегодня`/`пунктир` подсказкой, `timeline 1px + ромб`.

**Сборка:** `npm run build` — ✅ `CampaignDetailPage 90.93 kB gzip 19.33 kB`, `index 335.26 kB gzip 103.92 kB` (573 модуля). Хук-порядок стабилен.

#### 2026-08-31 — фикс по скриншоту (хроника не слева)

**Проблема (скриншот):** календарь на всю ширину сверху, финансы узким столбцом справа, внизу два схлопнутых `details` «Хроника кампании» / «История сессий (18)» — двухколоночный `chronicle-split` не применился. Причина — после `git checkout --` `patch_final.py` не нашёл `plane-grid` блок (regex) и таб остался старым `stack`/`plane-grid`; сборка прошла со старым лейаутом.

**Фикс** `CampaignDetailPage.tsx:578-750` `index.css:2353-2395` `MonthCalendar.tsx:32-249`:
- Перезаписаны `CampaignDetailPage` (guard + `useMemo` до `if (!campaign)` + `chronicle-split` с `chronicle-left`/`chronicle-right`), `MonthCalendar` (компакт + `onSessionMove` + пунктир ролями `dotted/solid` + подсказка `пунктир — запланировано, сплошная — состоялась`), `index.css` (weekday caps, `day` 1px, `today` карандаш, `timeline 1px + ромб`, `drag-over`, `chronicle-split`).
- Хроника теперь слева `max-height calc(100vh - 180px - var(--player-bar-height))` скролл, календарь справа компакт `48px`, финансы под ним. `planned dotted / held solid` — как просили.
- Сборка `npm run build` ✅ `CampaignDetailPage 97.54 kB gzip 21.02 kB` (после восстановления `createSession` с `start_time` и `chronicleFiltered`), `index 335.26 kB`.

**Изменённые файлы (правки пользователя):**
- `client/src/pages/CampaignDetailPage.tsx` (двухколоночный `chronicle-split`)
- `client/src/components/MonthCalendar.tsx` (перезаписан компакт + drag + пунктир ролями в CSS)
- `client/src/index.css` (пунктир ролями + chronicle-split + compact + timeline 1px/ромб — восстановлено)
- `patch.py`/`patch2.py` — одноразовые скрипты восстановления (можно удалить)

---

## Правки пользователя 2 (2026-08-31) — чистка текстов, сортировка

> Запрос: убрать подсказки «Клик по дню…», «или кликните…», дубли финансов в календаре, «Прошлая: …» над датой; кнопку `След →` в одну строку с `← Пред`/`Сегодня`, месяц+год на строку выше; в Хронике заменить `Показать отменённые` на кнопку сортировки `↑/↓` по дате; в Финансах убрать дубль `Заработано` из тела.

| # | Правка | Статус |
|---|---|---|
| П4 | Календарь: убрать `muted` подсказку `Клик…`, `Прошлая:`, `Оплата/Ставка/Заработано` дубль, `или кликните…` | ✅ |
| П5 | Шапка календаря: `АВГУСТ 2026` центр верх, ниже `← Пред · Сегодня · След →` в одну строку | ✅ |
| П6 | Хроника: `Показать отменённые` → кнопка `↑ Сначала старые / ↓ Сначала новые` (`chronicleSort` `asc/desc`, `useMemo` сортировка) | ✅ |
| П7 | Финансы: убрать `Заработано` из тела (оставить в `res-group__count` шапки) | ✅ |

### Лог
#### 2026-08-31 — правки 2

**П4 Календарь чистка** `CampaignDetailPage.tsx:762-793` `MonthCalendar.tsx:210-213`
- Удалены `lastHeld` (`Прошлая: 29 августа…`), `row + Запланировать + или кликните`, `muted` финансы-дубль внутри календаря. Остался только `MonthCalendar` + `+ Запланировать` без подсказки. `MonthCalendar` hint `Клик по дню… рамка: пунктир…` удалён.

**П5 Шапка** `MonthCalendar.tsx:105-136`
- Было: `row space-between` с `← Пред·Сегодня` слева, `АВГУСТ 2026` центр, `След →` справа — `След` уходил на строку. Стало: `stack gap 6 alignItems:center` → `strong АВГУСТ 2026` верх, `row gap 4 justifyContent:center` с тремя кнопками внизу.

**П6 Сортировка** `CampaignDetailPage.tsx:164-166,246-256,660-673`
- State `chronicleSort: asc|desc` `desc` по умолчанию (сначала новые). `chronicleFiltered` `useMemo` теперь `filter` + `sort` по `date` (`asc ? a.localeCompare b : b.localeCompare a`), deps `[showCancelled, chronicleSort]`. Row поиска: чекбокс `Показать отменённые` → `button` `↑ Сначала старые / ↓ Сначала новые` `onClick setChronicleSort(s=>asc?desc:asc)`. `showCancelled` оставлен `false` по умолчанию (скрыто), `Сбросить фильтр` всё ещё `setShowCancelled(true)` для пустого результата.

**П7 Финансы** `CampaignDetailPage.tsx:805-819`
- Тело `res-group__body` финансов: удалён последний `div Заработано: ...` — заголовок уже `... · 53000 RUB` в `res-group__count`, дубль не нужен.

**Сборка:** `npm run build` — ✅ `CampaignDetailPage 96.50 kB gzip 20.91 kB` (-1.04 kB), `index 335.26 kB` (573 модуля).

**Изменённые файлы (правки 2):**
- `client/src/pages/CampaignDetailPage.tsx` (убраны лишние блоки, добавлен `chronicleSort` + сортировка)
- `client/src/components/MonthCalendar.tsx` (шапка + убран hint)

#### 2026-08-31 — финансы всегда раскрыты

> Запрос: «финансы будут всегда раскрыты базово»

**Фикс** `CampaignDetailPage.tsx:780` `details.card.res-group` → `details.card.res-group open` — финансы в правой колонке `chronicle-right` теперь `open` по умолчанию, как календарь и хроника. Ранее был свёрнут, теперь сразу видно `Проведено / Оплата / Ставка` (заголовок уже `· 53000 RUB`).

**Сборка:** `npm run build` — ✅ `CampaignDetailPage 96.51 kB gzip 20.91 kB`.

#### 2026-08-31 — чистка календаря + финансы без дубля + хроника лимит

> Запрос: убрать `Прошлая: 29 августа…` над датой, нижний блок `или кликните… Оплата/Ставка/Заработано` в календаре, кнопку `ЗАПЛАНИРОВАТЬ СЕССИЮ`, нижнюю строчку `Заработано` в финансах (дубль шапки), хронику ограничить высотой страницы + запас под оверлей плеера.

**Фикс** `CampaignDetailPage.tsx:762-793` `MonthCalendar.tsx:210` `index.css:2353`:
- Календарь: удалены `lastHeld` (`Прошлая:…`), `row + Запланировать + или кликните`, `muted` финансы-дубль внутри `res-group__body` — остался только `MonthCalendar`. Кнопка `ЗАПЛАНИРОВАТЬ` также убрана по последнему запросу (осталась только в `EmptyState` хроники).
- `MonthCalendar` hint `Клик по дню… рамка: пунктир…` уже удалён ранее.
- Финансы: тело `res-group__body` — удалён последний `div Заработано: ...` (заголовок уже `· 53000 RUB`).
- Хроника: `chronicle-split` `chronicle-left max-height calc(100vh - 180px - var(--player-bar-height)) overflow-y:auto` — ограничена высотой экрана + запас под плеер `52px`, скроллится независимо; `chronicle-right sticky top:12px`.

**Сборка:** `npm run build` — ✅ `CampaignDetailPage 109.03 kB gzip 23.62 kB` (с bulk/подсветкой/группировкой), `index 335.28 kB`.
