import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rpgmanager.gm.mobile',
  appName: 'RPG Manager — Мастер',
  webDir: 'client/dist'
};

export default config;
